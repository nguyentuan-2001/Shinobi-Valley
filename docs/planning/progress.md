# Tiến độ dự án

> File này ghi lại **chức năng nào đã code xong, ở file nào, còn thiếu/lỗi gì** — đọc file này trước khi bắt đầu 1 phiên làm việc mới để biết đang đứng ở đâu, khỏi phải dò lại toàn bộ code.
>
> **Quy tắc bắt buộc**: mỗi khi thêm/sửa xong 1 chức năng, phải cập nhật vào file này ngay trong cùng phiên làm việc đó — thêm dòng mới vào đúng Sprint, hoặc thêm vào "Vấn đề đã biết" nếu chưa xong hẳn. Không để tồn đọng qua phiên sau mới ghi. Quy tắc gốc nằm ở `CLAUDE.md`.

---

## Đang ở đâu

**Sprint 0, 1, 3 xong. Sprint 2 (hệ thống ô đất) xong phần cuốc/trồng/lớn theo thời gian thực** — còn thiếu tưới nước/độ ẩm, thu hoạch, inventory thật (đúng theo scope Sprint 4 trong `dev-schedule.md`, không phải thiếu sót của Sprint 2). Sprint 3 (game clock) đã xong, làm trước Sprint 4 theo đúng thứ tự lịch trình.

**⚠️ Cần gen gấp 20 file `<id>_harvest.png` (crops)** — code đã đổi sang dùng file harvest riêng biệt với item icon, prompt có sẵn ở `art-refs/items/crops.md`, nhưng chưa có file thật nào → ô đất cây chín trên map đang lỗi texture, xem chi tiết ở "Vấn đề đã biết" Sprint 2.

---

## Sprint 0 — Data pipeline & project structure ✅

- [src/data/types.ts](../../src/data/types.ts) — TS interface cho toàn bộ schema (Crop, Item, Weapon, Armor, Skill, Monster, Npc, Quest, Recipe, Fish, Profession, GachaPool, GameEvent, SaveState...).
- [src/data/DataLoader.ts](../../src/data/DataLoader.ts) — `preloadGameData()` + `initGameData()`, export `GameData` để tra dữ liệu.
- [src/systems/SaveManager.ts](../../src/systems/SaveManager.ts) — `createDefaultSaveState`, `saveGame`/`loadGame`/`hasSaveGame`/`clearSaveGame` (localStorage).
- `public/data/*.json` — 13 file mẫu (crops, items, weapons, armor, skills, monsters, npc, quests, recipes, fish, professions, gacha, events), mỗi file 1 entry mẫu.

## Sprint 1 — Player movement + tilemap nông trại 🟡 (gần xong)

- **~~Tileset Farm dạng tile-grid~~ — ĐÃ THAY THẾ bằng ảnh nền tĩnh (xem mục "Đổi nền map sang ảnh tĩnh" bên dưới).** 14 tile gốc vẫn còn nằm trong `public/assets/tilesets/farm/` (không xóa) nhưng không còn dùng trong code từ khi đổi sang `background.png`.
- **`public/assets/tilemaps/farm_test.json`** — map test bằng Tiled, **không còn được `GameScene.ts` load nữa** (đã đổi sang ảnh nền tĩnh), vẫn giữ file lại trên đĩa, chưa xóa.
- **[src/entities/Player.ts](../../src/entities/Player.ts)** — `Phaser.Physics.Arcade.Sprite`, di chuyển 4 hướng bằng **phím mũi tên** (đã bỏ WASD theo yêu cầu), animation đổi theo hướng (idle_front, walk_front/back/side), hitbox thu nhỏ ở chân. **Phím Space** kích hoạt animation `attack` (đứng yên, chạy hết 8 frame rồi tự quay lại idle/walk) — chỉ có bản quay mặt ra trước, dùng chung cho mọi hướng ở bản Alpha (đúng ghi chú asset-manifest.md). Chưa có logic gây sát thương/hitbox tấn công thật — mới chỉ là animation.
- **[src/scenes/GameScene.ts](../../src/scenes/GameScene.ts)** — spawn player, va chạm ẩn cho nhà/giếng/chuồng (xem mục ảnh nền tĩnh), camera `startFollow` bám player.
- **[src/scenes/PreloadScene.ts](../../src/scenes/PreloadScene.ts)** — load toàn bộ data JSON + ảnh nền farm + spritesheet player (2 giới tính).
- **Prompt gen ảnh Farm** đã thêm vào `art-refs/world/maps.md` (Map 0 — Nông Trại) kèm định hướng art chung chống lệch tone "toylike" cho toàn bộ file.

### Vấn đề đã biết / chưa xong

- **Map Farm giờ dùng ảnh nền tĩnh (`background.png`), không còn Tiled/tile-grid** (xem mục "Đổi nền map Farm sang ảnh nền tĩnh" ở trên) — va chạm chỉ có 3 vùng ẩn tay (nhà/giếng/chuồng), luống đất trong ảnh chưa có va chạm/logic gì. Sprint 2 (`FarmTile`) sẽ cần nghĩ lại cách xác định + lưu state từng ô đất trồng trọt vì không còn dựa vào tile index Tiled được nữa.
- `village/` tileset vẫn chưa có prompt art-refs riêng — chưa cắt ảnh.
- Player còn thiếu asset: `idle_back/idle_left/idle_right`, `attack_back/attack_side` (đang dùng tạm frame khác thay thế theo asset-manifest.md).
- Seam/grid line mờ giữa tile — **không còn liên quan** vì map Farm không còn ghép tile nữa (đã đổi sang ảnh nền tĩnh), có thể vẫn còn ở map khác nếu sau này dùng lại tile-grid.
- **Đã chốt chuẩn mới: 8 frame/hành động/hướng** (thay vì 7/3/3/3/4 như trước) — đã viết lại `art-refs/characters/player.md`, thêm mục "Gen dạng sprite sheet" (10 prompt gộp cả 8 frame vào 1 ảnh, nền trắng đặc). Cập nhật số frame mục tiêu trong `docs/data/asset-manifest.md`. **Đã xóa mục "Nhân Vật Gốc — gen từng frame riêng"** (bản dự phòng 80 prompt/1-ảnh-1-frame, viết ban đầu nhưng chưa từng dùng) vì thực tế tool đang dùng gen được thẳng sprite sheet — chỉ còn giữ mục "Gen dạng sprite sheet" (đang dùng thật, đã tạo ra toàn bộ asset women). File từ ~620 dòng còn ~263 dòng.
- **Prompt Walk Trước/Sau — tay vung, đã thử 4 lần, vẫn chưa chốt được:**
  1. Bản gốc: chỉ frame 1/5 (chạm đất) có mô tả tay, frame giữa không nhắc → tay gần như đứng yên.
  2. Lần 1 dùng "fully" + câu chốt "must swing continuously... never staying still" → test thật: tay vung quá đà như đấm box ở frame 1/5, frame giữa vẫn đứng yên.
  3. Lần 2 dùng "gently"/"naturally" (quá mơ hồ) → test thật: tay đứng yên hoàn toàn, tệ hơn cả bản gốc.
  4. Lần 3 dùng mốc cụ thể "fist reaching about to hip level" + "relaxed hanging position" cho frame giữa → test thật: **vẫn chưa rõ ràng** (user báo "chưa rõ ràng lắm"), gần như không thấy khác biệt giữa các frame.
  5. **Lần 4**: đưa mô tả tay lên đầu prompt + ví dụ tham chiếu "marching soldier" + yêu cầu nắm tay "visibly outside/offset from the body outline" (lộ hẳn ra ngoài bóng cơ thể) cho mọi frame. **Đã gen thử (`women-white-huong-truoc-new.png`) và tích hợp vào game — có cải thiện thật**: tay + chân đều thấy khác biệt rõ giữa các frame khi chạy animation trong game (không còn dính cứng như lần 3), nhưng biên độ vung vẫn khá tinh tế/nhẹ, chưa phải kiểu vung tay rõ rệt mạnh. **Đã ghi đè `walk_front_01..08.png` + `walk_front_strip.png` của women, PreloadScene không cần sửa (kích thước 162×334 khớp sẵn).** Coi như tạm chấp nhận được, không tiếp tục dò thêm trừ khi user yêu cầu.
  - **Walk Sau đã gen lại theo lần 4 và tích hợp xong** (`women-white-huong-sau-new.png` → `walk_back_01..08.png` + strip, 162×334, cùng toạ độ cắt với walk_front v2) — verify qua ảnh chụp thật trong game: đúng hướng lưng xuyên suốt (không lộ mặt), tay/chân biến đổi nhẹ giữa các frame, cùng chất lượng với walk_front v2, không lỗi console. **Cả walk_front và walk_back của women giờ đều dùng bản "lần 4" (marching soldier + tay lộ ngoài bóng cơ thể).**
  - Walk Nghiêng (`walk_side`) chưa áp dụng thay đổi này (user chỉ yêu cầu "đi tiến và lùi").
- **Bài học prompt walk Trước/Sau**: mô tả chân kiểu "duỗi thẳng ra trước/ra sau" (fore-aft) khiến AI hiểu nhầm thành góc nhìn nghiêng. Đã sửa sang mô tả "gập gối/nhấc chân" (không fore-aft) + câu chốt "shoulders and hips must stay squared..., must NOT be drawn from a side angle or 3/4 turned view". Hướng Nghiêng (`walk_side`) vẫn giữ fore-aft vì đó đúng là góc nhìn nghiêng. **Đã test thật với asset women mới — hướng đúng hoàn toàn, không còn nghiêng.**
- **Bài học "càng thêm mô tả càng xấu"**: nhồi quá nhiều chi tiết cùng lúc (nếp vải, viền đỏ, băng quấn tay, rim light, "polished reflective", "confident smirk") làm AI vẽ rối (mũ giáp xám to bất thường, biểu cảm méo) thay vì đẹp hơn. Đã rút gọn: bỏ băng quấn tay/fold-shading/rim light/"polished reflective", đổi "confident smirk" → "warm cheerful smile". **Đã test thật với asset women mới — kết quả đẹp, sạch, không còn lỗi mũ giáp/biểu cảm méo.**
- **`women` đã hoàn thành bộ 8-frame mới cho toàn bộ 5 hành động** (idle_front, walk_front, walk_back, walk_side, attack) — cắt từ `public/images/Characters/Women/none-bg/women-{dung-yen,huong-truoc,huong-sau,nghiêng-phai,white-attack-pose}.png` (nền đã xóa sẵn). idle/walk mỗi frame 162×334, attack riêng 170×335 (tay vung rộng hơn nên bbox khác). Script cắt dùng xong đã xóa (không lưu lại trong repo, chỉ giữ output trong `public/assets/sprites/player/women/`). Đã cập nhật `Player.ts` (`FRAME_SIZES`, `WALK_CONFIG` theo giới tính — women dùng `generateFrameNumbers` full 8 frame, men vẫn dùng mảng frame cũ đã lọc) và `PreloadScene.ts` (frame size load theo từng action × giới tính qua bảng `frameSizes`, không còn dùng chung 1 kích thước cho cả gender). Đã verify bằng ảnh chụp thật cả 4 hướng di chuyển + load asset attack không lỗi — animation mượt, đúng hướng, không lỗi console.
- **Ảnh nguồn thô KHÔNG bị xóa** — các file `women-white-*.png` (bản nền trắng trước khi rembg) và `public/images/Characters/women.png` (sheet gốc hệ cũ) vẫn giữ nguyên trong `public/images/`, chỉ ghi đè output trong `public/assets/sprites/player/women/`. User đã xác nhận không muốn xóa gì ở bước này.
- **`men` vẫn còn bộ cũ** (idle 7 frame, walk 3 frame/hướng dùng tạm 2 frame hợp lệ, attack 4 frame, kích thước 164×213) — chưa gen lại theo chuẩn 8-frame mới.
- **Gameplay mặc định đã đổi sang `'women'`** trong `GameScene.ts` (trước đó là `'men'`) — vì asset women mượt/đẹp hơn hẳn. Muốn test lại bộ men thì đổi tham số gender khi tạo `new Player(...)`.
- **Giảm size nhân vật in-game**: thêm `SPRITE_SCALE` theo giới tính trong `Player.ts` (men 0.38, women 0.24) + gọi `setScale()` ở constructor — trước đó sprite render nguyên kích thước gốc (162×334 với women), to gấp nhiều lần tile 32×32. Đã verify bằng ảnh chụp thật, nhân vật giờ cao khoảng 1.5-2 lần tile, hợp lý với map.
- **Y-sort + bóng đổ (2.5D giả lập) — "Phương án 2"**: map top-down 2D thuần vẫn phẳng, không có cảm giác chiều sâu như game tham chiếu "Avatar" (Avatar Teamobi). Thay vì vẽ lại toàn bộ art theo góc nghiêng (phương án 1, tốn công + rủi ro lệch tone với ~210 decoration đã xong), chọn làm bằng code trước:
  - **Y-sort**: `Player.update()` gọi `this.setDepth(this.y)` mỗi frame; `GameScene.placeFarmProps()` gọi `.setDepth(y)` một lần khi đặt mỗi prop (fence/pond/signpost/hay_bale/wood_pile/grass_tuft) theo toạ độ chân (anchor Y). Nhờ vậy prop nào có Y lớn hơn (thấp hơn trên màn hình) sẽ vẽ đè lên object phía trên — player có thể đi "trước" hoặc "sau" prop tuỳ vị trí, giống hiệu ứng độ sâu.
  - **Bóng đổ**: texture ellipse mờ đen (`shadow_oval`) tạo 1 lần bằng `Phaser.GameObjects.Graphics.generateTexture()` (không cần asset PNG riêng) trong `GameScene.createShadowTexture()`. Player có 1 shadow image riêng theo sát chân (`shadowOffsetY` tính từ frame height/scale từng giới tính), cập nhật vị trí + depth (`y - 1`, luôn thấp hơn depth player) mỗi frame trong `update()`. Prop có chiều cao thật (rào, biển báo, rơm, củi) cũng có bóng tương tự, đặt 1 lần lúc khởi tạo; ao nước (phẳng) và cỏ trang trí (quá nhỏ) không cần bóng.
  - Tầng nền tilemap (`ground` layer) set `depth = -10000` cố định để luôn nằm dưới cùng, không tham gia Y-sort.
  - **Đã verify bằng Puppeteer**: teleport player qua `window.__game` (debug expose tạm, đã xoá lại sau khi test) để đứng ở Y thấp hơn/cao hơn 1 prop (hay_bale) đúng điểm giao cắt — xác nhận player bị che khi Y nhỏ hơn prop, và đè lên prop khi Y lớn hơn, đúng như kỳ vọng Y-sort. Bóng đổ hiển thị rõ dưới chân player và dưới prop có chiều cao. Theo yêu cầu user "thử phương án 2 đi, nếu xấu tôi sẽ làm lại phương án 1" — kết quả code này coi như bản thử đầu tiên, cần user tự đánh giá trực quan trong game xem đã đủ cảm giác chiều sâu chưa trước khi quyết định có cần quay lại phương án 1 (vẽ lại art nghiêng) hay không.
  - **Bài học bóng đổ v1 "trông giả"**: bản đầu dùng `Graphics.fillEllipse` tô đặc 1 màu đen alpha cố định, viền cứng — user phản hồi ngay "trông nó hơi giả" (nhìn như dán đè lên nền, nhất là trên tile đất cày có texture rõ). **Đã sửa sang gradient toả tròn** (`createCanvas` + `CanvasRenderingContext2D.createRadialGradient`, tâm đậm alpha 0.4 mờ dần về alpha 0 ở mép) thay vì tô đặc, đồng thời bật `texture.setFilter(Phaser.Textures.FilterMode.LINEAR)` riêng cho texture này (game đang bật `pixelArt: true` mặc định NEAREST — không bật LINEAR riêng thì gradient bị vằn/răng cưa thay vì mờ mượt). Đã verify lại bằng ảnh chụp thật — bóng giờ mờ dần tự nhiên, không còn viền cứng.
- **Đổi nền map Farm từ tile-grid sang ảnh nền tĩnh**: user phát hiện tile `soil_tilled_wet` (đất đã tưới) trông như nước xanh chứ không giống đất ẩm khi lặp lại nhiều ô — hỏi có thể dùng `background.png` (ảnh minh hoạ toàn cảnh nông trại, cắt nguyên khối từ góc trên-trái `Farm.png` gốc, 569×388px, có sẵn nhà/luống đất/giếng/chuồng heo-bò) làm nền thay vì ghép tile không. Đã xác nhận với user 2 điểm quan trọng trước khi đổi:
  1. Bỏ hẳn tile-grid (`farm_test.json` + `tileset.png`) cho map Farm, chấp nhận mất khả năng track state từng ô đất theo `Tiled` — hệ thống `FarmTile` (Sprint 2) sẽ cần thiết kế lại cách xác định vùng đất trồng trọt (không còn dựa vào tile index nữa).
  2. Bỏ hết prop tự đặt bằng code cũ (hàng rào/ao/biển báo/rơm/củi) vì `background.png` đã vẽ sẵn nhà + chuồng heo-bò-có-hàng-rào ở vị trí khác — dùng đúng những gì ảnh có sẵn, tránh 2 bộ hàng rào/ao chồng chéo hình ảnh.
  - **`PreloadScene.ts`**: chỉ còn load `farm_background` (ảnh `background.png`), đã bỏ load `farm_test` tilemap JSON + toàn bộ ảnh prop riêng lẻ (fence/pond/signpost/hay_bale/wood_pile/grass_tuft/tileset).
  - **`GameScene.ts`**: `create()` giờ chỉ `add.image(background)` + 3 vùng va chạm ẩn (`Phaser.GameObjects.Zone` + static body, không vẽ gì, không texture) đặt đúng vị trí nhà/giếng/chuồng heo-bò trong ảnh (toạ độ ước lượng bằng mắt theo tỉ lệ ảnh, không cần khớp pixel chính xác). Luống đất trồng trọt trong ảnh **chưa có va chạm** (chưa cần thiết ở giai đoạn này).
  - **Phóng to nền theo `BACKGROUND_SCALE = 600/388`** (lúc này khung nhìn game còn là 800×600, xem mục đổi sang 960×540 ở cuối file): ảnh gốc 569×388 nhỏ hơn khung nhìn nên nếu giữ nguyên tỉ lệ sẽ lộ viền tối (dark bar) quanh map khi camera không scroll đủ xa để lấp đầy màn hình. Phóng theo chiều cao cho khớp 600px, dư ra chút bề ngang (880 > 800) để camera vẫn có khoảng scroll thay vì đứng yên hệt — đã verify bằng ảnh chụp, không còn viền tối. **Toàn bộ mục này giờ đã lỗi thời** — `background.png` + `BACKGROUND_SCALE` không còn dùng, xem mục "Đổi tiếp nền map Farm sang `BaseMap.png`" ngay dưới.
  - **Đã verify va chạm 3 vùng ẩn bằng Puppeteer** (đi từ ngoài vào nhà/giếng/chuồng, xác nhận bị chặn đúng biên) — **rút ra bài học viết test**: dùng `sprite.body.reset(x,y)` để "teleport" nhân vật trong lúc test làm sai lệch body tạm thời (bỏ qua offset hitbox đã cấu hình, set thẳng body.position theo x,y truyền vào thay vì tính lại từ offset), khiến kết quả test đợt đầu trông như "va chạm không hoạt động" dù code game hoàn toàn đúng. Cách test đúng: chỉ `sprite.setPosition(x,y)` rồi đợi 1 tick vật lý để Phaser tự đồng bộ lại body theo offset, không gọi `body.reset()` khi chỉ muốn di chuyển tức thời.
  - File tile cũ (`farm_test.json`, `tileset.png`, và 13 ảnh tile lẻ trong `public/assets/tilesets/farm/`) lúc này **chưa xóa khỏi đĩa** — theo nguyên tắc không tự xóa file khi chưa chắc chắn được yêu cầu. **Đã xóa ở phiên dọn dẹp sau này**, xem mục "Rà soát + dọn dẹp cấu trúc thư mục" cuối file.
- **Đổi tiếp nền map Farm từ `background.png` sang `BaseMap.png`**: user cung cấp ảnh mới đẹp/chi tiết hơn hẳn (1774×887, đồng cỏ mở với lưới đường mòn chia lô, suối cắt ngang có 2 cầu gỗ, thác nước + vách đá góc trên-trái, ao nước góc dưới-phải, viền rừng thông + hoa anh đào, dãy núi/bầu trời phía xa) — **không còn nhà/giếng/chuồng** như bản cũ, nên bỏ hẳn 3 vùng va chạm ẩn cũ, thay bằng va chạm mới khớp địa hình ảnh này.
  - Ảnh mới (1774×887) đã lớn hơn khung nhìn game (960×540, sau khi đổi ở mục cuối file — lúc quyết định này khung nhìn còn là 800×600, kết luận vẫn đúng ở cả 2 mốc) ở cả 2 chiều nên **bỏ `BACKGROUND_SCALE`**, dùng nguyên tỉ lệ gốc (scale=1).
  - **Va chạm mới** (`GameScene.placeInvisibleObstacles()`): dải bầu trời/núi xa ở top (không phải mặt đất — 1 trong các kỹ thuật "background horizon art" đã bàn ở mục Y-sort phía trên), vách đá + thác nước chính góc trên-trái, đoạn suối nhỏ nối thác xuống sông chính, 3 đoạn sông ngang (chừa đúng 2 khoảng trống ở vị trí 2 cầu gỗ để đi qua được), và ao nước góc dưới-phải.
  - **Toạ độ suối/cầu lấy từ soi màu pixel thật** thay vì ước lượng bằng mắt thuần: cài tạm `pngjs` (gỡ lại sau khi xong, không lưu trong repo) để đọc RGB từng pixel của `BaseMap.png`, dò cột nào có màu nước (kênh B trội) để tìm chính xác dải sông + 2 khoảng trống là vị trí cầu, quét theo `x`. Cách này chính xác hơn hẳn so với đoán bằng mắt — bản đoán bằng mắt đầu tiên (dải sông đặt cao hơn vị trí nước thật ~50-80px) tạo ra 1 "bức tường vô hình" lơ lửng giữa đồng cỏ, phải soi pixel lại mới sửa đúng.
  - **Bug quan trọng phát hiện khi verify**: `physics.world.bounds` (vùng giới hạn vật lý toàn bộ world) **không tự khớp theo kích thước ảnh nền** — mặc định lấy theo kích thước config game (lúc đó 800×600, giờ là 960×540) trừ khi tự gọi `physics.world.setBounds(...)`. Vì chưa gọi hàm này, player bị chặn cứng ở góc trên-trái khung config dù map thật lớn hơn nhiều, khiến mọi test va chạm sông/ao/thác ở xa đều vô nghĩa (player còn chưa đi tới nơi đã bị world bounds chặn lại trước). **Đã sửa**: thêm `this.physics.world.setBounds(0, 0, background.displayWidth, background.displayHeight)` trong `create()`, cùng lúc với `cameras.main.setBounds(...)`. Bug này tồn tại từ khi đổi sang ảnh nền tĩnh lớn hơn khung nhìn config (cả bản `background.png` 880×600 lẫn bản `BaseMap.png` 1774×887) — nhắc nhở: **mọi khi đổi kích thước map/nền HOẶC kích thước khung nhìn game, phải nhớ set cả 2: `cameras.main.setBounds` VÀ `physics.world.setBounds`, không chỉ 1 trong 2.**
  - **Đã verify lại toàn bộ va chạm bằng Puppeteer sau khi sửa bug world bounds**: băng qua cầu 1 thành công (đi vào đúng khoảng trống x giữa 2 mép sông), bị chặn đúng mép sông ở đoạn không có cầu, bị chặn trước ao nước, bị chặn ở dải bầu trời không cho đi lên núi. Player spawn tại (890, 430), giữa 1 ô đất trống trên lưới đường mòn.
  - **`public/assets/tilesets/farm/background.png` và 3 file liên quan (nhà/giếng/chuồng cũ)** không còn dùng nhưng vẫn giữ trên đĩa, không xóa.
- **Đổi kích thước khung nhìn game từ 800×600 sang 960×540** (`src/main.ts`, `Phaser.Game` config `width`/`height`) — user tự sửa trực tiếp trong IDE. Không ảnh hưởng UIScene (toạ độ HUD neo góc trên-trái, không hardcode theo width/height) hay PreloadScene (thanh loading dùng `this.scale.width/height` động). `GameScene` cũng không hardcode 800×600 ở đâu (world bounds + camera bounds đều lấy theo kích thước ảnh nền `background.displayWidth/Height`, không liên quan viewport) nên chỉ cần sửa comment cho khớp thực tế, không cần đổi logic. Đã verify bằng Puppeteer — canvas render đúng 960×540, layout/HUD/map hiển thị bình thường.
- **Công cụ chỉnh tay vùng va chạm (EditorScene)**: user muốn tự "custom từng khu" thay vì nhờ đoán toạ độ qua soi pixel/ước lượng bằng mắt — bản đầu làm 1 scene editor riêng dùng chung Arcade rectangle zone hiện tại (tránh đổi kiến trúc physics). **Ngay sau đó user yêu cầu thêm — xem mục "Nâng cấp vùng va chạm... sang polygon" bên dưới, đã đổi hẳn sang đa giác, không còn giới hạn hình chữ nhật nữa.**
  - **[src/data/collisionZones.ts](../../src/data/collisionZones.ts)** (mới) — tách mảng `FARM_COLLISION_ZONES` (kèm `label` mô tả từng khu) ra khỏi `GameScene.ts` thành nguồn dữ liệu dùng chung, để cả `GameScene` (chạy game thật) và `EditorScene` (chỉnh tay) cùng đọc.
  - **[src/scenes/EditorScene.ts](../../src/scenes/EditorScene.ts)** (mới) — mở bằng phím **Q** từ `GameScene` (ban đầu dùng F1, user đổi lại thành Q ngay sau đó). Cho phép: kéo thân khối để di chuyển, kéo 1 trong 4 handle góc (ô vàng) để đổi kích thước (góc đối diện giữ cố định làm mốc, tự chuẩn hoá lại thành x/y/width/height dương kể cả khi kéo lố qua phía bên kia), kéo trên vùng trống để vẽ khối mới, chọn 1 khối (viền chuyển cyan) rồi bấm Delete/Backspace để xoá, phím mũi tên để pan camera, lăn chuột để zoom (0.3x–3x). Bấm **E** xuất toàn bộ mảng hiện tại thành code TS ra 1 `<textarea>` đè giữa màn hình (đã chọn sẵn text để copy ngay) — dán đè lại vào `collisionZones.ts` là xong. Bấm **Q** lần nữa quay lại `GameScene`.
  - **Bug phát hiện khi verify**: bấm phím "E" vừa mở export vừa gõ luôn ký tự "e" vào `<textarea>` vừa được focus, đè mất nội dung code đã select sẵn — do trình duyệt vẫn xử lý default action của keydown gốc sau khi focus đã nhảy sang textarea trong cùng 1 event. **Đã sửa**: gọi `event.preventDefault()` ngay đầu mọi handler phím tắt trong `EditorScene` (và cả phím mở editor ở `GameScene`).
- **Nâng cấp vùng va chạm từ hình chữ nhật sang đa giác (polygon) tuỳ ý**: user thấy khối chữ nhật che quá nhiều cỏ/đường mòn xung quanh sông uốn lượn, muốn "custom từng khu" thay vì chỉ co dãn hình chữ nhật. Đổi hẳn model dữ liệu + cơ chế va chạm:
  - **`CollisionZone`** (`collisionZones.ts`) đổi từ `{x,y,width,height,label}` sang `{label, points: {x,y}[]}` — đa giác >=3 điểm bất kỳ. 7 khu cũ migrate sang dạng 4 điểm góc (coi như hình chữ nhật) làm điểm khởi đầu, chỉnh tiếp qua editor.
  - **`GameScene.ts`**: Arcade Physics không hỗ trợ collider dạng polygon nên **bỏ hẳn** `Zone` + `physics.add.collider` cũ, thay bằng tự kiểm tra thủ công mỗi frame (`checkPolygonCollisions`): dự đoán vị trí đáy hitbox (`body.center.x`, `body.bottom`) sau khi di chuyển theo **từng trục X/Y riêng biệt** (không phải cả điểm đích cùng lúc — quan trọng để player vẫn trượt dọc biên polygon khi đi chéo thay vì bị dính cứng), dùng `Phaser.Geom.Polygon.Contains` để biết có "lọt" vào vùng chặn không, nếu có thì `setVelocityX(0)`/`setVelocityY(0)` tương ứng.
  - **`EditorScene.ts`** viết lại hoàn toàn cách tương tác — bỏ hệ thống `setInteractive({draggable})`/`'drag'` event của Phaser (không xử lý được việc "chuyển giao" 1 lượt kéo đang diễn ra sang 1 handle khác mới tạo), thay bằng tự quản lý toàn bộ bằng `pointerdown/pointermove/pointerup` + hit-test thủ công (khoảng cách tới từng handle):
    - Mỗi khu vẽ bằng `Phaser.GameObjects.Graphics` (clear + vẽ lại path mỗi lần đổi điểm) thay vì `Rectangle` — cần thiết vì Graphics vẽ được đa giác N điểm tuỳ ý, Rectangle thì không.
    - Handle góc (vàng) ở từng điểm — kéo để sửa riêng điểm đó; **phải-chuột** 1 điểm để xoá riêng nó (giữ tối thiểu 3 điểm).
    - Handle giữa cạnh (cam, nhỏ hơn) ở trung điểm mỗi cạnh — kéo là **chèn điểm mới** ngay tại đó (biến 1 cạnh thẳng thành 2 đoạn), biến hình chữ nhật thành hình dạng bất kỳ tuỳ ý chỉnh.
    - Handle chỉ hiện cho khối đang được chọn (giảm rối mắt khi có nhiều khối).
  - Đã verify bằng Puppeteer: (1) va chạm thật trong game vẫn đúng sau khi đổi cơ chế (sông/cầu/bầu trời), (2) chọn khối hiện đúng handle, (3) kéo handle giữa cạnh chèn điểm mới tạo hình lõm bám theo đường cong sông thật, (4) kéo riêng 1 góc di chuyển đúng 1 điểm, (5) xuất ra đúng code polygon 5 điểm sau khi chỉnh.
- **EditorScene: thêm cách xoá riêng 1 điểm bằng click trái + phím Delete** (user yêu cầu — trước đó chỉ có phải-chuột để xoá điểm ngay lập tức, không có cách nào dùng phím Delete). Thêm field `selectedVertexIndex` (điểm đang được "chọn riêng" trong khối đang chọn): click trái 1 handle góc vừa chọn điểm đó (tô đỏ viền trắng, phân biệt với các handle vàng còn lại) vừa cho kéo di chuyển ngay như cũ; click thả ra không nhúc nhích thì coi như chỉ chọn, không đổi vị trí điểm. `deleteSelectedVertexOrZone()` (gắn vào phím Delete/Backspace, thay thế thẳng `deleteSelectedZone()` cũ) ưu tiên xoá điểm đang chọn riêng nếu có, chỉ xoá cả khối khi chưa chọn điểm nào — giữ nguyên hành vi cũ khi không có điểm nào được chọn riêng. `selectedVertexIndex` tự reset về `null` khi: đổi/bỏ chọn khối (trong `select()`), xoá 1 điểm xong (chỉ số các điểm sau đó bị lệch, đơn giản nhất là bỏ chọn hẳn thay vì tính lại đúng chỉ số mới), hoặc xoá cả khối.
  - Đã verify bằng Puppeteer: chọn 1 zone thật (`Sông - đoạn phải`), click đúng 1 handle góc (điểm index 2/20) → `selectedVertexIndex` đúng bằng 2, ảnh chụp xác nhận handle đó tô đỏ khác hẳn các handle vàng còn lại; bấm Delete → điểm bị xoá đúng (còn 19/20), `selectedVertexIndex` reset về `null`, ảnh chụp sau xác nhận đường biên nối thẳng qua 2 điểm lân cận, không còn điểm đỏ. Không lỗi console mới.
- **Màn hình loading dùng logo thật thay vì chữ text**: trước đó chỉ có dòng chữ "Shinobi Valley" monospace trơn. Copy `public/images/logo-remove-bg.png` (nguồn gốc, không xoá) sang `public/assets/ui/logo.png` (asset thật dùng trong game). **`BootScene.ts`** thêm `preload()` load riêng logo này **trước** khi `PreloadScene` chạy — để logo có sẵn ngay khi màn hình loading vừa hiện, không phải đợi tự load cùng lúc với chính thanh loading nó thuộc về. **`PreloadScene.ts`**: bỏ text "Shinobi Valley", thay bằng `add.image('game_logo')` scale theo tỉ lệ chiều cao màn hình (logo gốc vuông 1254×1254, scale để cao ~55% màn hình), thanh progress dời xuống dưới logo (`height * 0.82`). Verify bằng Puppeteer: dùng `page.setRequestInterception` để cố tình làm chậm riêng các asset nặng (ảnh nền, spritesheet) ~400ms/asset (không đụng bundle JS/HTML ban đầu để tránh timeout điều hướng) — đủ thời gian chụp được màn hình loading đang chạy, xác nhận logo + thanh progress hiển thị đúng vị trí, đúng tỉ lệ.
- **User tự cập nhật `BaseMap.png` sang bản mới** (1672×941, đổi từ 1774×887 — bố cục cũng khác: ao nước chuyển từ góc dưới-phải sang giữa-phải, thêm cổng torii + đèn đá góc dưới-trái). User yêu cầu chỉ kiểm tra/sửa các logic khác, **không đụng vào toạ độ va chạm** trong `collisionZones.ts` (user tự chỉnh lại bằng EditorScene sau).
  - Kiểm tra `GameScene.ts`: `cameras.main.setBounds` + `physics.world.setBounds` đã đọc động qua `background.displayWidth/displayHeight` từ trước — không cần sửa gì, tự khớp theo ảnh mới.
  - Chỉ có comment trong `GameScene.ts` còn hardcode số cũ (1774×887, 569×388) — đã sửa lại thành mô tả chung chung không ghim số cụ thể, tránh lỗi thời mỗi lần đổi ảnh nền.
  - Vị trí spawn player `(890, 430)` vẫn nằm trong ô đất trống trên lưới đường mòn ở ảnh mới (đã verify bằng ảnh chụp) — không cần đổi.
  - Đã verify bằng Puppeteer: ảnh nền mới render đúng, world/camera bounds khớp kích thước mới, di chuyển bình thường, không phát sinh lỗi console mới. **Vùng va chạm (`collisionZones.ts`) chưa khớp lại với bố cục ảnh mới** — để nguyên theo yêu cầu user, cần vào EditorScene (phím Q) chỉnh lại thủ công.
- **[art-refs/theme.md](../../art-refs/theme.md)** (mới) — user cung cấp "Theme Guide & Color Palette Bible" đầy đủ (Global Theme Prompt, Visual Rules, bảng màu hex theo từng loại địa hình/vật thể: sky/mountain/grass/flower/dirt/river/lake/tree/cherry-blossom/rock/wood, World Design Rules, Lighting, AI Negative Prompt, Theme Identity %). Lưu thành file rule riêng, dùng chung cho **mọi** prompt gen ảnh sau này (không chỉ riêng 1 loại asset) — đã thêm link tham chiếu ở đầu `art-refs/README.md` để session sau đọc trước khi viết prompt mới.
- **Làm lại màn hình loading theo màu theme + sửa lỗi progress bar**: bar cũ tính `bar.width = 296 * value` theo % tải nhưng bị lộ lỗi vẽ tràn ra ngoài khung viền trắng (nghi `value` từ event `'progress'` không tuyến tính/có lúc vượt 1 khi nhiều loader — JSON data + ảnh + spritesheet — chạy song song). **Đã bỏ hẳn progress bar, thay bằng spinner xoay tròn** (`Phaser.GameObjects.Graphics` vẽ 1 vòng tròn mờ làm track + 1 cung 270° màu vàng `#FFD963`, xoay liên tục bằng `tweens.add({angle:360, repeat:-1})`) — spinner không cần đồng bộ theo %, chỉ báo "đang tải" nên né hẳn được lỗi cũ. **Nền màn hình loading** đổi từ màu phẳng tối `#1a1a2e` sang gradient màu trời; màu nền config game (`src/main.ts`) cũng đổi sang Sky Mid `#79C4FF` làm màu dự phòng cho khoảnh khắc trắng trước khi `PreloadScene` kịp vẽ gradient của nó.
  - **Bản đầu dùng `Graphics.fillGradientStyle()`** nội suy thẳng giữa 2 màu Sky Top/Sky Horizon — user phản hồi nhìn vẫn như màu phẳng, không rõ là gradient. **Đã sửa sang gradient 3 điểm dừng** dùng canvas thật (`textures.createCanvas` + `CanvasRenderingContext2D.createLinearGradient` với 3 `addColorStop`: Sky Top `#5CAEF8` → Sky Mid `#79C4FF` (55%) → Sky Horizon `#B7E2FF`) — dùng đúng cả 3 màu sky đã định nghĩa sẵn trong `theme.md` thay vì chỉ 2/3 màu như bản đầu. `fillGradientStyle` của Phaser Graphics chỉ hỗ trợ nội suy 2 màu ở 2 đầu nên không lên được hiệu ứng nhiều điểm dừng rõ rệt như canvas gradient thật.
  - Đã verify bằng Puppeteer (làm chậm asset nặng để chụp kịp lúc đang loading) — nền gradient 3 điểm dừng + spinner hiển thị đúng, rõ rệt hơn hẳn bản 2 màu, load xong chuyển sang `GameScene` bình thường.
- **Rà soát + dọn dẹp cấu trúc thư mục toàn dự án**: user yêu cầu đọc lại toàn bộ file/thư mục xem có cần tổ chức lại không. Kết luận: `src/`, `docs/`, `art-refs/`, `public/data/`, `public/assets/sprites/*`, `public/assets/tilesets/{6 biome}` đã khoa học, không cần đổi. Phát hiện + xử lý:
  - **Đã xoá** (xác nhận không còn được code nào tham chiếu qua `grep`): 15 file tile-grid cũ trong `public/assets/tilesets/farm/` (`background.png`, `dirt_path_corner/straight`, `fence_corner/gate/straight`, `grass_plain`, `grass_tuft`, `hay_bale`, `pond_water_tile`, `signpost`, `soil_tilled_dry/wet`, `tileset.png` — chỉ còn lại `BaseMap.png` đang dùng thật) + `public/assets/tilemaps/farm_test.json` (tilemap Tiled cũ, không còn load) + thư mục rỗng `public/images/Characters/Men/`.
  - **Đã đổi tên** cho rõ nghĩa (không xoá nội dung, chỉ đặt lại tên): `public/images/Characters/asset_XGzBdyELaaMNcjve9Fo8n7u8_..._# Female player character — AI.png` (tên chứa nguyên prompt AI, ảnh thật là 1 concept ninja nam cầm kiếm, không liên quan hệ thống player nam/nữ hiện tại) → `ninja-concept-early-draft.png`. 2 file `public/images/BaseMap/ChatGPT Image ....png` (tên mặc định do tool đặt) → `basemap-draft-with-buildings.png` (bản concept có nhà/cối xay gió, không dùng) và `basemap-draft-open-field.png` (bản đồng cỏ mở, gần giống bản đang dùng). `public/images/BaseMap/BaseMap.png` (bản cũ 1774×887, đã bị thay bởi `BaseMap-V2.png`) → `basemap-v1-old.png` để phân biệt rõ với `BaseMap-V2.png`.
  - **Đã sửa lại bảng ở `docs/data/asset-manifest.md`** (mục 17, dòng `farm/`) — vẫn liệt kê đủ 14 tile lẻ cũ như đang dùng thật, nay đã lỗi thời, cập nhật ghi rõ farm/ giờ dùng `BaseMap.png` làm nền tĩnh, tile lẻ đã xoá.
  - **Tự sửa sai giữa chừng**: ban đầu định xoá luôn `public/images/base.png` + `base-1.png` vì tưởng "không rõ mục đích/không ai tham chiếu" — mở ra xem trước khi xoá thì phát hiện đây là 2 file **"FILE MÔ TẢ PROMPT/GEN ẢNH – FARM NINJA"**, tức tài liệu tham khảo gốc (infographic) định hướng toàn bộ danh sách asset cần gen cho game (nhân vật, nhà cửa, cây trồng, quái, boss, UI...) — rất quan trọng về mặt tư liệu thiết kế, dù không được code tham chiếu. **Đã giữ nguyên, không xoá** — bài học: file "không được code tham chiếu" không đồng nghĩa "không có giá trị", phải mở xem nội dung trước khi kết luận là rác.
  - **Không đổi tên `BaseMap-V2.png`**: ban đầu định gán nó là "nguồn gốc" của `public/assets/tilesets/farm/BaseMap.png` đang dùng thật (cùng kích thước 1672×941) — nhưng so sánh pixel-by-pixel (`pngjs`, cài tạm) cho thấy **73% byte khác nhau** dù cùng kích thước, tức đây là 2 ảnh khác nhau (có thể qua thêm 1 bước xử lý/tái tạo màu), không phải cùng 1 file. Không đủ chắc chắn về nguồn gốc thật nên không gán quan hệ "A là nguồn của B" vào tên file — chỉ đổi tên bản cũ (`BaseMap.png` → `basemap-v1-old.png`) theo dữ kiện chắc chắn (đã bị thay thế), không suy diễn thêm.
  - Đã verify: `grep` toàn bộ `src/` xác nhận không có chỗ nào import trực tiếp từ `public/images/` (chỉ dùng làm nguồn thô để cắt/copy sang `public/assets/`), `npx tsc --noEmit` sạch, và chạy thử game bằng Puppeteer sau khi dọn — render/di chuyển bình thường, không lỗi console mới.
- **Thêm 3 ảnh trạng thái ô đất trồng cây + đặt lên map Farm** (chuẩn bị nền tảng cho Sprint 2 — hệ thống ô đất): user tự gen thêm 3 frame trong `BaseMap` tileset (`chưa cuốc`, `đã cuốc`, `chậu nước` — chậu nước dùng để trồng cây dưới nước như sen). Yêu cầu: đổi tên đúng chuẩn, đặt 36 ô "chưa cuốc" + 12 ô "chậu nước" lên các thảm cỏ trống trong `BaseMap.png`, sắp xếp đẹp; ô "đã cuốc" chưa dùng, để dành cho sau khi có logic cuốc đất thật.
  - **Đổi tên**: `chưa cuốc.png` → `soil_untilled.png`, `đã cuốc.png` → `soil_tilled.png`, `chậu nước.png` → `soil_water_pot.png` (trong `public/assets/tilesets/farm/`).
  - **Tìm vị trí đặt bằng soi pixel thật** (giống cách tìm suối/cầu trước đây) — quét màu để tìm chính xác 4 thảm cỏ trống không dính đường mòn: 3 thảm hàng trên (mỗi thảm ~320×110px, ngăn cách bởi 2 đường mòn dọc + kẹp giữa 2 đường mòn ngang) dùng cho 36 ô chưa cuốc (12 ô/thảm, lưới 6×2), và 1 thảm nhỏ hàng dưới (~180×90px) dùng cho 12 ô chậu nước (lưới 4×3, tile nhỏ hơn vì thảm thấp hơn nhiều).
  - **[src/data/farmTiles.ts](../../src/data/farmTiles.ts)** (mới) — định nghĩa 4 "thảm" (`PlotGrid`: toạ độ, số cột/hàng, kích thước ô) + hàm `generateGrid()` tự tính toạ độ từng ô căn giữa trong thảm (không phải gõ tay 48 toạ độ), xuất ra `FARM_TILE_PLACEMENTS` dùng chung được cho `GameScene`. Mỗi ô nhỏ hơn kích thước lưới 2px để tạo khe hở nhìn tách bạch giữa các ô thay vì dính liền khối.
  - **`GameScene.ts`**: thêm `placeFarmTiles()` — chỉ là `add.image()` tĩnh (chưa có tương tác cuốc/trồng/tưới, đó là việc của Sprint 2), depth `-1` (trên nền `-10000`, dưới player vì player depth = y luôn > 0 trong map này).
  - Đã verify bằng Puppeteer — cả 36 ô chưa cuốc (3 thảm × 12) lẫn 12 ô chậu nước đều vừa khít trong viền cỏ từng thảm, không tràn ra đường mòn hay đè lên cây, đúng ngay lần đặt đầu tiên nhờ toạ độ đo bằng pixel thay vì áng chừng.
- **Debug: click vào map log toạ độ world** — thêm 1 listener `pointerdown` trong `GameScene.create()`, quy đổi toạ độ màn hình sang world qua `cameras.main.getWorldPoint()` rồi `console.log`. Mục đích: dò nhanh toạ độ (để viết `collisionZones.ts`/`farmTiles.ts` bằng tay) mà không cần mở `EditorScene` (phím Q). Đã verify bằng Puppeteer — click log đúng toạ độ world.
- **Đổi bố cục ô đất chưa cuốc sang dạng "luống cày"**: user gửi ảnh mẫu (luống dài chia ô, giống crop row Stardew Valley) + yêu cầu "1 cột 5 ô, chia làm 3 khu" — khác hẳn lưới ô vuông co cụm giữa thảm ban đầu (6×2, có margin). Viết lại `data/farmTiles.ts`:
  - **`FarmTilePlacement`** đổi từ `size` (1 số, ô vuông) sang `width`/`height` riêng (ô có thể chữ nhật).
  - **`generateGrid()`** đổi cách tính: chia đều `plot.width/cols` và `plot.height/rows` để **lấp kín toàn bộ thảm** (không còn co cụm + căn giữa như bản trước) — mỗi cột = 1 luống dài, tự nhiên chia thành `rows` ô xếp dọc theo yêu cầu.
  - 3 thảm trên đổi từ `6×2` (12 ô/thảm) sang `6×5` (30 ô/thảm) — tổng 90 ô chưa cuốc, lấp kín cả 3 thảm thay vì chỉ chiếm 1 góc nhỏ. Thảm chậu nước giữ nguyên `4×3` nhưng cũng đổi qua công thức lấp-kín mới (trước đó vốn đã gần khít nên không đổi nhiều).
  - Đã verify bằng Puppeteer — mỗi cột hiện rõ 5 ô xếp dọc có khe hở tách bạch, lấp kín đúng 3 khu đất đã đo pixel trước đó, giống bố cục ảnh mẫu.
- **`farmTiles.ts` đổi cơ chế: `cellWidth`/`cellHeight` khai báo trực tiếp thay vì chia `width`/`height` thảm cho `cols`/`rows`** — chia theo cách cũ khiến ô méo thành chữ nhật bất cứ khi nào `cols !== rows`, kể cả khi thảm hình vuông (vd `cols:6,rows:5` luôn ra ô không vuông dù `width===height`). Giờ `cols`/`rows` chỉ quyết định số ô, không ảnh hưởng kích thước — tổng vùng chiếm = `cols*cellWidth` x `rows*cellHeight`, không tự khớp khít viền thảm nữa, cần tự canh `x`/`y`/`cellWidth`/`cellHeight`/`cols`/`rows` cho vừa.
- **Hàng rào gỗ bao quanh khu đất** — user tự thêm `public/images/BaseMap/Farm/Fence.png` (nhiều mẫu tham khảo, không phải tileset sẵn dùng), yêu cầu cắt cho khớp và bao quanh khu đất.
  - Dò bounding-box từng mảnh bằng connected-component trên kênh alpha (`sharp`, cài tạm) thay vì đoán toạ độ — cắt được `fence_horizontal.png` (đoạn ngang, có sẵn trụ 2 đầu) và `fence_vertical.png` (đoạn dọc, có sẵn trụ 2 đầu), lưu ở `public/assets/tilesets/farm/`.
  - **[src/data/fencePlacements.ts](../../src/data/fencePlacements.ts)** (mới) — đặt theo kiểu lặp nhiều đoạn nhỏ dọc theo cạnh (`FenceRun` + `generateRun()`, cùng pattern với `PlotGrid`/`generateGrid()` ở farmTiles.ts) thay vì kéo giãn 1 ảnh duy nhất cho cả cạnh: kéo giãn độc lập width/height làm ảnh chữ nhật gốc (240×140 và 103×350) bị méo gần vuông, lặp nhiều đoạn nhỏ đúng tỉ lệ gốc (`NATIVE_SIZE` + `segmentSize()`) thì giữ được hình dạng thật.
  - `GameScene.placeFence()` — depth theo `bottomY` (đáy từng đoạn) để Y-sort với player.
  - **Bug quan trọng phát hiện khi verify cạnh dưới**: `Player.ts` tính depth theo **tâm sprite** (`this.y`) thay vì **chân** (`this.y + shadowOffsetY`, lệch nhau ~31px với nhân vật women) — khiến player phải đi sâu qua khỏi hàng rào thêm ~31px mới được vẽ đè lên, trong khi hàng rào chỉ dày ~21px, nên đứng rõ ràng bên dưới cạnh dưới hàng rào vẫn bị hàng rào che. **Đã sửa**: `Player.ts` giờ tính depth + vị trí bóng đổ theo toạ độ chân, không phải tâm. Ảnh hưởng tốt tới Y-sort với mọi prop tĩnh khác dùng `bottomY`, không riêng hàng rào.
- **Nhà chính người chơi (3 cấp)** — user tự thêm `public/images/BaseMap/Home/Home.png` (3 mẫu nhà đặt cạnh nhau trong 1 ảnh, khớp đúng mô tả 3 cấp ở `art-refs/world/buildings.md` + giá nâng cấp ở `docs/gameplay/economy.md`: Lv2 10.000đ, Lv3 50.000đ).
  - Cắt bằng connected-component (giống hàng rào) thành `player_house_1/2/3.png` ở `public/assets/sprites/buildings/` (thư mục mới, đúng chuẩn id đã định sẵn trong `docs/data/asset-manifest.md`).
  - **[src/data/housePlacement.ts](../../src/data/housePlacement.ts)** (mới) — hiện chỉ đặt nhà cấp 1 lên map (`PLAYER_HOUSE`), cấp 2/3 preload sẵn nhưng chưa dùng, chờ logic nâng cấp nhà thật (chưa có trong `dev-schedule.md`). Vị trí đặt lên khoảnh cỏ trống cạnh cụm ô đất, dò bằng chính debug click-log toạ độ world đã có sẵn (không đoán chừng).
  - `GameScene.placeHouse()` — cùng pattern Y-sort `bottomY` như hàng rào.
- **Bóng đổ (shadow) cho hàng rào + nhà, tăng cảm giác 3D**: player đã có bóng từ trước (tự di chuyển theo mỗi frame); hàng rào/nhà là prop tĩnh nên vẽ bóng 1 lần lúc đặt.
  - Hàng rào: dùng lại `SHADOW_TEXTURE` (gradient tròn mờ có sẵn cho player) qua helper `addGroundShadow()`, đặt đúng đáy từng đoạn — nhìn ổn ngay, không cần chỉnh thêm.
  - Nhà: **3 lần thử mới ra bản ổn**, ghi lại làm bài học cho prop lớn sau này:
    1. Dùng lại `SHADOW_TEXTURE` phóng to/kéo dẹt — ra 1 vệt tròn nhỏ giữa đáy, nhìn như miếng dán lơ lửng (nhà to, đáy rộng, texture vốn thiết kế cho player nhỏ gần tròn).
    2. Đổi sang texture riêng hình chữ nhật bo góc mờ viền (`ctx.filter = blur()`) + lệch hướng sáng — đỡ hơn nhưng user vẫn thấy "chưa đủ tự nhiên".
    3. Thử bóng silhouette thật (nhân bản chính ảnh nhà, tint đen, bẹp trục Y, mờ) để bóng theo đúng viền mái/ống khói — gặp 2 bug liên tiếp trước khi ổn: (a) dùng `postFX.addBlur()` (BlurFX runtime của Phaser) khiến bóng **render lệch hẳn sang vị trí khác** trên màn hình khi origin khác (0.5,0.5) — đây là hạn chế thật của pipeline này, phải bỏ hẳn, thay bằng tự vẽ silhouette đã mờ sẵn vào 1 canvas texture (`ctx.filter=blur()` lúc vẽ + `globalCompositeOperation='source-in'` để tô đen đúng theo alpha đã mờ) rồi hiển thị như ảnh tĩnh thường; (b) bẹp bóng theo tỉ lệ **chiều cao ảnh gốc** (316px, cao chủ yếu do mái nhọn vẽ chếch lên) khiến bóng dài gần bằng thân nhà, tràn qua cả đường mòn phía dưới — phải tính theo **bề ngang** thay vì chiều cao.
    4. Dù đã sửa 2 bug trên, silhouette vẫn có khoảng cách/tách rời nhìn không tự nhiên với đáy nhà (do padding + blur làm mờ viền tiếp giáp) — **cuối cùng quay lại đúng công thức đã ổn với hàng rào** (`addGroundShadow`, bầu dục mờ viền, đặt sát đáy, chỉ đổi bề ngang cho phù hợp vật to) thay vì tiếp tục đẽo gọt silhouette. **Bài học**: đừng over-engineer bóng đổ cho 1 prop lớn nếu công thức đơn giản đã verify ổn cho prop khác — thử phức tạp hơn trước, lỗi nhiều hơn, không chắc đẹp hơn.
- **EditorScene giờ hiện đủ ngữ cảnh xung quanh + cho đi lại để test va chạm trực tiếp** — trước đây Editor chỉ có nền trơn + polygon đỏ, không thấy nhà/hàng rào/ruộng nên khó canh vùng chặn khớp thực tế, và chỉ pan camera bằng mũi tên chứ không đi lại được để tự kiểm tra khu nào đi được.
  - **[src/systems/WorldDecorations.ts](../../src/systems/WorldDecorations.ts)** (mới) — `renderStaticWorldDecorations(scene)` vẽ tĩnh ruộng/hàng rào/nhà lên bất kỳ scene nào (không gắn `FarmManager`, ô đất luôn hiện "chưa cuốc"). Cố tình KHÔNG dùng chung code với `GameScene.placeFarmTiles/placeFence/placeHouse` (đã verify kỹ, gắn chặt state riêng) để tránh rủi ro đụng vỡ — chấp nhận trùng lặp nhỏ đổi lấy an toàn.
  - **`EditorScene.ts`**: thêm `Player` thật (spawn trùng `GameScene`), **camera đổi từ pan-bằng-mũi-tên sang `startFollow` bám player** — mũi tên giờ dùng để đi lại. Va chạm dùng lại `resolvePolygonCollision` (xem mục bug bên dưới) nhưng lấy polygon trực tiếp từ `this.zones` (dữ liệu đang chỉnh tay, có thể chưa export) — sửa xong 1 khối là đi thử được ngay, không cần xuất/dán rồi quay lại `GameScene`.
  - Đã verify bằng Puppeteer: mở Editor thấy đủ nhà/hàng rào/ruộng cạnh các vùng chặn, đi lại bằng mũi tên + camera theo mượt, không lỗi console mới.
- **Bug va chạm: kẹt cứng vĩnh viễn ở góc lõm/mũi nhọn của polygon** (user báo + gửi ảnh: đứng giữa viền răng cưa hàng cây, bấm hướng nào cũng không nhúc nhích được). Nguyên nhân kép trong thuật toán chặn theo trục cũ (`checkPolygonCollisions` ở cả `GameScene` lẫn `EditorScene`, trùng lặp y hệt nhau):
  1. **Lọt chéo qua góc (corner-cutting)**: chỉ kiểm "di chuyển X thôi" và "di chuyển Y thôi" riêng lẻ để cho phép trượt dọc biên khi đi chéo — nhưng ở góc lõm sâu (viền zigzag), CẢ 2 kiểm tra trục riêng lẻ có thể ĐỀU pass trong khi điểm đích CHÉO (đi cả 2 trục cùng lúc) lại cắt ngang qua đúng mũi nhọn đó, không trục nào phát hiện ra. Player "lách chéo" lọt hẳn vào trong mũi nhọn.
  2. **Không có đường thoát khi đã kẹt bên trong**: 1 khi đã lọt vào (do bug 1, hoặc bất kỳ lý do nào khác), bước di chuyển nhỏ mỗi frame theo BẤT KỲ hướng nào từ bên trong 1 mũi nhọn hẹp vẫn tính là "còn nằm trong" polygon → velocity cả 2 trục bị huỷ liên tục → đứng im vĩnh viễn, đổi hướng cũng vô ích.
  - **[src/systems/CollisionUtils.ts](../../src/systems/CollisionUtils.ts)** (mới) — `resolvePolygonCollision(sprite, polygons, deltaMs)` dùng chung cho cả `GameScene` và `EditorScene` (xoá code trùng lặp cũ), sửa nguyên nhân 1: sau khi xử lý từng trục, kiểm thêm 1 lần điểm đích CHÉO — nếu vẫn lọt vào polygon thì huỷ nốt trục Y (giữ trượt ngang, chặn trượt chéo xuyên góc).
  - Đã verify bằng test dựng tay trong trình duyệt thật (polygon hình vuông đơn giản + fake Arcade body): đi chéo cắt góc vào vật cản bị chặn đúng (chỉ 1 trục còn trượt, không xuyên chéo được).
  - **Bản sửa nguyên nhân 2 đầu tiên (bỏ qua chặn hoàn toàn khi đã lỡ ở trong) gây ra bug MỚI nghiêm trọng hơn**: user báo + gửi ảnh — đi lại thoải mái, xuyên thẳng qua cả vùng sông đáng lẽ phải chặn. Lý do: mỗi frame vị trí vẫn "đang ở trong" nên cứ bỏ qua chặn mãi mãi, biến 1 lần lọt vào (dù chỉ do sai số) thành đặc quyền noclip vĩnh viễn trong cả vùng đó. **Đã sửa lại đúng hướng**: thay bằng `pushOutOfPolygon()` — duyệt từng CẠNH polygon (không chỉ đỉnh) tìm điểm gần nhất trên biên, đẩy sprite ra ngoài điểm đó thêm 2px cho chắc, huỷ vận tốc. Dùng `sprite.x/y` để dịch chuyển (không đụng thẳng `body.x/y`) — đúng bài học "setPosition thay vì body.reset() khi dịch chuyển tức thời" đã rút ra trước đó.
  - Đã verify lại bằng Puppeteer (teleport player vào giữa polygon "Sông - đoạn giữa"): bị đẩy ra đúng mép sông gần nhất ngay trong 1 frame, đứng yên ổn định sau đó (không dao động, không lọt lại vào trong), ảnh chụp xác nhận vị trí đẩy ra tự nhiên (đứng ngay trên bờ). Không lỗi console mới.
- **EditorScene thêm pan bản đồ tự do** — sau khi đổi mũi tên sang di chuyển player (mục trên), không còn cách nào xem chỗ khác trên bản đồ mà không đi bộ tới. Thêm **kéo chuột GIỮA** để pan camera tự do (tạm `stopFollow()`); bấm **mũi tên bất kỳ lúc nào** cũng gọi lại `startFollow()` để camera bám về player ngay — không cần phím riêng để "thoát chế độ pan". Đã verify bằng Puppeteer: kéo chuột giữa dịch đúng hướng, bấm mũi tên trả về đúng vị trí theo player.
- **Bug: bấm Q lần 2 (Editor → GameScene) không thoát được, đứng im** (user báo). Nguyên nhân: `UIScene` đăng ký lắng nghe `changedata-selectedSeedName`/`changedata-gameTimeText` trên `this.registry.events` — đây là **event bus TOÀN CỤC dùng chung mọi scene**, không tự huỷ theo vòng đời riêng của `UIScene`. Khi `UIScene` bị `scene.stop()` (lúc vào Editor), listener cũ vẫn treo trên bus toàn cục dù các Text object của nó đã bị destroy theo scene. Lần `GameScene.create()` chạy lại (bấm Q lần 2) gọi `selectSeed()` → ghi registry → bắn sự kiện → listener cũ (treo lơ lửng) cố `.setText()` lên Text object ĐÃ CHẾT → ném lỗi JS giữa chừng `create()` → toàn bộ phần code sau đó (spawn player, `scene.launch('UIScene')`...) không bao giờ chạy, nhìn như "không thoát".
  - **`UIScene.ts`**: đổi 2 listener sang dùng named handler (để `.off()` được), gỡ đăng ký trong `this.events.once(Phaser.Scenes.Events.SHUTDOWN, ...)` — đúng vòng đời của chính scene này, không phụ thuộc ai khác dọn hộ.
  - Đã verify bằng Puppeteer: bấm Q liên tiếp 4 lần (vào/ra/vào/ra) đều chuyển scene đúng, không còn crash, không lỗi console mới. **Bài học chung**: bất kỳ khi nào 1 scene lắng nghe sự kiện trên object TOÀN CỤC (registry, hoặc event emitter dùng chung khác) thay vì `this.events`/`this.input` riêng của chính nó, PHẢI tự gỡ đăng ký lúc `shutdown` — Phaser không tự dọn hộ vì nó không biết listener đó thuộc về scene nào.
- **Nâng cấp quy trình tự test bằng Puppeteer** — trước giờ mỗi lần test phải cài tạm `puppeteer-core` (`--no-save`), viết lại 1 script `.cjs` từ đầu, xong lại gỡ + revert `yarn.lock`. User đề xuất giữ hẳn 1 file harness tái dùng thay vì tạo/xoá liên tục.
  - `puppeteer-core` giờ là **devDependency thật** trong `package.json` (không còn tạm).
  - **[src/main.ts](../../src/main.ts)**: expose `window.__game` bọc trong `if (import.meta.env.DEV)` — Vite tự loại bỏ hẳn khỏi bundle production, không lo lộ ra bản build thật. Không cần thêm/xoá thủ công mỗi lần test như trước.
  - **[scripts/dev-check.cjs](../../scripts/dev-check.cjs)** (mới) — harness cố định: tự dò cổng dev server đang chạy (không cần biết trước `yarn dev` chạy ở cổng nào), có sẵn `withPage()`/`screenshot()`/`getActiveScenes()` dùng chung. Chỉ cần sửa phần "SCENARIO" ở cuối file cho từng lần test, chạy bằng `yarn dev:check`. Ảnh chụp lưu vào `.dev-check-output/` (đã thêm `.gitignore`, không lẫn vào repo).
  - Đã verify bằng cách lặp lại đúng bài test Q ở mục trên (sửa phần scenario, không cài gì thêm) — ra kết quả giống hệt lúc debug thủ công.

## Sprint 2 — Hệ thống ô đất 🟡 (cuốc/trồng/lớn xong, tưới/thu hoạch/inventory để Sprint 4)

- **Cuốc đất → trồng hạt → cây lớn theo thời gian thực** — đúng "Done when" của Sprint 2 trong `dev-schedule.md`. Không đụng tới tưới nước/độ ẩm/thu hoạch/inventory vì 3 việc đó thuộc scope Sprint 4 ("Tưới nước, độ ẩm, thu hoạch, Inventory") theo đúng lịch trình, không phải bỏ sót.
  - **[src/systems/FarmManager.ts](../../src/systems/FarmManager.ts)** (mới) — quản lý state runtime của từng ô đất (`empty→tilled→planted→ready`, dùng lại `FarmTileState` đã có sẵn trong `data/types.ts`), tách biệt hoàn toàn khỏi rendering (GameScene chỉ đọc state từ đây để vẽ). `till()`/`plant()` đổi state có validate (không cuốc lại ô đã cuốc, không trồng lên ô chưa cuốc). `update(now)` mỗi frame kiểm tra ô nào đủ `growth_hours` (tính theo **giờ thực trôi qua**, đúng thiết kế `farming.md`) thì chuyển `ready`. `getVisualStage()` suy ra giai đoạn hiển thị (`seed/sprout/growing/ready`) từ % thời gian đã trồng — tách riêng khỏi `FarmTileState` vì cả 3 giai đoạn đầu đều chung 1 state `planted`.
  - **`data/farmTiles.ts`**: `FarmTilePlacement` thêm field `id` (số thứ tự ổn định, gán 1 lần lúc build mảng) — cần thiết để `FarmManager` map state theo đúng ô, trước đó không có cách nào định danh 1 ô cụ thể.
  - **`public/data/crops.json`**: thêm `green_onion` (Hành lá) + `potato` (Khoai tây) — đủ 3 cây Tier Cơ Bản đầu theo yêu cầu Sprint 2 (trước đó chỉ có `carrot` mẫu). Số liệu lấy đúng bảng trong `docs/gameplay/farming.md` (giá hạt, giờ chín, độ giảm ẩm/giờ...). Asset sprite 4 giai đoạn cho cả 3 cây **đã có sẵn từ trước** (`public/assets/sprites/crops/`), không cần gen thêm/dùng placeholder.
  - **`GameScene.ts`**: `placeFarmTiles()` giữ lại reference ảnh đất theo `id` (`soilImages`) để đổi texture khi cuốc; `syncFarmVisuals()` (gọi mỗi frame sau `farmManager.update()`) đồng bộ texture đất + tạo/đổi/xoá ảnh cây theo giai đoạn, so sánh texture key trước khi `setTexture()` để không upload GPU thừa. Tương tác: phím **E** cuốc/trồng ô đất ngay dưới chân player (dùng chung điểm `body.center.x`/`body.bottom` với `checkPolygonCollisions`), phím **1/2/3** chọn hạt giống (chưa có inventory thật — Sprint 4 — nên tạm thay bằng chọn phím số), phím **G** (DEBUG) đẩy nhanh +1 giờ giả lập mỗi lần bấm để test lớn/chín mà không phải chờ hàng giờ thực (chưa có `TimeManager` ở Sprint 3, đây chỉ là công cụ test tạm, cần xoá khi có game clock thật).
  - **`UIScene.ts`**: thêm dòng hiển thị hạt giống đang chọn, đồng bộ qua `this.registry` (Data Manager toàn cục, dùng chung được giữa `GameScene` và `UIScene` mà không cần custom event bus).
  - Đã verify bằng Puppeteer (gửi phím E/1/2/3/G qua `page.keyboard.press`): cuốc đất đổi đúng texture (ảnh đất đổi từ ô vuông nhỏ sang luống cày), trồng hạt hiện đúng sprite seed, fast-forward qua các mốc giờ hiện đúng sprout→growing→ready (cà rốt chín đúng hình củ cà rốt to), đổi hạt giống cập nhật UI ngay lập tức. Không phát sinh lỗi console mới.
- **Chưa làm trong Sprint 2 này (để Sprint 4 đúng lịch)**: tưới nước/độ ẩm (`moisture` field đã có trong `FarmTile` type nhưng chưa dùng), cộng vật phẩm thu hoạch vào inventory thật (xem action Enter ở ô `ready` bên dưới — mới chỉ có hiệu ứng hình ảnh, chưa có nơi lưu vì Inventory chưa tồn tại), lưu/tải trạng thái ô đất vào `SaveManager` (state hiện chỉ tồn tại trong bộ nhớ 1 phiên chơi, mất khi reload — chưa wiring vì chưa có UI/trigger save-load nào trong game).
- **Sửa lỗi ô đất đã cuốc trông to hơn hẳn ô chưa cuốc** (user báo kèm ảnh) — nguyên nhân: `soil_tilled.png` (221×221, viền cỏ mỏng ~2.7%/cạnh) và `soil_untilled.png` (134×134, viền dày ~6.7%/cạnh) có tỉ lệ viền/nội dung khác hẳn nhau, cùng vẽ ở 1 kích thước ô như nhau thì phần đất cày bị tràn to hơn. Thêm `SOIL_TEXTURE_DISPLAY_SCALE = { farm_soil_tilled: 0.915 }` trong `GameScene.ts`, áp dụng ở cả `placeFarmTiles()` lẫn `syncFarmVisuals()` (chỗ sau trước đó chỉ đổi texture mà không set lại size — cùng dạng bug với mục icon menu hạt bên dưới).
- **Đổi luồng tương tác cuốc/trồng sang phím Enter (bỏ phím E/số)**: bấm Enter lần 1 lên ô đất gần đó để cuốc, bấm Enter lần 2 (hoặc bấm Enter khi ô đã cuốc sẵn) mở **menu chọn hạt** dạng panel neo giữa-dưới màn hình — mũi tên trái/phải đổi hạt đang chọn, Enter xác nhận trồng, Esc hoặc click ra ngoài panel đóng menu không trồng gì. Thay hẳn cơ chế cũ (phím 1/2/3 chọn số, phím E cuốc/trồng luôn không qua bước chọn).
- **Sửa độ rộng box menu hạt cố định quá dài** (user: "kích thước của box bên ngoài hơi dài, cho ngắn lại") — đổi `seedMenuPanelWidth` sang tính động theo số icon hiển thị (`slots * iconSize + gap`) thay vì 1 số hardcode, box tự khít theo nội dung.
- **Tăng bán kính vùng tương tác lên 32px** (user: "giờ cứ phải đứng lên trên thì nó mới trỏ, giờ muốn chỉ cần đứng gần là đã trỏ rồi") — `FarmManager.findTileAt()` (đúng bounds) đổi thành `findNearestTile(x, y, maxDistance)` (ô gần nhất trong bán kính `FARM_TILE_INTERACT_RADIUS = 32`), dùng chung cho cả con trỏ chỉ dẫn (`findInteractionTarget`) lẫn tương tác Enter thật (`interactWithFarmTile`) — đảm bảo "trỏ được" và "bấm Enter tác động được" luôn khớp cùng 1 ô.
- **Mở rộng menu chọn hạt từ 3 cây lên đủ 19 cây (dạng carousel cuộn)** (user báo "chưa thấy load ra đây" — menu vẫn chỉ hiện 3 icon dù `crops.json` đã có 19 cây). `PLANTABLE_CROP_IDS` mở rộng đủ 19 id theo đúng thứ tự `crops.json` — **cho chọn tự do, chưa gate theo `unlock_level`/trừ tiền hạt giống thật** vì Inventory/tiền tệ chưa tồn tại (đúng scope Sprint 4, tạm chấp nhận "miễn phí" ở giai đoạn này). Vì 19 icon không thể nhét vừa 1 hàng, đổi UI sang **cửa sổ trượt** (`SEED_MENU_VISIBLE_SLOTS = 7`): `getSeedMenuWindowStart()` canh giữa ô đang chọn rồi kẹp trong `[0, total-7]`, thêm 2 mũi tên `‹`/`›` (chỉ hiện khi còn cây ẩn ở phía đó), nhãn hiện `"Tên cây (vị trí/19)"`.
  - **Bug phát hiện khi test lần đầu**: 1 số icon hiện nhỏ xíu/gần như trong suốt, lộ nền ô đất phía sau — do `Image.setTexture()` khi đổi giữa các sprite cây khác nhau **không tự giữ lại `setDisplaySize()` đã set trước đó** (kích thước hiển thị = kích thước gốc × scale, mà scale là số cũ để lại từ texture trước, các sprite cây có kích thước gốc rất khác nhau nên ra kết quả sai lệch tuỳ cây). **Đã sửa**: thêm field `seedMenuIconSize = 32`, gọi lại `.setDisplaySize(seedMenuIconSize, seedMenuIconSize)` ngay sau mọi lần `.setTexture()` trong `updateSeedMenuSelection()`. **Bài học chung, áp dụng cho mọi chỗ dùng `setTexture()` để đổi ảnh động**: phải tự set lại size sau khi đổi texture nếu 2 ảnh có kích thước gốc khác nhau — Phaser không tự làm hộ.
  - Đã verify bằng `scripts/dev-check.cjs` (cuốc → mở menu → cuộn phải 5 lần → cuộn tới cuối) + xem trực tiếp 3 ảnh chụp: icon hiển thị đúng kích thước đồng đều ở mọi vị trí cuộn (không còn icon tí hon/trong suốt), cửa sổ trượt đúng (carrot 2/19 → strawberry 7/19 → tomato 8/19 khi cuộn tới cuối do bị kẹp lại ở `total-7`), không lỗi console mới.
  - **Bug thứ 2 phát hiện SAU KHI xem ảnh chụp kỹ hơn** (user gửi ảnh: từ vị trí Bí đỏ 5/19 trở đi, các icon hiện ô đen viền xanh lá chéo — texture "missing" mặc định của Phaser, không phải bug kích thước icon ở trên): `PreloadScene.ts` có 1 danh sách `PLANTABLE_CROPS` RIÊNG (dùng để biết cần load texture `crop_<id>_<stage>` nào) **bị quên cập nhật** khi mở rộng `GameScene.PLANTABLE_CROP_IDS` — vẫn chỉ preload đúng 3 cây cũ (`green_onion`, `carrot`, `potato`), nên 16 cây còn lại chưa từng có texture nào trong cache, `setTexture()` gán key không tồn tại thì Phaser tự thay bằng texture `__MISSING`. **Bài học**: khi 1 danh sách id được dùng ở nhiều nơi (ở đây là "cây nào cho chọn trong menu" ở `GameScene` và "cây nào cần preload texture" ở `PreloadScene`), phải rà tìm HẾT các nơi có bản sao trước khi coi là xong — sửa 1 chỗ tưởng đủ nhưng còn sót bản sao ở scene preload. Đã mở rộng `PLANTABLE_CROPS` trong `PreloadScene.ts` khớp đủ 19 id (đã kiểm tra `public/assets/sprites/crops/` có đủ sprite 4 giai đoạn cho tất cả, không thiếu file nào).
  - Đã verify lại bằng `scripts/dev-check.cjs`: cả 19 icon (green_onion→ancient_seed) hiện đúng sprite tương ứng ở mọi vị trí cuộn, không còn ô "missing texture" nào, không lỗi console mới (chỉ còn 404 `favicon.ico` không liên quan).
- **Con trỏ báo khối tương tác được** — user yêu cầu thêm chỉ dẫn UI kiểu game farming tham khảo (mũi tên vàng trỏ vào khối player đang đứng gần, ví dụ đứng lên ô đất thì trỏ vào ô đất, đứng gần nhà thì trỏ vào nhà). `GameScene.findInteractionTarget()` ưu tiên ô đất ngay dưới chân (đúng ô mà phím E sẽ tác động — dùng lại điểm `body.center.x`/`body.bottom`), nếu không đứng lên ô nào thì kiểm tra khoảng cách tới nhà (bán kính = 70% bề ngang nhà). `updateInteractionPointer()` gọi mỗi frame để hiện/ẩn + di chuyển 1 ảnh mũi tên dùng chung, thêm hiệu ứng nhấp nhô nhẹ (sin theo thời gian). Mũi tên vẽ bằng code (canvas, kim cương vàng viền nâu, giống màu spinner loading đã dùng) chứ không cần asset riêng, depth cực cao (999999) để luôn nổi trên mọi prop. Nhà hiện **chưa có tương tác thật** (chưa có gì để làm khi đứng gần) nhưng vẫn báo trước theo đúng yêu cầu, dễ gắn logic thật vào sau (mở shop, vào nhà...). Đã verify bằng Puppeteer 3 kịch bản (đứng trên ô đất / giữa đồng cỏ trống / đứng gần nhà) — hiện/ẩn đúng, trỏ đúng vị trí.
- **`public/data/crops.json` — thêm đủ 19 cây theo toàn bộ bảng trong `docs/gameplay/farming.md`** (trước đó chỉ có 3 cây Tier Cơ Bản: green_onion/carrot/potato). Thêm 16 cây còn lại đủ 4 tier: Cơ Bản (`cabbage`, `pumpkin`), Trung Cấp mở Lv10 (`mushroom`, `strawberry`, `tomato`, `corn`, `watermelon`), Cao Cấp mở Lv25 (`medicinal_herb`, `green_tea`, `lotus`, `ginseng`, `red_ginseng`), Hiếm mở Lv40+ (`sunflower`, `moonlight_flower`, `spirit_energy_plant`, `ancient_seed`). Đã kiểm tra `public/assets/sprites/crops/` trước khi thêm — **đủ sẵn sprite 4 giai đoạn cho cả 19 cây**, không cần gen thêm/dùng placeholder.
  - Số liệu (giá hạt, giờ chín, độ giảm ẩm/giờ, multi-harvest + chu kỳ hái lại, sản lượng, giá bán) lấy đúng theo bảng thiết kế, không bịa. 1 vài chỗ bảng gốc không cho số cụ thể phải tự suy luận có ghi chú: hạt giống "Drop từ Boss/hang động" (Hoa ánh trăng, Cây linh khí, Hạt giống cổ đại) → `seed_cost: 0` (không mua được ở shop); `special_tool: "divine_watering_can"` cho Cây linh khí (bảng ghi "Cần Bình tưới thần") — chưa có id vật phẩm thật nào định nghĩa tool này, đặt tạm slug tiếng Anh cho nhất quán, cần khớp lại khi có hệ thống tool thật; Hạt giống cổ đại cho ra "Random item" nên `sell_price: 0` (giá tuỳ item ra, không phải giá cố định của bản thân hạt).
  - **Cố tình KHÔNG thêm "Tinh hoa thiên nhiên"** dù có đủ sprite trong thư mục — theo đúng mô tả farming.md đây là nguyên liệu CRAFT (3 Nhân sâm + 3 Sâm đỏ + 5 Linh khí → 1 Tinh hoa), "Không trồng" được, không có `seed_cost`/`growth_hours` nên không khớp schema `Crop` — thuộc về `items.json` (chưa thêm, ngoài phạm vi yêu cầu này).
  - **`GameScene.PLANTABLE_CROP_IDS` giờ đã mở rộng đủ 19 cây** (xem mục "Mở rộng menu chọn hạt... carousel" bên dưới) — vẫn **chưa gate theo `unlock_level`/trừ tiền hạt giống thật**, cho chọn tự do mọi cây vì Inventory/tiền tệ thuộc Sprint 4, chưa tồn tại.
  - Đã verify: `node -e "require('./public/data/crops.json')"` parse JSON hợp lệ, đúng 19 entry; chạy game thật qua `scripts/dev-check.cjs` xác nhận `PreloadScene` load đủ 19 cây vào cache, không lỗi console mới.
- **⚠️ Đổi lại giai đoạn hiển thị cây chín thành `harvest` riêng biệt khỏi item icon** (user yêu cầu — trước đó 1 phiên làm việc khác đã gộp chung "Ready/Item Icon" thành 1 file `<id>.png` vì tưởng code chỉ có 4 stage). `FarmManager.CropVisualStage` đổi `'ready'` → `'harvest'`, `getVisualStage()` trả `'harvest'` khi `tile.state === 'ready'`; `PreloadScene.ts` đổi `CROP_STAGE_SUFFIX.ready: ''` → `CROP_STAGE_SUFFIX.harvest: '_harvest'` (load file `<id>_harvest.png` riêng, không dùng chung `<id>.png` nữa). `art-refs/items/crops.md` + `docs/data/asset-manifest.md` đã cập nhật thêm block/file Harvest cho cả 20 cây.
  - **Vấn đề chưa xong**: chưa có file `<id>_harvest.png` thật nào cho 20/20 cây (chỉ có prompt trong `crops.md`) — **ô đất cây chín trên map hiện tại sẽ load ảnh lỗi/thiếu texture** cho tới khi gen đủ 20 file này và bỏ vào `public/assets/sprites/crops/`. Item icon `<id>.png` (dùng cho Inventory sau này ở Sprint 4) không bị ảnh hưởng, vẫn đủ sẵn.
- **Action thu hoạch khi Enter lên ô `ready`** — `interactWithFarmTile()` thêm nhánh thứ 3 (trước chỉ xử lý `empty`/`tilled`): gọi `FarmManager.harvest(tile)` (mới, reset `state→empty`/`cropId→null`/`plantedAt→null`, trả về `cropId` vừa hái), rồi `playHarvestFx()` hiện icon `<id>.png` (texture `crop_<id>_item`, preload thêm ở `PreloadScene.ts` — trước đó cố tình chưa load vì nghĩ để dành Inventory Sprint 4, giờ cần ngay cho hiệu ứng) tại đúng vị trí ô, bay lên 40px + mờ dần trong 600ms rồi tự `destroy()`. Ô đất tự động về lại `empty` (chưa cuốc) nhờ `syncFarmVisuals()` đọc state mới, không cần code thêm. **Chỉ là phản hồi hình ảnh** — chưa cộng gì vào inventory thật (chưa tồn tại).
  - **Bug phát hiện lúc verify bằng Puppeteer**: icon bay lên tồn tại đúng vị trí/kích thước/alpha (kiểm tra qua `scene.children.list`) nhưng **không thấy gì trong screenshot** — nguyên nhân: con trỏ tương tác (`interactionPointer`, hình thoi vàng 22×26, depth 999999) vẫn hiện đúng ngay tại ô vừa hái (còn trong bán kính tương tác, giờ có thể cuốc lại), depth cao hơn icon (999998 lúc đầu) nên che kín hoàn toàn icon nhỏ bên dưới suốt lúc bay lên. **Đã sửa**: đổi `HARVEST_FX_DEPTH = INTERACTION_POINTER_DEPTH + 0.5` (nổi trên con trỏ, dưới menu hạt giống). Verify lại bằng screenshot zoom đúng bounding box của icon (`object.getBounds()` quy đổi qua camera scroll/zoom) — xác nhận thấy rõ hình carrot orange/xanh, không còn bị hình thoi che.

## Sprint 3 — Game Clock (chu kỳ ngày/đêm) ✅

- **[src/systems/TimeManager.ts](../../src/systems/TimeManager.ts)** (mới) — đồng hồ trong game, **tách biệt hoàn toàn** khỏi giờ THỰC dùng để lớn cây ở `FarmManager` (2 hệ thời gian độc lập, cố tình không liên quan tới nhau — đừng nhầm lẫn khi đọc lại code sau này). Tỉ lệ chọn: **1 giờ game = 60 giây thực** (1 ngày = 24 phút thực) — chọn số tròn để 1 giây thực = 1 phút game, dễ verify khi test (chờ N giây thực thì kim đồng hồ phải nhảy đúng N phút). Bắt đầu lúc 6:00 sáng ngày 1. Extend `Phaser.Events.EventEmitter` để phát `hourTick`/`dayChange`/`nightStart`/`dayStart` — chưa có ai subscribe (đúng scope Sprint 3, các hệ thống ăn theo giờ/đêm là việc của sprint sau), nhưng event bus đã sẵn sàng.
  - `getNightOverlayAlpha()` nội suy tuyến tính trong khung giờ hoàng hôn (19h→21h) và bình minh (4h→6h) thay vì bật/tắt đột ngột — đúng yêu cầu "Overlay tint màu tối dần" trong `dev-schedule.md`.
  - `debugAdvanceHours(hours)` — nhảy nhanh N giờ giả lập, gọi qua phím **T** (DEBUG) ở `GameScene`, tương tự phím G đã có cho farm — dùng để test chu kỳ ngày/đêm không phải chờ thật.
- **`GameScene.ts`**: `nightOverlay` là 1 `Rectangle` phủ kín camera (`setScrollFactor(0)`, không phải world object nên không cần theo dõi vị trí camera), alpha cập nhật mỗi frame theo `timeManager.getNightOverlayAlpha()`. `updateTimeHud()` ghi `"Ngày X — HH:MM"` vào registry cho `UIScene` đọc (cùng pattern với `selectedSeedName` ở Sprint 2).
- **`UIScene.ts`**: thêm dòng ngày/giờ ở góc trên-phải (tách khỏi cụm HP/MP/EXP/hạt giống bên trái).
- **Bug phát hiện khi verify bằng Puppeteer**: bấm nhanh nhiều phím liên tiếp không có khoảng nghỉ (`page.keyboard.press()` gọi dồn dập trong vòng lặp) khiến 1 lần bấm bắn ra tới 3-4 lần sự kiện `keydown` (không phải do code sai — test lại với khoảng nghỉ hợp lý giữa các lần bấm thì đúng 1-1 hoàn toàn chính xác, đây là đặc thù môi trường test tự động chứ người chơi bấm phím tay thật không gặp phải). Dù vậy vẫn **thêm `event.repeat` guard cho mọi phím tắt dạng "1 lần bấm = 1 hành động"** (Q, E, 1/2/3, G, T) trong `GameScene.ts` làm phòng vệ đúng đắn cho trường hợp OS/trình duyệt thật sự auto-repeat khi giữ phím hơi lâu — trước đó các phím này chưa có guard này.
- Đã verify bằng Puppeteer (phím T với khoảng nghỉ giữa các lần bấm): HUD hiện đúng "Ngày X — HH:MM", tăng giờ chính xác 1-1 theo số lần bấm, overlay tối dần đúng lúc 19h-21h (thấy rõ ảnh chụp tối hơn ở 21h), sáng lại đúng lúc bình minh, chuyển sang Ngày 2 đúng khi qua nửa đêm. Không lỗi console mới.
- **Đổi hẳn ảnh nền sang bản đêm lúc tối hẳn** — user tự vẽ thêm `public/images/BaseMap/Farm/base-map-night.png` (cùng bố cục/kích thước 1672×941 với bản ngày — đã verify trước khi dùng, không đoán chừng). Copy vào `public/assets/tilesets/farm/BaseMap_night.png`, load texture `farm_background_night` ở `PreloadScene.ts`.
  - `TimeManager.getNightOverlayAlpha()` tách thành `getNightFraction()` (0-1, nguồn chung) + `getNightOverlayAlpha()` (quy đổi sang alpha overlay) — để `GameScene` dùng chung 1 mốc % cho cả 2 hiệu ứng (crossfade nền + tối prop), tránh lệch nhịp chuyển cảnh nếu mỗi chỗ tự tính riêng.
  - `GameScene.ts`: thêm ảnh `backgroundNight` chồng ngay trên ảnh nền ngày (cùng gốc toạ độ, `depth` cao hơn 1), alpha = `getNightFraction()` mỗi frame — **crossfade dần** giữa 2 ảnh theo đúng khung giờ hoàng hôn/bình minh đã có sẵn từ overlay, không bật/tắt đột ngột. Vì cả 2 ảnh cùng kích thước nên toạ độ va chạm/ô đất/hàng rào/nhà không bị ảnh hưởng gì.
  - Player/hàng rào/nhà/ô đất chưa có bản vẽ riêng cho ban đêm — vẫn tối đi nhờ `nightOverlay` (lớp phủ alpha đen) phủ lên trên chúng, phối hợp cùng lúc với crossfade nền.
  - Đã verify bằng Puppeteer: ảnh nền chuyển mượt từ bản ngày sang bản đêm (thấy rõ trăng/sao ở bản đêm) đúng khung giờ 19h-21h, giữ nguyên bản đêm suốt đêm, chuyển ngược lại đúng lúc bình minh. Không lỗi console mới.

## Sprint 4+ — chưa bắt đầu

Xem chi tiết từng sprint ở [dev-schedule.md](dev-schedule.md) Phụ lục C.
