# Tiến độ dự án

> File này ghi lại **chức năng nào đã code xong, ở file nào, còn thiếu/lỗi gì** — đọc file này trước khi bắt đầu 1 phiên làm việc mới để biết đang đứng ở đâu, khỏi phải dò lại toàn bộ code.
>
> **Quy tắc bắt buộc**: mỗi khi thêm/sửa xong 1 chức năng, phải cập nhật vào file này ngay trong cùng phiên làm việc đó — thêm dòng mới vào đúng Sprint, hoặc thêm vào "Vấn đề đã biết" nếu chưa xong hẳn. Không để tồn đọng qua phiên sau mới ghi. Quy tắc gốc nằm ở `CLAUDE.md`.

---

## Đang ở đâu

**Sprint 0-6 xong — ALPHA HOÀN THÀNH theo "Done when" của dev-schedule.md.** Farm loop đầy đủ: cuốc → trồng → tưới (giữ độ ẩm) → lớn theo thời gian thực → chín → hái ra sản lượng thật theo % ẩm → vào Inventory (stack đúng, hỗ trợ multi-harvest/regrow). Sprint 3 (game clock) tách biệt hoàn toàn khỏi thời gian nông trại (chủ đích). Sprint 5 (Combat cơ bản): hệ thống chuyển màn Farm ↔ Bãi Tập Luyện/Đồng Cỏ, Người Rơm, quái thật (Thỏ Hoang) với AI/damage/EXP/gold/drop thật, HUD HP/MP/EXP thật, hotbar chiêu thức, ngắm mục tiêu F2 — xem chi tiết ở mục Sprint 5. Sprint 6 (Save/Load thật): lưu/tải đầy đủ farm + inventory + stats + đồng hồ game + vị trí ra `localStorage`, autosave theo hành động quan trọng + định kỳ + lúc đóng tab — xem chi tiết ở mục Sprint 6.

**Bảng nhân vật (Hành trang/Tiềm năng/Kỹ năng/Thông tin/Trang bị) — ngoài lịch trình, user yêu cầu trực tiếp kèm ảnh tham khảo UI 1 game khác.** Kéo sớm 1 phần Sprint 11 (đổi trang bị) + hệ điểm thuộc tính tự do (`docs/planning/progression.md`) trước lịch — thay hẳn bảng túi đồ riêng cũ. Xem chi tiết ở mục riêng bên dưới. **Sprint 7 (14 cây + phân bón) là bước code tiếp theo** theo đúng lịch trình Phase 2 — Beta.

**⚠️ Toàn bộ asset chiến đấu Sprint 5 (Người Rơm, Thỏ Hoang, hiệu ứng chém/hit/vỡ, nền Bãi Tập Luyện/Đồng Cỏ) đều là hình khối vẽ bằng code (placeholder)** — chưa gen ảnh thật nào, xem chi tiết ở mục Sprint 5.

**Đã cắt xong đủ 20 file `<id>_harvest.png`** (+ làm mới `_seed`/`_sprout`/`_growing`/`<id>.png` cho 18/19 cây trong `crops.json`) từ ảnh tham khảo user cung cấp — xem chi tiết ở mục "Cắt lại toàn bộ sprite cây trồng" trong Sprint 2. Cảnh báo cũ ở đây đã hết hiệu lực.

**⚠️ Chưa có asset thật cho**: overlay đất ẩm theo %, hiệu ứng tưới nước (giọt nước bay lên) — đang dùng texture vẽ bằng code tạm (xem Sprint 4). Icon Bình tưới đã có prompt sẵn ở `art-refs/items/tools.md`; overlay đất ẩm vừa thêm prompt mới cùng file, cần gen khi tới lúc làm asset thật.

**Đã rà soát + sửa toàn bộ `art-refs/` cho khớp với spec thật** (`docs/world/maps.md`, `docs/world/npc.md`, `docs/world/village.md`, `docs/design/story.md`, `docs/gameplay/gacha.md`, `docs/gameplay/professions.md`) — trước đó nhiều file art-refs được viết tự do, lệch hẳn với lore/thiết kế thật đã chốt sau này:
- `art-refs/enemies/monsters.md`: viết lại toàn bộ — tên quái từng map trước đó không khớp `docs/world/maps.md` (map nào dùng tên tự đặt khác hẳn), thiếu hẳn Map 6 (Rừng Thiêng), thiếu Map 7 riêng (trước đó Map 6+7 gộp nhầm thành 1 "Rừng Cổ"), và **boss cuối trước đó ghi sai là "Rồng Cổ Đại" — thật ra là "Linh Thụ Ma Hóa"** (chính Linh Thụ Cổ Đại của làng bị Ma Khí chiếm, theo `docs/design/story.md`). Thêm quái cốt truyện đặc biệt Hoàng (bị Ma Khí kiểm soát, không giết được).
- `art-refs/characters/npcs.md`: NPC #10 trước đó là "Người Giao Nhiệm Vụ" tự đặt, không khớp 10 NPC canonical trong `docs/world/npc.md` — đổi thành đúng NPC #10 thật: **Cô Gái Gacha**. Thêm mục "NPC Cốt Truyện" cho **Linh** (Thiếu Nữ Rừng, phân thân Linh Thụ).
- `art-refs/world/maps.md`: đánh lại số toàn bộ map cho khớp `docs/world/maps.md` (map cũ đánh số 0-5 lệch 1 so với spec thật 0-8). Thêm **Map 0 — Làng Ẩn Nhân** (hub, trước đó hoàn toàn không có prompt) và **Map 6 — Rừng Thiêng** (map ô nhiễm/tha hóa, trước đó không tồn tại, dễ nhầm với Map 7 Rừng Cổ). Sửa mô tả đấu trường boss cuối (Map 8) từ theme rồng sang theme cây cổ thụ tha hóa.
- `art-refs/world/buildings.md`: thêm Bến Câu Cá, Linh Thụ Cổ Đại (bản chưa tha hóa), Quảng Trường Làng — cả 3 đều có trong `docs/world/village.md` (13 địa điểm) nhưng trước đó thiếu. Đổi "Cổng Làng" (mô tả vui vẻ, đèn lồng chào mừng) thành đúng "Cổng Làng Đông — bị phong ấn" (tường năng lượng tối, lính gác ma) theo đúng lore thật.
- `art-refs/ui/ui.md`: thêm minimap frame, 6 icon nghề nghiệp (khớp `docs/gameplay/professions.md`), banner + hiệu ứng reveal Gacha (khớp `docs/gameplay/gacha.md` — 2 pool Standard/Premium), icon marker quest thế giới.
- `art-refs/items/materials.md`: thêm icon Mảnh Ký Ức (lore fragment, dùng chung 1 sprite cho cả 10 mảnh).
- `docs/data/asset-manifest.md`: cập nhật toàn bộ bảng id liên quan (NPC, quái/boss, buildings, UI, materials, tileset theo map) khớp lại với các thay đổi trên. `art-refs/README.md` cũng sửa 2 chỗ đếm số (map, quái) bị lỗi thời.
- **Toàn bộ việc trên chỉ là sửa prompt/tài liệu — chưa gen ảnh thật cho bất kỳ mục mới nào** (Map 0, Map 6, Bến Câu Cá, Linh Thụ Cổ Đại, Quảng Trường Làng, Cô Gái Gacha, Linh, Hoàng, boss cuối bản cây, minimap, icon nghề, gacha banner, lore fragment). Việc gen ảnh vẫn theo quy trình ở Phụ lục A `dev-schedule.md`.

---

## Sprint 0 — Data pipeline & project structure ✅

- [src/data/types.ts](../../src/data/types.ts) — TS interface cho toàn bộ schema (Crop, Item, Weapon, Armor, Skill, Monster, Npc, Quest, Recipe, Fish, Profession, GachaPool, GameEvent, SaveState...).
- [src/data/DataLoader.ts](../../src/data/DataLoader.ts) — `preloadGameData()` + `initGameData()`, export `GameData` để tra dữ liệu.
- [src/systems/SaveManager.ts](../../src/systems/SaveManager.ts) — `createDefaultSaveState`, `saveGame`/`loadGame`/`hasSaveGame`/`clearSaveGame` (localStorage).
- `public/data/*.json` — 13 file mẫu (crops, items, weapons, armor, skills, monsters, npc, quests, recipes, fish, professions, gacha, events), mỗi file 1 entry mẫu.

## Sprint 1 — Player movement + tilemap nông trại 🟡 (gần xong)

- **~~Tileset Farm dạng tile-grid~~ — ĐÃ THAY THẾ bằng ảnh nền tĩnh (xem mục "Đổi nền map sang ảnh tĩnh" bên dưới).** 14 tile gốc vẫn còn nằm trong `public/assets/tilesets/farm/` (không xóa) nhưng không còn dùng trong code từ khi đổi sang `background.png`.
- **`public/assets/tilemaps/farm_test.json`** — map test bằng Tiled, **không còn được `GameScene.ts` load nữa** (đã đổi sang ảnh nền tĩnh), vẫn giữ file lại trên đĩa, chưa xóa.
- **[src/entities/Player.ts](../../src/entities/Player.ts)** — `Phaser.Physics.Arcade.Sprite`, di chuyển 4 hướng bằng **phím mũi tên** (đã bỏ WASD theo yêu cầu), animation đổi theo hướng (idle_front, walk_front/back/side), hitbox thu nhỏ ở chân. **Phím Space** kích hoạt animation `attack` (đứng yên, chạy hết 8 frame rồi tự quay lại idle/walk) — chỉ có bản quay mặt ra trước, dùng chung cho mọi hướng ở bản Alpha (đúng ghi chú asset-manifest.md). Chưa có logic gây sát thương/hitbox tấn công thật — mới chỉ là animation.
- **[src/scenes/GameScene.ts](../../src/scenes/GameScene.ts)** — spawn player, va chạm ẩn cho nhà/giếng/chuồng (xem mục ảnh nền tĩnh), camera `startFollow` bám player.
- **[src/scenes/PreloadScene.ts](../../src/scenes/PreloadScene.ts)** — load toàn bộ data JSON + ảnh nền farm + spritesheet player (2 giới tính).
- **Prompt gen ảnh Farm** đã thêm vào `art-refs/world/maps.md` (Map 0 — Nông Trại) kèm định hướng art chung chống lệch tone "toylike" cho toàn bộ file.

### Vấn đề đã biết / chưa xong

- **Map Farm giờ dùng ảnh nền tĩnh (`background.png`), không còn Tiled/tile-grid** (xem mục "Đổi nền map Farm sang ảnh nền tĩnh" ở trên) — va chạm chỉ có 3 vùng ẩn tay (nhà/giếng/chuồng), luống đất trong ảnh chưa có va chạm/logic gì. Sprint 2 (`FarmTile`) sẽ cần nghĩ lại cách xác định + lưu state từng ô đất trồng trọt vì không còn dựa vào tile index Tiled được nữa.
- `village/` tileset vẫn chưa có prompt art-refs riêng — chưa cắt ảnh.
- Player còn thiếu asset: `idle_back/idle_left/idle_right`, `attack_back/attack_side` (đang dùng tạm frame khác thay thế theo asset-manifest.md).
- Seam/grid line mờ giữa tile — **không còn liên quan** vì map Farm không còn ghép tile nữa (đã đổi sang ảnh nền tĩnh), có thể vẫn còn ở map khác nếu sau này dùng lại tile-grid.
- **Đã chốt chuẩn mới: 8 frame/hành động/hướng** (thay vì 7/3/3/3/4 như trước) — đã viết lại `art-refs/characters/player.md`, thêm mục "Gen dạng sprite sheet" (10 prompt gộp cả 8 frame vào 1 ảnh, nền trắng đặc). Cập nhật số frame mục tiêu trong `docs/data/asset-manifest.md`. **Đã xóa mục "Nhân Vật Gốc — gen từng frame riêng"** (bản dự phòng 80 prompt/1-ảnh-1-frame, viết ban đầu nhưng chưa từng dùng) vì thực tế tool đang dùng gen được thẳng sprite sheet — chỉ còn giữ mục "Gen dạng sprite sheet" (đang dùng thật, đã tạo ra toàn bộ asset women). File từ ~620 dòng còn ~263 dòng.
- **Prompt Walk Trước/Sau — tay vung, đã thử 4 lần, vẫn chưa chốt được:**
  1. Bản gốc: chỉ frame 1/5 (chạm đất) có mô tả tay, frame giữa không nhắc → tay gần như đứng yên.
  2. Lần 1 dùng "fully" + câu chốt "must swing continuously... never staying still" → test thật: tay vung quá đà như đấm box ở frame 1/5, frame giữa vẫn đứng yên.
  3. Lần 2 dùng "gently"/"naturally" (quá mơ hồ) → test thật: tay đứng yên hoàn toàn, tệ hơn cả bản gốc.
  4. Lần 3 dùng mốc cụ thể "fist reaching about to hip level" + "relaxed hanging position" cho frame giữa → test thật: **vẫn chưa rõ ràng** (user báo "chưa rõ ràng lắm"), gần như không thấy khác biệt giữa các frame.
  5. **Lần 4**: đưa mô tả tay lên đầu prompt + ví dụ tham chiếu "marching soldier" + yêu cầu nắm tay "visibly outside/offset from the body outline" (lộ hẳn ra ngoài bóng cơ thể) cho mọi frame. **Đã gen thử (`women-white-huong-truoc-new.png`) và tích hợp vào game — có cải thiện thật**: tay + chân đều thấy khác biệt rõ giữa các frame khi chạy animation trong game (không còn dính cứng như lần 3), nhưng biên độ vung vẫn khá tinh tế/nhẹ, chưa phải kiểu vung tay rõ rệt mạnh. **Đã ghi đè `walk_front_01..08.png` + `walk_front_strip.png` của women, PreloadScene không cần sửa (kích thước 162×334 khớp sẵn).** Coi như tạm chấp nhận được, không tiếp tục dò thêm trừ khi user yêu cầu.
  - **Walk Sau đã gen lại theo lần 4 và tích hợp xong** (`women-white-huong-sau-new.png` → `walk_back_01..08.png` + strip, 162×334, cùng toạ độ cắt với walk_front v2) — verify qua ảnh chụp thật trong game: đúng hướng lưng xuyên suốt (không lộ mặt), tay/chân biến đổi nhẹ giữa các frame, cùng chất lượng với walk_front v2, không lỗi console. **Cả walk_front và walk_back của women giờ đều dùng bản "lần 4" (marching soldier + tay lộ ngoài bóng cơ thể).**
  - Walk Nghiêng (`walk_side`) chưa áp dụng thay đổi này (user chỉ yêu cầu "đi tiến và lùi").
- **Bài học prompt walk Trước/Sau**: mô tả chân kiểu "duỗi thẳng ra trước/ra sau" (fore-aft) khiến AI hiểu nhầm thành góc nhìn nghiêng. Đã sửa sang mô tả "gập gối/nhấc chân" (không fore-aft) + câu chốt "shoulders and hips must stay squared..., must NOT be drawn from a side angle or 3/4 turned view". Hướng Nghiêng (`walk_side`) vẫn giữ fore-aft vì đó đúng là góc nhìn nghiêng. **Đã test thật với asset women mới — hướng đúng hoàn toàn, không còn nghiêng.**
- **Bài học "càng thêm mô tả càng xấu"**: nhồi quá nhiều chi tiết cùng lúc (nếp vải, viền đỏ, băng quấn tay, rim light, "polished reflective", "confident smirk") làm AI vẽ rối (mũ giáp xám to bất thường, biểu cảm méo) thay vì đẹp hơn. Đã rút gọn: bỏ băng quấn tay/fold-shading/rim light/"polished reflective", đổi "confident smirk" → "warm cheerful smile". **Đã test thật với asset women mới — kết quả đẹp, sạch, không còn lỗi mũ giáp/biểu cảm méo.**
- **`women` đã hoàn thành bộ 8-frame mới cho toàn bộ 5 hành động** (idle_front, walk_front, walk_back, walk_side, attack) — cắt từ `public/images/Characters/Women/none-bg/women-{dung-yen,huong-truoc,huong-sau,nghiêng-phai,white-attack-pose}.png` (nền đã xóa sẵn). idle/walk mỗi frame 162×334, attack riêng 170×335 (tay vung rộng hơn nên bbox khác). Script cắt dùng xong đã xóa (không lưu lại trong repo, chỉ giữ output trong `public/assets/sprites/player/women/`). Đã cập nhật `Player.ts` (`FRAME_SIZES`, `WALK_CONFIG` theo giới tính — women dùng `generateFrameNumbers` full 8 frame, men vẫn dùng mảng frame cũ đã lọc) và `PreloadScene.ts` (frame size load theo từng action × giới tính qua bảng `frameSizes`, không còn dùng chung 1 kích thước cho cả gender). Đã verify bằng ảnh chụp thật cả 4 hướng di chuyển + load asset attack không lỗi — animation mượt, đúng hướng, không lỗi console.
- **Ảnh nguồn thô KHÔNG bị xóa** — các file `women-white-*.png` (bản nền trắng trước khi rembg) và `public/images/Characters/women.png` (sheet gốc hệ cũ) vẫn giữ nguyên trong `public/images/`, chỉ ghi đè output trong `public/assets/sprites/player/women/`. User đã xác nhận không muốn xóa gì ở bước này.
- **`men` vẫn còn bộ cũ** (idle 7 frame, walk 3 frame/hướng dùng tạm 2 frame hợp lệ, attack 4 frame, kích thước 164×213) — chưa gen lại theo chuẩn 8-frame mới.
- **Gameplay mặc định đã đổi sang `'women'`** trong `GameScene.ts` (trước đó là `'men'`) — vì asset women mượt/đẹp hơn hẳn. Muốn test lại bộ men thì đổi tham số gender khi tạo `new Player(...)`.
- **Giảm size nhân vật in-game**: thêm `SPRITE_SCALE` theo giới tính trong `Player.ts` (men 0.38, women 0.24) + gọi `setScale()` ở constructor — trước đó sprite render nguyên kích thước gốc (162×334 với women), to gấp nhiều lần tile 32×32. Đã verify bằng ảnh chụp thật, nhân vật giờ cao khoảng 1.5-2 lần tile, hợp lý với map.
- **Y-sort + bóng đổ (2.5D giả lập) — "Phương án 2"**: map top-down 2D thuần vẫn phẳng, không có cảm giác chiều sâu như game tham chiếu "Avatar" (Avatar Teamobi). Thay vì vẽ lại toàn bộ art theo góc nghiêng (phương án 1, tốn công + rủi ro lệch tone với ~210 decoration đã xong), chọn làm bằng code trước:
  - **Y-sort**: `Player.update()` gọi `this.setDepth(this.y)` mỗi frame; `GameScene.placeFarmProps()` gọi `.setDepth(y)` một lần khi đặt mỗi prop (fence/pond/signpost/hay_bale/wood_pile/grass_tuft) theo toạ độ chân (anchor Y). Nhờ vậy prop nào có Y lớn hơn (thấp hơn trên màn hình) sẽ vẽ đè lên object phía trên — player có thể đi "trước" hoặc "sau" prop tuỳ vị trí, giống hiệu ứng độ sâu.
  - **Bóng đổ**: texture ellipse mờ đen (`shadow_oval`) tạo 1 lần bằng `Phaser.GameObjects.Graphics.generateTexture()` (không cần asset PNG riêng) trong `GameScene.createShadowTexture()`. Player có 1 shadow image riêng theo sát chân (`shadowOffsetY` tính từ frame height/scale từng giới tính), cập nhật vị trí + depth (`y - 1`, luôn thấp hơn depth player) mỗi frame trong `update()`. Prop có chiều cao thật (rào, biển báo, rơm, củi) cũng có bóng tương tự, đặt 1 lần lúc khởi tạo; ao nước (phẳng) và cỏ trang trí (quá nhỏ) không cần bóng.
  - Tầng nền tilemap (`ground` layer) set `depth = -10000` cố định để luôn nằm dưới cùng, không tham gia Y-sort.
  - **Đã verify bằng Puppeteer**: teleport player qua `window.__game` (debug expose tạm, đã xoá lại sau khi test) để đứng ở Y thấp hơn/cao hơn 1 prop (hay_bale) đúng điểm giao cắt — xác nhận player bị che khi Y nhỏ hơn prop, và đè lên prop khi Y lớn hơn, đúng như kỳ vọng Y-sort. Bóng đổ hiển thị rõ dưới chân player và dưới prop có chiều cao. Theo yêu cầu user "thử phương án 2 đi, nếu xấu tôi sẽ làm lại phương án 1" — kết quả code này coi như bản thử đầu tiên, cần user tự đánh giá trực quan trong game xem đã đủ cảm giác chiều sâu chưa trước khi quyết định có cần quay lại phương án 1 (vẽ lại art nghiêng) hay không.
  - **Bài học bóng đổ v1 "trông giả"**: bản đầu dùng `Graphics.fillEllipse` tô đặc 1 màu đen alpha cố định, viền cứng — user phản hồi ngay "trông nó hơi giả" (nhìn như dán đè lên nền, nhất là trên tile đất cày có texture rõ). **Đã sửa sang gradient toả tròn** (`createCanvas` + `CanvasRenderingContext2D.createRadialGradient`, tâm đậm alpha 0.4 mờ dần về alpha 0 ở mép) thay vì tô đặc, đồng thời bật `texture.setFilter(Phaser.Textures.FilterMode.LINEAR)` riêng cho texture này (game đang bật `pixelArt: true` mặc định NEAREST — không bật LINEAR riêng thì gradient bị vằn/răng cưa thay vì mờ mượt). Đã verify lại bằng ảnh chụp thật — bóng giờ mờ dần tự nhiên, không còn viền cứng.
- **Đổi nền map Farm từ tile-grid sang ảnh nền tĩnh**: user phát hiện tile `soil_tilled_wet` (đất đã tưới) trông như nước xanh chứ không giống đất ẩm khi lặp lại nhiều ô — hỏi có thể dùng `background.png` (ảnh minh hoạ toàn cảnh nông trại, cắt nguyên khối từ góc trên-trái `Farm.png` gốc, 569×388px, có sẵn nhà/luống đất/giếng/chuồng heo-bò) làm nền thay vì ghép tile không. Đã xác nhận với user 2 điểm quan trọng trước khi đổi:
  1. Bỏ hẳn tile-grid (`farm_test.json` + `tileset.png`) cho map Farm, chấp nhận mất khả năng track state từng ô đất theo `Tiled` — hệ thống `FarmTile` (Sprint 2) sẽ cần thiết kế lại cách xác định vùng đất trồng trọt (không còn dựa vào tile index nữa).
  2. Bỏ hết prop tự đặt bằng code cũ (hàng rào/ao/biển báo/rơm/củi) vì `background.png` đã vẽ sẵn nhà + chuồng heo-bò-có-hàng-rào ở vị trí khác — dùng đúng những gì ảnh có sẵn, tránh 2 bộ hàng rào/ao chồng chéo hình ảnh.
  - **`PreloadScene.ts`**: chỉ còn load `farm_background` (ảnh `background.png`), đã bỏ load `farm_test` tilemap JSON + toàn bộ ảnh prop riêng lẻ (fence/pond/signpost/hay_bale/wood_pile/grass_tuft/tileset).
  - **`GameScene.ts`**: `create()` giờ chỉ `add.image(background)` + 3 vùng va chạm ẩn (`Phaser.GameObjects.Zone` + static body, không vẽ gì, không texture) đặt đúng vị trí nhà/giếng/chuồng heo-bò trong ảnh (toạ độ ước lượng bằng mắt theo tỉ lệ ảnh, không cần khớp pixel chính xác). Luống đất trồng trọt trong ảnh **chưa có va chạm** (chưa cần thiết ở giai đoạn này).
  - **Phóng to nền theo `BACKGROUND_SCALE = 600/388`** (lúc này khung nhìn game còn là 800×600, xem mục đổi sang 960×540 ở cuối file): ảnh gốc 569×388 nhỏ hơn khung nhìn nên nếu giữ nguyên tỉ lệ sẽ lộ viền tối (dark bar) quanh map khi camera không scroll đủ xa để lấp đầy màn hình. Phóng theo chiều cao cho khớp 600px, dư ra chút bề ngang (880 > 800) để camera vẫn có khoảng scroll thay vì đứng yên hệt — đã verify bằng ảnh chụp, không còn viền tối. **Toàn bộ mục này giờ đã lỗi thời** — `background.png` + `BACKGROUND_SCALE` không còn dùng, xem mục "Đổi tiếp nền map Farm sang `BaseMap.png`" ngay dưới.
  - **Đã verify va chạm 3 vùng ẩn bằng Puppeteer** (đi từ ngoài vào nhà/giếng/chuồng, xác nhận bị chặn đúng biên) — **rút ra bài học viết test**: dùng `sprite.body.reset(x,y)` để "teleport" nhân vật trong lúc test làm sai lệch body tạm thời (bỏ qua offset hitbox đã cấu hình, set thẳng body.position theo x,y truyền vào thay vì tính lại từ offset), khiến kết quả test đợt đầu trông như "va chạm không hoạt động" dù code game hoàn toàn đúng. Cách test đúng: chỉ `sprite.setPosition(x,y)` rồi đợi 1 tick vật lý để Phaser tự đồng bộ lại body theo offset, không gọi `body.reset()` khi chỉ muốn di chuyển tức thời.
  - File tile cũ (`farm_test.json`, `tileset.png`, và 13 ảnh tile lẻ trong `public/assets/tilesets/farm/`) lúc này **chưa xóa khỏi đĩa** — theo nguyên tắc không tự xóa file khi chưa chắc chắn được yêu cầu. **Đã xóa ở phiên dọn dẹp sau này**, xem mục "Rà soát + dọn dẹp cấu trúc thư mục" cuối file.
- **Đổi tiếp nền map Farm từ `background.png` sang `BaseMap.png`**: user cung cấp ảnh mới đẹp/chi tiết hơn hẳn (1774×887, đồng cỏ mở với lưới đường mòn chia lô, suối cắt ngang có 2 cầu gỗ, thác nước + vách đá góc trên-trái, ao nước góc dưới-phải, viền rừng thông + hoa anh đào, dãy núi/bầu trời phía xa) — **không còn nhà/giếng/chuồng** như bản cũ, nên bỏ hẳn 3 vùng va chạm ẩn cũ, thay bằng va chạm mới khớp địa hình ảnh này.
  - Ảnh mới (1774×887) đã lớn hơn khung nhìn game (960×540, sau khi đổi ở mục cuối file — lúc quyết định này khung nhìn còn là 800×600, kết luận vẫn đúng ở cả 2 mốc) ở cả 2 chiều nên **bỏ `BACKGROUND_SCALE`**, dùng nguyên tỉ lệ gốc (scale=1).
  - **Va chạm mới** (`GameScene.placeInvisibleObstacles()`): dải bầu trời/núi xa ở top (không phải mặt đất — 1 trong các kỹ thuật "background horizon art" đã bàn ở mục Y-sort phía trên), vách đá + thác nước chính góc trên-trái, đoạn suối nhỏ nối thác xuống sông chính, 3 đoạn sông ngang (chừa đúng 2 khoảng trống ở vị trí 2 cầu gỗ để đi qua được), và ao nước góc dưới-phải.
  - **Toạ độ suối/cầu lấy từ soi màu pixel thật** thay vì ước lượng bằng mắt thuần: cài tạm `pngjs` (gỡ lại sau khi xong, không lưu trong repo) để đọc RGB từng pixel của `BaseMap.png`, dò cột nào có màu nước (kênh B trội) để tìm chính xác dải sông + 2 khoảng trống là vị trí cầu, quét theo `x`. Cách này chính xác hơn hẳn so với đoán bằng mắt — bản đoán bằng mắt đầu tiên (dải sông đặt cao hơn vị trí nước thật ~50-80px) tạo ra 1 "bức tường vô hình" lơ lửng giữa đồng cỏ, phải soi pixel lại mới sửa đúng.
  - **Bug quan trọng phát hiện khi verify**: `physics.world.bounds` (vùng giới hạn vật lý toàn bộ world) **không tự khớp theo kích thước ảnh nền** — mặc định lấy theo kích thước config game (lúc đó 800×600, giờ là 960×540) trừ khi tự gọi `physics.world.setBounds(...)`. Vì chưa gọi hàm này, player bị chặn cứng ở góc trên-trái khung config dù map thật lớn hơn nhiều, khiến mọi test va chạm sông/ao/thác ở xa đều vô nghĩa (player còn chưa đi tới nơi đã bị world bounds chặn lại trước). **Đã sửa**: thêm `this.physics.world.setBounds(0, 0, background.displayWidth, background.displayHeight)` trong `create()`, cùng lúc với `cameras.main.setBounds(...)`. Bug này tồn tại từ khi đổi sang ảnh nền tĩnh lớn hơn khung nhìn config (cả bản `background.png` 880×600 lẫn bản `BaseMap.png` 1774×887) — nhắc nhở: **mọi khi đổi kích thước map/nền HOẶC kích thước khung nhìn game, phải nhớ set cả 2: `cameras.main.setBounds` VÀ `physics.world.setBounds`, không chỉ 1 trong 2.**
  - **Đã verify lại toàn bộ va chạm bằng Puppeteer sau khi sửa bug world bounds**: băng qua cầu 1 thành công (đi vào đúng khoảng trống x giữa 2 mép sông), bị chặn đúng mép sông ở đoạn không có cầu, bị chặn trước ao nước, bị chặn ở dải bầu trời không cho đi lên núi. Player spawn tại (890, 430), giữa 1 ô đất trống trên lưới đường mòn.
  - **`public/assets/tilesets/farm/background.png` và 3 file liên quan (nhà/giếng/chuồng cũ)** không còn dùng nhưng vẫn giữ trên đĩa, không xóa.
- **Đổi kích thước khung nhìn game từ 800×600 sang 960×540** (`src/main.ts`, `Phaser.Game` config `width`/`height`) — user tự sửa trực tiếp trong IDE. Không ảnh hưởng UIScene (toạ độ HUD neo góc trên-trái, không hardcode theo width/height) hay PreloadScene (thanh loading dùng `this.scale.width/height` động). `GameScene` cũng không hardcode 800×600 ở đâu (world bounds + camera bounds đều lấy theo kích thước ảnh nền `background.displayWidth/Height`, không liên quan viewport) nên chỉ cần sửa comment cho khớp thực tế, không cần đổi logic. Đã verify bằng Puppeteer — canvas render đúng 960×540, layout/HUD/map hiển thị bình thường.
- **Công cụ chỉnh tay vùng va chạm (EditorScene)**: user muốn tự "custom từng khu" thay vì nhờ đoán toạ độ qua soi pixel/ước lượng bằng mắt — bản đầu làm 1 scene editor riêng dùng chung Arcade rectangle zone hiện tại (tránh đổi kiến trúc physics). **Ngay sau đó user yêu cầu thêm — xem mục "Nâng cấp vùng va chạm... sang polygon" bên dưới, đã đổi hẳn sang đa giác, không còn giới hạn hình chữ nhật nữa.**
  - **[src/data/collisionZones.ts](../../src/data/collisionZones.ts)** (mới) — tách mảng `FARM_COLLISION_ZONES` (kèm `label` mô tả từng khu) ra khỏi `GameScene.ts` thành nguồn dữ liệu dùng chung, để cả `GameScene` (chạy game thật) và `EditorScene` (chỉnh tay) cùng đọc.
  - **[src/scenes/EditorScene.ts](../../src/scenes/EditorScene.ts)** (mới) — mở bằng phím **Q** từ `GameScene` (ban đầu dùng F1, user đổi lại thành Q ngay sau đó). Cho phép: kéo thân khối để di chuyển, kéo 1 trong 4 handle góc (ô vàng) để đổi kích thước (góc đối diện giữ cố định làm mốc, tự chuẩn hoá lại thành x/y/width/height dương kể cả khi kéo lố qua phía bên kia), kéo trên vùng trống để vẽ khối mới, chọn 1 khối (viền chuyển cyan) rồi bấm Delete/Backspace để xoá, phím mũi tên để pan camera, lăn chuột để zoom (0.3x–3x). Bấm **E** xuất toàn bộ mảng hiện tại thành code TS ra 1 `<textarea>` đè giữa màn hình (đã chọn sẵn text để copy ngay) — dán đè lại vào `collisionZones.ts` là xong. Bấm **Q** lần nữa quay lại `GameScene`.
  - **Bug phát hiện khi verify**: bấm phím "E" vừa mở export vừa gõ luôn ký tự "e" vào `<textarea>` vừa được focus, đè mất nội dung code đã select sẵn — do trình duyệt vẫn xử lý default action của keydown gốc sau khi focus đã nhảy sang textarea trong cùng 1 event. **Đã sửa**: gọi `event.preventDefault()` ngay đầu mọi handler phím tắt trong `EditorScene` (và cả phím mở editor ở `GameScene`).
- **Nâng cấp vùng va chạm từ hình chữ nhật sang đa giác (polygon) tuỳ ý**: user thấy khối chữ nhật che quá nhiều cỏ/đường mòn xung quanh sông uốn lượn, muốn "custom từng khu" thay vì chỉ co dãn hình chữ nhật. Đổi hẳn model dữ liệu + cơ chế va chạm:
  - **`CollisionZone`** (`collisionZones.ts`) đổi từ `{x,y,width,height,label}` sang `{label, points: {x,y}[]}` — đa giác >=3 điểm bất kỳ. 7 khu cũ migrate sang dạng 4 điểm góc (coi như hình chữ nhật) làm điểm khởi đầu, chỉnh tiếp qua editor.
  - **`GameScene.ts`**: Arcade Physics không hỗ trợ collider dạng polygon nên **bỏ hẳn** `Zone` + `physics.add.collider` cũ, thay bằng tự kiểm tra thủ công mỗi frame (`checkPolygonCollisions`): dự đoán vị trí đáy hitbox (`body.center.x`, `body.bottom`) sau khi di chuyển theo **từng trục X/Y riêng biệt** (không phải cả điểm đích cùng lúc — quan trọng để player vẫn trượt dọc biên polygon khi đi chéo thay vì bị dính cứng), dùng `Phaser.Geom.Polygon.Contains` để biết có "lọt" vào vùng chặn không, nếu có thì `setVelocityX(0)`/`setVelocityY(0)` tương ứng.
  - **`EditorScene.ts`** viết lại hoàn toàn cách tương tác — bỏ hệ thống `setInteractive({draggable})`/`'drag'` event của Phaser (không xử lý được việc "chuyển giao" 1 lượt kéo đang diễn ra sang 1 handle khác mới tạo), thay bằng tự quản lý toàn bộ bằng `pointerdown/pointermove/pointerup` + hit-test thủ công (khoảng cách tới từng handle):
    - Mỗi khu vẽ bằng `Phaser.GameObjects.Graphics` (clear + vẽ lại path mỗi lần đổi điểm) thay vì `Rectangle` — cần thiết vì Graphics vẽ được đa giác N điểm tuỳ ý, Rectangle thì không.
    - Handle góc (vàng) ở từng điểm — kéo để sửa riêng điểm đó; **phải-chuột** 1 điểm để xoá riêng nó (giữ tối thiểu 3 điểm).
    - Handle giữa cạnh (cam, nhỏ hơn) ở trung điểm mỗi cạnh — kéo là **chèn điểm mới** ngay tại đó (biến 1 cạnh thẳng thành 2 đoạn), biến hình chữ nhật thành hình dạng bất kỳ tuỳ ý chỉnh.
    - Handle chỉ hiện cho khối đang được chọn (giảm rối mắt khi có nhiều khối).
  - Đã verify bằng Puppeteer: (1) va chạm thật trong game vẫn đúng sau khi đổi cơ chế (sông/cầu/bầu trời), (2) chọn khối hiện đúng handle, (3) kéo handle giữa cạnh chèn điểm mới tạo hình lõm bám theo đường cong sông thật, (4) kéo riêng 1 góc di chuyển đúng 1 điểm, (5) xuất ra đúng code polygon 5 điểm sau khi chỉnh.
- **EditorScene: thêm cách xoá riêng 1 điểm bằng click trái + phím Delete** (user yêu cầu — trước đó chỉ có phải-chuột để xoá điểm ngay lập tức, không có cách nào dùng phím Delete). Thêm field `selectedVertexIndex` (điểm đang được "chọn riêng" trong khối đang chọn): click trái 1 handle góc vừa chọn điểm đó (tô đỏ viền trắng, phân biệt với các handle vàng còn lại) vừa cho kéo di chuyển ngay như cũ; click thả ra không nhúc nhích thì coi như chỉ chọn, không đổi vị trí điểm. `deleteSelectedVertexOrZone()` (gắn vào phím Delete/Backspace, thay thế thẳng `deleteSelectedZone()` cũ) ưu tiên xoá điểm đang chọn riêng nếu có, chỉ xoá cả khối khi chưa chọn điểm nào — giữ nguyên hành vi cũ khi không có điểm nào được chọn riêng. `selectedVertexIndex` tự reset về `null` khi: đổi/bỏ chọn khối (trong `select()`), xoá 1 điểm xong (chỉ số các điểm sau đó bị lệch, đơn giản nhất là bỏ chọn hẳn thay vì tính lại đúng chỉ số mới), hoặc xoá cả khối.
  - Đã verify bằng Puppeteer: chọn 1 zone thật (`Sông - đoạn phải`), click đúng 1 handle góc (điểm index 2/20) → `selectedVertexIndex` đúng bằng 2, ảnh chụp xác nhận handle đó tô đỏ khác hẳn các handle vàng còn lại; bấm Delete → điểm bị xoá đúng (còn 19/20), `selectedVertexIndex` reset về `null`, ảnh chụp sau xác nhận đường biên nối thẳng qua 2 điểm lân cận, không còn điểm đỏ. Không lỗi console mới.
- **Màn hình loading dùng logo thật thay vì chữ text**: trước đó chỉ có dòng chữ "Shinobi Valley" monospace trơn. Copy `public/images/logo-remove-bg.png` (nguồn gốc, không xoá) sang `public/assets/ui/logo.png` (asset thật dùng trong game). **`BootScene.ts`** thêm `preload()` load riêng logo này **trước** khi `PreloadScene` chạy — để logo có sẵn ngay khi màn hình loading vừa hiện, không phải đợi tự load cùng lúc với chính thanh loading nó thuộc về. **`PreloadScene.ts`**: bỏ text "Shinobi Valley", thay bằng `add.image('game_logo')` scale theo tỉ lệ chiều cao màn hình (logo gốc vuông 1254×1254, scale để cao ~55% màn hình), thanh progress dời xuống dưới logo (`height * 0.82`). Verify bằng Puppeteer: dùng `page.setRequestInterception` để cố tình làm chậm riêng các asset nặng (ảnh nền, spritesheet) ~400ms/asset (không đụng bundle JS/HTML ban đầu để tránh timeout điều hướng) — đủ thời gian chụp được màn hình loading đang chạy, xác nhận logo + thanh progress hiển thị đúng vị trí, đúng tỉ lệ.
- **User tự cập nhật `BaseMap.png` sang bản mới** (1672×941, đổi từ 1774×887 — bố cục cũng khác: ao nước chuyển từ góc dưới-phải sang giữa-phải, thêm cổng torii + đèn đá góc dưới-trái). User yêu cầu chỉ kiểm tra/sửa các logic khác, **không đụng vào toạ độ va chạm** trong `collisionZones.ts` (user tự chỉnh lại bằng EditorScene sau).
  - Kiểm tra `GameScene.ts`: `cameras.main.setBounds` + `physics.world.setBounds` đã đọc động qua `background.displayWidth/displayHeight` từ trước — không cần sửa gì, tự khớp theo ảnh mới.
  - Chỉ có comment trong `GameScene.ts` còn hardcode số cũ (1774×887, 569×388) — đã sửa lại thành mô tả chung chung không ghim số cụ thể, tránh lỗi thời mỗi lần đổi ảnh nền.
  - Vị trí spawn player `(890, 430)` vẫn nằm trong ô đất trống trên lưới đường mòn ở ảnh mới (đã verify bằng ảnh chụp) — không cần đổi.
  - Đã verify bằng Puppeteer: ảnh nền mới render đúng, world/camera bounds khớp kích thước mới, di chuyển bình thường, không phát sinh lỗi console mới. **Vùng va chạm (`collisionZones.ts`) chưa khớp lại với bố cục ảnh mới** — để nguyên theo yêu cầu user, cần vào EditorScene (phím Q) chỉnh lại thủ công.
- **[art-refs/theme.md](../../art-refs/theme.md)** (mới) — user cung cấp "Theme Guide & Color Palette Bible" đầy đủ (Global Theme Prompt, Visual Rules, bảng màu hex theo từng loại địa hình/vật thể: sky/mountain/grass/flower/dirt/river/lake/tree/cherry-blossom/rock/wood, World Design Rules, Lighting, AI Negative Prompt, Theme Identity %). Lưu thành file rule riêng, dùng chung cho **mọi** prompt gen ảnh sau này (không chỉ riêng 1 loại asset) — đã thêm link tham chiếu ở đầu `art-refs/README.md` để session sau đọc trước khi viết prompt mới.
- **Làm lại màn hình loading theo màu theme + sửa lỗi progress bar**: bar cũ tính `bar.width = 296 * value` theo % tải nhưng bị lộ lỗi vẽ tràn ra ngoài khung viền trắng (nghi `value` từ event `'progress'` không tuyến tính/có lúc vượt 1 khi nhiều loader — JSON data + ảnh + spritesheet — chạy song song). **Đã bỏ hẳn progress bar, thay bằng spinner xoay tròn** (`Phaser.GameObjects.Graphics` vẽ 1 vòng tròn mờ làm track + 1 cung 270° màu vàng `#FFD963`, xoay liên tục bằng `tweens.add({angle:360, repeat:-1})`) — spinner không cần đồng bộ theo %, chỉ báo "đang tải" nên né hẳn được lỗi cũ. **Nền màn hình loading** đổi từ màu phẳng tối `#1a1a2e` sang gradient màu trời; màu nền config game (`src/main.ts`) cũng đổi sang Sky Mid `#79C4FF` làm màu dự phòng cho khoảnh khắc trắng trước khi `PreloadScene` kịp vẽ gradient của nó.
  - **Bản đầu dùng `Graphics.fillGradientStyle()`** nội suy thẳng giữa 2 màu Sky Top/Sky Horizon — user phản hồi nhìn vẫn như màu phẳng, không rõ là gradient. **Đã sửa sang gradient 3 điểm dừng** dùng canvas thật (`textures.createCanvas` + `CanvasRenderingContext2D.createLinearGradient` với 3 `addColorStop`: Sky Top `#5CAEF8` → Sky Mid `#79C4FF` (55%) → Sky Horizon `#B7E2FF`) — dùng đúng cả 3 màu sky đã định nghĩa sẵn trong `theme.md` thay vì chỉ 2/3 màu như bản đầu. `fillGradientStyle` của Phaser Graphics chỉ hỗ trợ nội suy 2 màu ở 2 đầu nên không lên được hiệu ứng nhiều điểm dừng rõ rệt như canvas gradient thật.
  - Đã verify bằng Puppeteer (làm chậm asset nặng để chụp kịp lúc đang loading) — nền gradient 3 điểm dừng + spinner hiển thị đúng, rõ rệt hơn hẳn bản 2 màu, load xong chuyển sang `GameScene` bình thường.
- **Rà soát + dọn dẹp cấu trúc thư mục toàn dự án**: user yêu cầu đọc lại toàn bộ file/thư mục xem có cần tổ chức lại không. Kết luận: `src/`, `docs/`, `art-refs/`, `public/data/`, `public/assets/sprites/*`, `public/assets/tilesets/{6 biome}` đã khoa học, không cần đổi. Phát hiện + xử lý:
  - **Đã xoá** (xác nhận không còn được code nào tham chiếu qua `grep`): 15 file tile-grid cũ trong `public/assets/tilesets/farm/` (`background.png`, `dirt_path_corner/straight`, `fence_corner/gate/straight`, `grass_plain`, `grass_tuft`, `hay_bale`, `pond_water_tile`, `signpost`, `soil_tilled_dry/wet`, `tileset.png` — chỉ còn lại `BaseMap.png` đang dùng thật) + `public/assets/tilemaps/farm_test.json` (tilemap Tiled cũ, không còn load) + thư mục rỗng `public/images/Characters/Men/`.
  - **Đã đổi tên** cho rõ nghĩa (không xoá nội dung, chỉ đặt lại tên): `public/images/Characters/asset_XGzBdyELaaMNcjve9Fo8n7u8_..._# Female player character — AI.png` (tên chứa nguyên prompt AI, ảnh thật là 1 concept ninja nam cầm kiếm, không liên quan hệ thống player nam/nữ hiện tại) → `ninja-concept-early-draft.png`. 2 file `public/images/BaseMap/ChatGPT Image ....png` (tên mặc định do tool đặt) → `basemap-draft-with-buildings.png` (bản concept có nhà/cối xay gió, không dùng) và `basemap-draft-open-field.png` (bản đồng cỏ mở, gần giống bản đang dùng). `public/images/BaseMap/BaseMap.png` (bản cũ 1774×887, đã bị thay bởi `BaseMap-V2.png`) → `basemap-v1-old.png` để phân biệt rõ với `BaseMap-V2.png`.
  - **Đã sửa lại bảng ở `docs/data/asset-manifest.md`** (mục 17, dòng `farm/`) — vẫn liệt kê đủ 14 tile lẻ cũ như đang dùng thật, nay đã lỗi thời, cập nhật ghi rõ farm/ giờ dùng `BaseMap.png` làm nền tĩnh, tile lẻ đã xoá.
  - **Tự sửa sai giữa chừng**: ban đầu định xoá luôn `public/images/base.png` + `base-1.png` vì tưởng "không rõ mục đích/không ai tham chiếu" — mở ra xem trước khi xoá thì phát hiện đây là 2 file **"FILE MÔ TẢ PROMPT/GEN ẢNH – FARM NINJA"**, tức tài liệu tham khảo gốc (infographic) định hướng toàn bộ danh sách asset cần gen cho game (nhân vật, nhà cửa, cây trồng, quái, boss, UI...) — rất quan trọng về mặt tư liệu thiết kế, dù không được code tham chiếu. **Đã giữ nguyên, không xoá** — bài học: file "không được code tham chiếu" không đồng nghĩa "không có giá trị", phải mở xem nội dung trước khi kết luận là rác.
  - **Không đổi tên `BaseMap-V2.png`**: ban đầu định gán nó là "nguồn gốc" của `public/assets/tilesets/farm/BaseMap.png` đang dùng thật (cùng kích thước 1672×941) — nhưng so sánh pixel-by-pixel (`pngjs`, cài tạm) cho thấy **73% byte khác nhau** dù cùng kích thước, tức đây là 2 ảnh khác nhau (có thể qua thêm 1 bước xử lý/tái tạo màu), không phải cùng 1 file. Không đủ chắc chắn về nguồn gốc thật nên không gán quan hệ "A là nguồn của B" vào tên file — chỉ đổi tên bản cũ (`BaseMap.png` → `basemap-v1-old.png`) theo dữ kiện chắc chắn (đã bị thay thế), không suy diễn thêm.
  - Đã verify: `grep` toàn bộ `src/` xác nhận không có chỗ nào import trực tiếp từ `public/images/` (chỉ dùng làm nguồn thô để cắt/copy sang `public/assets/`), `npx tsc --noEmit` sạch, và chạy thử game bằng Puppeteer sau khi dọn — render/di chuyển bình thường, không lỗi console mới.
- **Thêm 3 ảnh trạng thái ô đất trồng cây + đặt lên map Farm** (chuẩn bị nền tảng cho Sprint 2 — hệ thống ô đất): user tự gen thêm 3 frame trong `BaseMap` tileset (`chưa cuốc`, `đã cuốc`, `chậu nước` — chậu nước dùng để trồng cây dưới nước như sen). Yêu cầu: đổi tên đúng chuẩn, đặt 36 ô "chưa cuốc" + 12 ô "chậu nước" lên các thảm cỏ trống trong `BaseMap.png`, sắp xếp đẹp; ô "đã cuốc" chưa dùng, để dành cho sau khi có logic cuốc đất thật.
  - **Đổi tên**: `chưa cuốc.png` → `soil_untilled.png`, `đã cuốc.png` → `soil_tilled.png`, `chậu nước.png` → `soil_water_pot.png` (trong `public/assets/tilesets/farm/`).
  - **Tìm vị trí đặt bằng soi pixel thật** (giống cách tìm suối/cầu trước đây) — quét màu để tìm chính xác 4 thảm cỏ trống không dính đường mòn: 3 thảm hàng trên (mỗi thảm ~320×110px, ngăn cách bởi 2 đường mòn dọc + kẹp giữa 2 đường mòn ngang) dùng cho 36 ô chưa cuốc (12 ô/thảm, lưới 6×2), và 1 thảm nhỏ hàng dưới (~180×90px) dùng cho 12 ô chậu nước (lưới 4×3, tile nhỏ hơn vì thảm thấp hơn nhiều).
  - **[src/data/farmTiles.ts](../../src/data/farmTiles.ts)** (mới) — định nghĩa 4 "thảm" (`PlotGrid`: toạ độ, số cột/hàng, kích thước ô) + hàm `generateGrid()` tự tính toạ độ từng ô căn giữa trong thảm (không phải gõ tay 48 toạ độ), xuất ra `FARM_TILE_PLACEMENTS` dùng chung được cho `GameScene`. Mỗi ô nhỏ hơn kích thước lưới 2px để tạo khe hở nhìn tách bạch giữa các ô thay vì dính liền khối.
  - **`GameScene.ts`**: thêm `placeFarmTiles()` — chỉ là `add.image()` tĩnh (chưa có tương tác cuốc/trồng/tưới, đó là việc của Sprint 2), depth `-1` (trên nền `-10000`, dưới player vì player depth = y luôn > 0 trong map này).
  - Đã verify bằng Puppeteer — cả 36 ô chưa cuốc (3 thảm × 12) lẫn 12 ô chậu nước đều vừa khít trong viền cỏ từng thảm, không tràn ra đường mòn hay đè lên cây, đúng ngay lần đặt đầu tiên nhờ toạ độ đo bằng pixel thay vì áng chừng.
- **Debug: click vào map log toạ độ world** — thêm 1 listener `pointerdown` trong `GameScene.create()`, quy đổi toạ độ màn hình sang world qua `cameras.main.getWorldPoint()` rồi `console.log`. Mục đích: dò nhanh toạ độ (để viết `collisionZones.ts`/`farmTiles.ts` bằng tay) mà không cần mở `EditorScene` (phím Q). Đã verify bằng Puppeteer — click log đúng toạ độ world.
- **Đổi bố cục ô đất chưa cuốc sang dạng "luống cày"**: user gửi ảnh mẫu (luống dài chia ô, giống crop row Stardew Valley) + yêu cầu "1 cột 5 ô, chia làm 3 khu" — khác hẳn lưới ô vuông co cụm giữa thảm ban đầu (6×2, có margin). Viết lại `data/farmTiles.ts`:
  - **`FarmTilePlacement`** đổi từ `size` (1 số, ô vuông) sang `width`/`height` riêng (ô có thể chữ nhật).
  - **`generateGrid()`** đổi cách tính: chia đều `plot.width/cols` và `plot.height/rows` để **lấp kín toàn bộ thảm** (không còn co cụm + căn giữa như bản trước) — mỗi cột = 1 luống dài, tự nhiên chia thành `rows` ô xếp dọc theo yêu cầu.
  - 3 thảm trên đổi từ `6×2` (12 ô/thảm) sang `6×5` (30 ô/thảm) — tổng 90 ô chưa cuốc, lấp kín cả 3 thảm thay vì chỉ chiếm 1 góc nhỏ. Thảm chậu nước giữ nguyên `4×3` nhưng cũng đổi qua công thức lấp-kín mới (trước đó vốn đã gần khít nên không đổi nhiều).
  - Đã verify bằng Puppeteer — mỗi cột hiện rõ 5 ô xếp dọc có khe hở tách bạch, lấp kín đúng 3 khu đất đã đo pixel trước đó, giống bố cục ảnh mẫu.
- **`farmTiles.ts` đổi cơ chế: `cellWidth`/`cellHeight` khai báo trực tiếp thay vì chia `width`/`height` thảm cho `cols`/`rows`** — chia theo cách cũ khiến ô méo thành chữ nhật bất cứ khi nào `cols !== rows`, kể cả khi thảm hình vuông (vd `cols:6,rows:5` luôn ra ô không vuông dù `width===height`). Giờ `cols`/`rows` chỉ quyết định số ô, không ảnh hưởng kích thước — tổng vùng chiếm = `cols*cellWidth` x `rows*cellHeight`, không tự khớp khít viền thảm nữa, cần tự canh `x`/`y`/`cellWidth`/`cellHeight`/`cols`/`rows` cho vừa.
- **Hàng rào gỗ bao quanh khu đất** — user tự thêm `public/images/BaseMap/Farm/Fence.png` (nhiều mẫu tham khảo, không phải tileset sẵn dùng), yêu cầu cắt cho khớp và bao quanh khu đất.
  - Dò bounding-box từng mảnh bằng connected-component trên kênh alpha (`sharp`, cài tạm) thay vì đoán toạ độ — cắt được `fence_horizontal.png` (đoạn ngang, có sẵn trụ 2 đầu) và `fence_vertical.png` (đoạn dọc, có sẵn trụ 2 đầu), lưu ở `public/assets/tilesets/farm/`.
  - **[src/data/fencePlacements.ts](../../src/data/fencePlacements.ts)** (mới) — đặt theo kiểu lặp nhiều đoạn nhỏ dọc theo cạnh (`FenceRun` + `generateRun()`, cùng pattern với `PlotGrid`/`generateGrid()` ở farmTiles.ts) thay vì kéo giãn 1 ảnh duy nhất cho cả cạnh: kéo giãn độc lập width/height làm ảnh chữ nhật gốc (240×140 và 103×350) bị méo gần vuông, lặp nhiều đoạn nhỏ đúng tỉ lệ gốc (`NATIVE_SIZE` + `segmentSize()`) thì giữ được hình dạng thật.
  - `GameScene.placeFence()` — depth theo `bottomY` (đáy từng đoạn) để Y-sort với player.
  - **Bug quan trọng phát hiện khi verify cạnh dưới**: `Player.ts` tính depth theo **tâm sprite** (`this.y`) thay vì **chân** (`this.y + shadowOffsetY`, lệch nhau ~31px với nhân vật women) — khiến player phải đi sâu qua khỏi hàng rào thêm ~31px mới được vẽ đè lên, trong khi hàng rào chỉ dày ~21px, nên đứng rõ ràng bên dưới cạnh dưới hàng rào vẫn bị hàng rào che. **Đã sửa**: `Player.ts` giờ tính depth + vị trí bóng đổ theo toạ độ chân, không phải tâm. Ảnh hưởng tốt tới Y-sort với mọi prop tĩnh khác dùng `bottomY`, không riêng hàng rào.
- **Nhà chính người chơi (3 cấp)** — user tự thêm `public/images/BaseMap/Home/Home.png` (3 mẫu nhà đặt cạnh nhau trong 1 ảnh, khớp đúng mô tả 3 cấp ở `art-refs/world/buildings.md` + giá nâng cấp ở `docs/gameplay/economy.md`: Lv2 10.000đ, Lv3 50.000đ).
  - Cắt bằng connected-component (giống hàng rào) thành `player_house_1/2/3.png` ở `public/assets/sprites/buildings/` (thư mục mới, đúng chuẩn id đã định sẵn trong `docs/data/asset-manifest.md`).
  - **[src/data/housePlacement.ts](../../src/data/housePlacement.ts)** (mới) — hiện chỉ đặt nhà cấp 1 lên map (`PLAYER_HOUSE`), cấp 2/3 preload sẵn nhưng chưa dùng, chờ logic nâng cấp nhà thật (chưa có trong `dev-schedule.md`). Vị trí đặt lên khoảnh cỏ trống cạnh cụm ô đất, dò bằng chính debug click-log toạ độ world đã có sẵn (không đoán chừng).
  - `GameScene.placeHouse()` — cùng pattern Y-sort `bottomY` như hàng rào.
- **Bóng đổ (shadow) cho hàng rào + nhà, tăng cảm giác 3D**: player đã có bóng từ trước (tự di chuyển theo mỗi frame); hàng rào/nhà là prop tĩnh nên vẽ bóng 1 lần lúc đặt.
  - Hàng rào: dùng lại `SHADOW_TEXTURE` (gradient tròn mờ có sẵn cho player) qua helper `addGroundShadow()`, đặt đúng đáy từng đoạn — nhìn ổn ngay, không cần chỉnh thêm.
  - Nhà: **3 lần thử mới ra bản ổn**, ghi lại làm bài học cho prop lớn sau này:
    1. Dùng lại `SHADOW_TEXTURE` phóng to/kéo dẹt — ra 1 vệt tròn nhỏ giữa đáy, nhìn như miếng dán lơ lửng (nhà to, đáy rộng, texture vốn thiết kế cho player nhỏ gần tròn).
    2. Đổi sang texture riêng hình chữ nhật bo góc mờ viền (`ctx.filter = blur()`) + lệch hướng sáng — đỡ hơn nhưng user vẫn thấy "chưa đủ tự nhiên".
    3. Thử bóng silhouette thật (nhân bản chính ảnh nhà, tint đen, bẹp trục Y, mờ) để bóng theo đúng viền mái/ống khói — gặp 2 bug liên tiếp trước khi ổn: (a) dùng `postFX.addBlur()` (BlurFX runtime của Phaser) khiến bóng **render lệch hẳn sang vị trí khác** trên màn hình khi origin khác (0.5,0.5) — đây là hạn chế thật của pipeline này, phải bỏ hẳn, thay bằng tự vẽ silhouette đã mờ sẵn vào 1 canvas texture (`ctx.filter=blur()` lúc vẽ + `globalCompositeOperation='source-in'` để tô đen đúng theo alpha đã mờ) rồi hiển thị như ảnh tĩnh thường; (b) bẹp bóng theo tỉ lệ **chiều cao ảnh gốc** (316px, cao chủ yếu do mái nhọn vẽ chếch lên) khiến bóng dài gần bằng thân nhà, tràn qua cả đường mòn phía dưới — phải tính theo **bề ngang** thay vì chiều cao.
    4. Dù đã sửa 2 bug trên, silhouette vẫn có khoảng cách/tách rời nhìn không tự nhiên với đáy nhà (do padding + blur làm mờ viền tiếp giáp) — **cuối cùng quay lại đúng công thức đã ổn với hàng rào** (`addGroundShadow`, bầu dục mờ viền, đặt sát đáy, chỉ đổi bề ngang cho phù hợp vật to) thay vì tiếp tục đẽo gọt silhouette. **Bài học**: đừng over-engineer bóng đổ cho 1 prop lớn nếu công thức đơn giản đã verify ổn cho prop khác — thử phức tạp hơn trước, lỗi nhiều hơn, không chắc đẹp hơn.
- **EditorScene giờ hiện đủ ngữ cảnh xung quanh + cho đi lại để test va chạm trực tiếp** — trước đây Editor chỉ có nền trơn + polygon đỏ, không thấy nhà/hàng rào/ruộng nên khó canh vùng chặn khớp thực tế, và chỉ pan camera bằng mũi tên chứ không đi lại được để tự kiểm tra khu nào đi được.
  - **[src/systems/WorldDecorations.ts](../../src/systems/WorldDecorations.ts)** (mới) — `renderStaticWorldDecorations(scene)` vẽ tĩnh ruộng/hàng rào/nhà lên bất kỳ scene nào (không gắn `FarmManager`, ô đất luôn hiện "chưa cuốc"). Cố tình KHÔNG dùng chung code với `GameScene.placeFarmTiles/placeFence/placeHouse` (đã verify kỹ, gắn chặt state riêng) để tránh rủi ro đụng vỡ — chấp nhận trùng lặp nhỏ đổi lấy an toàn.
  - **`EditorScene.ts`**: thêm `Player` thật (spawn trùng `GameScene`), **camera đổi từ pan-bằng-mũi-tên sang `startFollow` bám player** — mũi tên giờ dùng để đi lại. Va chạm dùng lại `resolvePolygonCollision` (xem mục bug bên dưới) nhưng lấy polygon trực tiếp từ `this.zones` (dữ liệu đang chỉnh tay, có thể chưa export) — sửa xong 1 khối là đi thử được ngay, không cần xuất/dán rồi quay lại `GameScene`.
  - Đã verify bằng Puppeteer: mở Editor thấy đủ nhà/hàng rào/ruộng cạnh các vùng chặn, đi lại bằng mũi tên + camera theo mượt, không lỗi console mới.
- **Bug va chạm: kẹt cứng vĩnh viễn ở góc lõm/mũi nhọn của polygon** (user báo + gửi ảnh: đứng giữa viền răng cưa hàng cây, bấm hướng nào cũng không nhúc nhích được). Nguyên nhân kép trong thuật toán chặn theo trục cũ (`checkPolygonCollisions` ở cả `GameScene` lẫn `EditorScene`, trùng lặp y hệt nhau):
  1. **Lọt chéo qua góc (corner-cutting)**: chỉ kiểm "di chuyển X thôi" và "di chuyển Y thôi" riêng lẻ để cho phép trượt dọc biên khi đi chéo — nhưng ở góc lõm sâu (viền zigzag), CẢ 2 kiểm tra trục riêng lẻ có thể ĐỀU pass trong khi điểm đích CHÉO (đi cả 2 trục cùng lúc) lại cắt ngang qua đúng mũi nhọn đó, không trục nào phát hiện ra. Player "lách chéo" lọt hẳn vào trong mũi nhọn.
  2. **Không có đường thoát khi đã kẹt bên trong**: 1 khi đã lọt vào (do bug 1, hoặc bất kỳ lý do nào khác), bước di chuyển nhỏ mỗi frame theo BẤT KỲ hướng nào từ bên trong 1 mũi nhọn hẹp vẫn tính là "còn nằm trong" polygon → velocity cả 2 trục bị huỷ liên tục → đứng im vĩnh viễn, đổi hướng cũng vô ích.
  - **[src/systems/CollisionUtils.ts](../../src/systems/CollisionUtils.ts)** (mới) — `resolvePolygonCollision(sprite, polygons, deltaMs)` dùng chung cho cả `GameScene` và `EditorScene` (xoá code trùng lặp cũ), sửa nguyên nhân 1: sau khi xử lý từng trục, kiểm thêm 1 lần điểm đích CHÉO — nếu vẫn lọt vào polygon thì huỷ nốt trục Y (giữ trượt ngang, chặn trượt chéo xuyên góc).
  - Đã verify bằng test dựng tay trong trình duyệt thật (polygon hình vuông đơn giản + fake Arcade body): đi chéo cắt góc vào vật cản bị chặn đúng (chỉ 1 trục còn trượt, không xuyên chéo được).
  - **Bản sửa nguyên nhân 2 đầu tiên (bỏ qua chặn hoàn toàn khi đã lỡ ở trong) gây ra bug MỚI nghiêm trọng hơn**: user báo + gửi ảnh — đi lại thoải mái, xuyên thẳng qua cả vùng sông đáng lẽ phải chặn. Lý do: mỗi frame vị trí vẫn "đang ở trong" nên cứ bỏ qua chặn mãi mãi, biến 1 lần lọt vào (dù chỉ do sai số) thành đặc quyền noclip vĩnh viễn trong cả vùng đó. **Đã sửa lại đúng hướng**: thay bằng `pushOutOfPolygon()` — duyệt từng CẠNH polygon (không chỉ đỉnh) tìm điểm gần nhất trên biên, đẩy sprite ra ngoài điểm đó thêm 2px cho chắc, huỷ vận tốc. Dùng `sprite.x/y` để dịch chuyển (không đụng thẳng `body.x/y`) — đúng bài học "setPosition thay vì body.reset() khi dịch chuyển tức thời" đã rút ra trước đó.
  - Đã verify lại bằng Puppeteer (teleport player vào giữa polygon "Sông - đoạn giữa"): bị đẩy ra đúng mép sông gần nhất ngay trong 1 frame, đứng yên ổn định sau đó (không dao động, không lọt lại vào trong), ảnh chụp xác nhận vị trí đẩy ra tự nhiên (đứng ngay trên bờ). Không lỗi console mới.
- **EditorScene thêm pan bản đồ tự do** — sau khi đổi mũi tên sang di chuyển player (mục trên), không còn cách nào xem chỗ khác trên bản đồ mà không đi bộ tới. Thêm **kéo chuột GIỮA** để pan camera tự do (tạm `stopFollow()`); bấm **mũi tên bất kỳ lúc nào** cũng gọi lại `startFollow()` để camera bám về player ngay — không cần phím riêng để "thoát chế độ pan". Đã verify bằng Puppeteer: kéo chuột giữa dịch đúng hướng, bấm mũi tên trả về đúng vị trí theo player.
- **Bug: bấm Q lần 2 (Editor → GameScene) không thoát được, đứng im** (user báo). Nguyên nhân: `UIScene` đăng ký lắng nghe `changedata-selectedSeedName`/`changedata-gameTimeText` trên `this.registry.events` — đây là **event bus TOÀN CỤC dùng chung mọi scene**, không tự huỷ theo vòng đời riêng của `UIScene`. Khi `UIScene` bị `scene.stop()` (lúc vào Editor), listener cũ vẫn treo trên bus toàn cục dù các Text object của nó đã bị destroy theo scene. Lần `GameScene.create()` chạy lại (bấm Q lần 2) gọi `selectSeed()` → ghi registry → bắn sự kiện → listener cũ (treo lơ lửng) cố `.setText()` lên Text object ĐÃ CHẾT → ném lỗi JS giữa chừng `create()` → toàn bộ phần code sau đó (spawn player, `scene.launch('UIScene')`...) không bao giờ chạy, nhìn như "không thoát".
  - **`UIScene.ts`**: đổi 2 listener sang dùng named handler (để `.off()` được), gỡ đăng ký trong `this.events.once(Phaser.Scenes.Events.SHUTDOWN, ...)` — đúng vòng đời của chính scene này, không phụ thuộc ai khác dọn hộ.
  - Đã verify bằng Puppeteer: bấm Q liên tiếp 4 lần (vào/ra/vào/ra) đều chuyển scene đúng, không còn crash, không lỗi console mới. **Bài học chung**: bất kỳ khi nào 1 scene lắng nghe sự kiện trên object TOÀN CỤC (registry, hoặc event emitter dùng chung khác) thay vì `this.events`/`this.input` riêng của chính nó, PHẢI tự gỡ đăng ký lúc `shutdown` — Phaser không tự dọn hộ vì nó không biết listener đó thuộc về scene nào.
- **Nâng cấp quy trình tự test bằng Puppeteer** — trước giờ mỗi lần test phải cài tạm `puppeteer-core` (`--no-save`), viết lại 1 script `.cjs` từ đầu, xong lại gỡ + revert `yarn.lock`. User đề xuất giữ hẳn 1 file harness tái dùng thay vì tạo/xoá liên tục.
  - `puppeteer-core` giờ là **devDependency thật** trong `package.json` (không còn tạm).
  - **[src/main.ts](../../src/main.ts)**: expose `window.__game` bọc trong `if (import.meta.env.DEV)` — Vite tự loại bỏ hẳn khỏi bundle production, không lo lộ ra bản build thật. Không cần thêm/xoá thủ công mỗi lần test như trước.
  - **[scripts/dev-check.cjs](../../scripts/dev-check.cjs)** (mới) — harness cố định: tự dò cổng dev server đang chạy (không cần biết trước `yarn dev` chạy ở cổng nào), có sẵn `withPage()`/`screenshot()`/`getActiveScenes()` dùng chung. Chỉ cần sửa phần "SCENARIO" ở cuối file cho từng lần test, chạy bằng `yarn dev:check`. Ảnh chụp lưu vào `.dev-check-output/` (đã thêm `.gitignore`, không lẫn vào repo).
  - Đã verify bằng cách lặp lại đúng bài test Q ở mục trên (sửa phần scenario, không cài gì thêm) — ra kết quả giống hệt lúc debug thủ công.

## Sprint 2 — Hệ thống ô đất 🟡 (cuốc/trồng/lớn xong, tưới/thu hoạch/inventory để Sprint 4)

- **Cuốc đất → trồng hạt → cây lớn theo thời gian thực** — đúng "Done when" của Sprint 2 trong `dev-schedule.md`. Không đụng tới tưới nước/độ ẩm/thu hoạch/inventory vì 3 việc đó thuộc scope Sprint 4 ("Tưới nước, độ ẩm, thu hoạch, Inventory") theo đúng lịch trình, không phải bỏ sót.
  - **[src/systems/FarmManager.ts](../../src/systems/FarmManager.ts)** (mới) — quản lý state runtime của từng ô đất (`empty→tilled→planted→ready`, dùng lại `FarmTileState` đã có sẵn trong `data/types.ts`), tách biệt hoàn toàn khỏi rendering (GameScene chỉ đọc state từ đây để vẽ). `till()`/`plant()` đổi state có validate (không cuốc lại ô đã cuốc, không trồng lên ô chưa cuốc). `update(now)` mỗi frame kiểm tra ô nào đủ `growth_hours` (tính theo **giờ thực trôi qua**, đúng thiết kế `farming.md`) thì chuyển `ready`. `getVisualStage()` suy ra giai đoạn hiển thị (`seed/sprout/growing/ready`) từ % thời gian đã trồng — tách riêng khỏi `FarmTileState` vì cả 3 giai đoạn đầu đều chung 1 state `planted`.
  - **`data/farmTiles.ts`**: `FarmTilePlacement` thêm field `id` (số thứ tự ổn định, gán 1 lần lúc build mảng) — cần thiết để `FarmManager` map state theo đúng ô, trước đó không có cách nào định danh 1 ô cụ thể.
  - **`public/data/crops.json`**: thêm `green_onion` (Hành lá) + `potato` (Khoai tây) — đủ 3 cây Tier Cơ Bản đầu theo yêu cầu Sprint 2 (trước đó chỉ có `carrot` mẫu). Số liệu lấy đúng bảng trong `docs/gameplay/farming.md` (giá hạt, giờ chín, độ giảm ẩm/giờ...). Asset sprite 4 giai đoạn cho cả 3 cây **đã có sẵn từ trước** (`public/assets/sprites/crops/`), không cần gen thêm/dùng placeholder.
  - **`GameScene.ts`**: `placeFarmTiles()` giữ lại reference ảnh đất theo `id` (`soilImages`) để đổi texture khi cuốc; `syncFarmVisuals()` (gọi mỗi frame sau `farmManager.update()`) đồng bộ texture đất + tạo/đổi/xoá ảnh cây theo giai đoạn, so sánh texture key trước khi `setTexture()` để không upload GPU thừa. Tương tác: phím **E** cuốc/trồng ô đất ngay dưới chân player (dùng chung điểm `body.center.x`/`body.bottom` với `checkPolygonCollisions`), phím **1/2/3** chọn hạt giống (chưa có inventory thật — Sprint 4 — nên tạm thay bằng chọn phím số), phím **G** (DEBUG) đẩy nhanh +1 giờ giả lập mỗi lần bấm để test lớn/chín mà không phải chờ hàng giờ thực (chưa có `TimeManager` ở Sprint 3, đây chỉ là công cụ test tạm, cần xoá khi có game clock thật).
  - **`UIScene.ts`**: thêm dòng hiển thị hạt giống đang chọn, đồng bộ qua `this.registry` (Data Manager toàn cục, dùng chung được giữa `GameScene` và `UIScene` mà không cần custom event bus).
  - Đã verify bằng Puppeteer (gửi phím E/1/2/3/G qua `page.keyboard.press`): cuốc đất đổi đúng texture (ảnh đất đổi từ ô vuông nhỏ sang luống cày), trồng hạt hiện đúng sprite seed, fast-forward qua các mốc giờ hiện đúng sprout→growing→ready (cà rốt chín đúng hình củ cà rốt to), đổi hạt giống cập nhật UI ngay lập tức. Không phát sinh lỗi console mới.
- **Chưa làm trong Sprint 2 này (để Sprint 4 đúng lịch)**: tưới nước/độ ẩm (`moisture` field đã có trong `FarmTile` type nhưng chưa dùng), cộng vật phẩm thu hoạch vào inventory thật (xem action Enter ở ô `ready` bên dưới — mới chỉ có hiệu ứng hình ảnh, chưa có nơi lưu vì Inventory chưa tồn tại), lưu/tải trạng thái ô đất vào `SaveManager` (state hiện chỉ tồn tại trong bộ nhớ 1 phiên chơi, mất khi reload — chưa wiring vì chưa có UI/trigger save-load nào trong game).
- **Sửa lỗi ô đất đã cuốc trông to hơn hẳn ô chưa cuốc** (user báo kèm ảnh) — nguyên nhân: `soil_tilled.png` (221×221, viền cỏ mỏng ~2.7%/cạnh) và `soil_untilled.png` (134×134, viền dày ~6.7%/cạnh) có tỉ lệ viền/nội dung khác hẳn nhau, cùng vẽ ở 1 kích thước ô như nhau thì phần đất cày bị tràn to hơn. Thêm `SOIL_TEXTURE_DISPLAY_SCALE = { farm_soil_tilled: 0.915 }` trong `GameScene.ts`, áp dụng ở cả `placeFarmTiles()` lẫn `syncFarmVisuals()` (chỗ sau trước đó chỉ đổi texture mà không set lại size — cùng dạng bug với mục icon menu hạt bên dưới).
- **Đổi luồng tương tác cuốc/trồng sang phím Enter (bỏ phím E/số)**: bấm Enter lần 1 lên ô đất gần đó để cuốc, bấm Enter lần 2 (hoặc bấm Enter khi ô đã cuốc sẵn) mở **menu chọn hạt** dạng panel neo giữa-dưới màn hình — mũi tên trái/phải đổi hạt đang chọn, Enter xác nhận trồng, Esc hoặc click ra ngoài panel đóng menu không trồng gì. Thay hẳn cơ chế cũ (phím 1/2/3 chọn số, phím E cuốc/trồng luôn không qua bước chọn).
- **Sửa độ rộng box menu hạt cố định quá dài** (user: "kích thước của box bên ngoài hơi dài, cho ngắn lại") — đổi `seedMenuPanelWidth` sang tính động theo số icon hiển thị (`slots * iconSize + gap`) thay vì 1 số hardcode, box tự khít theo nội dung.
- **Tăng bán kính vùng tương tác lên 32px** (user: "giờ cứ phải đứng lên trên thì nó mới trỏ, giờ muốn chỉ cần đứng gần là đã trỏ rồi") — `FarmManager.findTileAt()` (đúng bounds) đổi thành `findNearestTile(x, y, maxDistance)` (ô gần nhất trong bán kính `FARM_TILE_INTERACT_RADIUS = 32`), dùng chung cho cả con trỏ chỉ dẫn (`findInteractionTarget`) lẫn tương tác Enter thật (`interactWithFarmTile`) — đảm bảo "trỏ được" và "bấm Enter tác động được" luôn khớp cùng 1 ô.
- **Mở rộng menu chọn hạt từ 3 cây lên đủ 19 cây (dạng carousel cuộn)** (user báo "chưa thấy load ra đây" — menu vẫn chỉ hiện 3 icon dù `crops.json` đã có 19 cây). `PLANTABLE_CROP_IDS` mở rộng đủ 19 id theo đúng thứ tự `crops.json` — **cho chọn tự do, chưa gate theo `unlock_level`/trừ tiền hạt giống thật** vì Inventory/tiền tệ chưa tồn tại (đúng scope Sprint 4, tạm chấp nhận "miễn phí" ở giai đoạn này). Vì 19 icon không thể nhét vừa 1 hàng, đổi UI sang **cửa sổ trượt** (`SEED_MENU_VISIBLE_SLOTS = 7`): `getSeedMenuWindowStart()` canh giữa ô đang chọn rồi kẹp trong `[0, total-7]`, thêm 2 mũi tên `‹`/`›` (chỉ hiện khi còn cây ẩn ở phía đó), nhãn hiện `"Tên cây (vị trí/19)"`.
  - **Bug phát hiện khi test lần đầu**: 1 số icon hiện nhỏ xíu/gần như trong suốt, lộ nền ô đất phía sau — do `Image.setTexture()` khi đổi giữa các sprite cây khác nhau **không tự giữ lại `setDisplaySize()` đã set trước đó** (kích thước hiển thị = kích thước gốc × scale, mà scale là số cũ để lại từ texture trước, các sprite cây có kích thước gốc rất khác nhau nên ra kết quả sai lệch tuỳ cây). **Đã sửa**: thêm field `seedMenuIconSize = 32`, gọi lại `.setDisplaySize(seedMenuIconSize, seedMenuIconSize)` ngay sau mọi lần `.setTexture()` trong `updateSeedMenuSelection()`. **Bài học chung, áp dụng cho mọi chỗ dùng `setTexture()` để đổi ảnh động**: phải tự set lại size sau khi đổi texture nếu 2 ảnh có kích thước gốc khác nhau — Phaser không tự làm hộ.
  - Đã verify bằng `scripts/dev-check.cjs` (cuốc → mở menu → cuộn phải 5 lần → cuộn tới cuối) + xem trực tiếp 3 ảnh chụp: icon hiển thị đúng kích thước đồng đều ở mọi vị trí cuộn (không còn icon tí hon/trong suốt), cửa sổ trượt đúng (carrot 2/19 → strawberry 7/19 → tomato 8/19 khi cuộn tới cuối do bị kẹp lại ở `total-7`), không lỗi console mới.
  - **Bug thứ 2 phát hiện SAU KHI xem ảnh chụp kỹ hơn** (user gửi ảnh: từ vị trí Bí đỏ 5/19 trở đi, các icon hiện ô đen viền xanh lá chéo — texture "missing" mặc định của Phaser, không phải bug kích thước icon ở trên): `PreloadScene.ts` có 1 danh sách `PLANTABLE_CROPS` RIÊNG (dùng để biết cần load texture `crop_<id>_<stage>` nào) **bị quên cập nhật** khi mở rộng `GameScene.PLANTABLE_CROP_IDS` — vẫn chỉ preload đúng 3 cây cũ (`green_onion`, `carrot`, `potato`), nên 16 cây còn lại chưa từng có texture nào trong cache, `setTexture()` gán key không tồn tại thì Phaser tự thay bằng texture `__MISSING`. **Bài học**: khi 1 danh sách id được dùng ở nhiều nơi (ở đây là "cây nào cho chọn trong menu" ở `GameScene` và "cây nào cần preload texture" ở `PreloadScene`), phải rà tìm HẾT các nơi có bản sao trước khi coi là xong — sửa 1 chỗ tưởng đủ nhưng còn sót bản sao ở scene preload. Đã mở rộng `PLANTABLE_CROPS` trong `PreloadScene.ts` khớp đủ 19 id (đã kiểm tra `public/assets/sprites/crops/` có đủ sprite 4 giai đoạn cho tất cả, không thiếu file nào).
  - Đã verify lại bằng `scripts/dev-check.cjs`: cả 19 icon (green_onion→ancient_seed) hiện đúng sprite tương ứng ở mọi vị trí cuộn, không còn ô "missing texture" nào, không lỗi console mới (chỉ còn 404 `favicon.ico` không liên quan).
- **Con trỏ báo khối tương tác được** — user yêu cầu thêm chỉ dẫn UI kiểu game farming tham khảo (mũi tên vàng trỏ vào khối player đang đứng gần, ví dụ đứng lên ô đất thì trỏ vào ô đất, đứng gần nhà thì trỏ vào nhà). `GameScene.findInteractionTarget()` ưu tiên ô đất ngay dưới chân (đúng ô mà phím E sẽ tác động — dùng lại điểm `body.center.x`/`body.bottom`), nếu không đứng lên ô nào thì kiểm tra khoảng cách tới nhà (bán kính = 70% bề ngang nhà). `updateInteractionPointer()` gọi mỗi frame để hiện/ẩn + di chuyển 1 ảnh mũi tên dùng chung, thêm hiệu ứng nhấp nhô nhẹ (sin theo thời gian). Mũi tên vẽ bằng code (canvas, kim cương vàng viền nâu, giống màu spinner loading đã dùng) chứ không cần asset riêng, depth cực cao (999999) để luôn nổi trên mọi prop. Nhà hiện **chưa có tương tác thật** (chưa có gì để làm khi đứng gần) nhưng vẫn báo trước theo đúng yêu cầu, dễ gắn logic thật vào sau (mở shop, vào nhà...). Đã verify bằng Puppeteer 3 kịch bản (đứng trên ô đất / giữa đồng cỏ trống / đứng gần nhà) — hiện/ẩn đúng, trỏ đúng vị trí.
- **`public/data/crops.json` — thêm đủ 19 cây theo toàn bộ bảng trong `docs/gameplay/farming.md`** (trước đó chỉ có 3 cây Tier Cơ Bản: green_onion/carrot/potato). Thêm 16 cây còn lại đủ 4 tier: Cơ Bản (`cabbage`, `pumpkin`), Trung Cấp mở Lv10 (`mushroom`, `strawberry`, `tomato`, `corn`, `watermelon`), Cao Cấp mở Lv25 (`medicinal_herb`, `green_tea`, `lotus`, `ginseng`, `red_ginseng`), Hiếm mở Lv40+ (`sunflower`, `moonlight_flower`, `spirit_energy_plant`, `ancient_seed`). Đã kiểm tra `public/assets/sprites/crops/` trước khi thêm — **đủ sẵn sprite 4 giai đoạn cho cả 19 cây**, không cần gen thêm/dùng placeholder.
  - Số liệu (giá hạt, giờ chín, độ giảm ẩm/giờ, multi-harvest + chu kỳ hái lại, sản lượng, giá bán) lấy đúng theo bảng thiết kế, không bịa. 1 vài chỗ bảng gốc không cho số cụ thể phải tự suy luận có ghi chú: hạt giống "Drop từ Boss/hang động" (Hoa ánh trăng, Cây linh khí, Hạt giống cổ đại) → `seed_cost: 0` (không mua được ở shop); `special_tool: "divine_watering_can"` cho Cây linh khí (bảng ghi "Cần Bình tưới thần") — chưa có id vật phẩm thật nào định nghĩa tool này, đặt tạm slug tiếng Anh cho nhất quán, cần khớp lại khi có hệ thống tool thật; Hạt giống cổ đại cho ra "Random item" nên `sell_price: 0` (giá tuỳ item ra, không phải giá cố định của bản thân hạt).
  - **Cố tình KHÔNG thêm "Tinh hoa thiên nhiên"** dù có đủ sprite trong thư mục — theo đúng mô tả farming.md đây là nguyên liệu CRAFT (3 Nhân sâm + 3 Sâm đỏ + 5 Linh khí → 1 Tinh hoa), "Không trồng" được, không có `seed_cost`/`growth_hours` nên không khớp schema `Crop` — thuộc về `items.json` (chưa thêm, ngoài phạm vi yêu cầu này).
    - **⤷ ĐẢO NGƯỢC quyết định này sau (user yêu cầu)**: đã thêm entry `natures_essence` thật vào `crops.json` (tier `rare`, `growth_hours: 60` — dài nhất trong toàn bộ 20 cây, `unlock_level: 60`, `seed_cost: 0`/`sell_price: 20000`, không multi-harvest) và vào `GameScene.PLANTABLE_CROP_IDS` + `PreloadScene.PLANTABLE_CROPS` — giờ trồng được như mọi cây khác. **Không xoá công thức craft cũ** (3 Nhân sâm + 3 Sâm đỏ + 5 Linh khí, vẫn dùng cho quest MQ + crafting Mythic gear ở `crafting.md`/`story.md`/`quests.md`) — trồng trực tiếp chỉ là 1 NGUỒN THAY THẾ, không thay thế cách craft. Cập nhật lại dòng tương ứng trong `farming.md`/`economy.md` cho khớp 2 nguồn. Đã verify: `crops.json` parse hợp lệ đúng 20 entry, `tsc --noEmit` sạch.
  - **`GameScene.PLANTABLE_CROP_IDS` giờ đã mở rộng đủ 19 cây** (xem mục "Mở rộng menu chọn hạt... carousel" bên dưới) — vẫn **chưa gate theo `unlock_level`/trừ tiền hạt giống thật**, cho chọn tự do mọi cây vì Inventory/tiền tệ thuộc Sprint 4, chưa tồn tại.
  - Đã verify: `node -e "require('./public/data/crops.json')"` parse JSON hợp lệ, đúng 19 entry; chạy game thật qua `scripts/dev-check.cjs` xác nhận `PreloadScene` load đủ 19 cây vào cache, không lỗi console mới.
- **⚠️ Đổi lại giai đoạn hiển thị cây chín thành `harvest` riêng biệt khỏi item icon** (user yêu cầu — trước đó 1 phiên làm việc khác đã gộp chung "Ready/Item Icon" thành 1 file `<id>.png` vì tưởng code chỉ có 4 stage). `FarmManager.CropVisualStage` đổi `'ready'` → `'harvest'`, `getVisualStage()` trả `'harvest'` khi `tile.state === 'ready'`; `PreloadScene.ts` đổi `CROP_STAGE_SUFFIX.ready: ''` → `CROP_STAGE_SUFFIX.harvest: '_harvest'` (load file `<id>_harvest.png` riêng, không dùng chung `<id>.png` nữa). `art-refs/items/crops.md` + `docs/data/asset-manifest.md` đã cập nhật thêm block/file Harvest cho cả 20 cây.
  - **Đã xong**: xem mục "Cắt lại toàn bộ sprite cây trồng theo ảnh tham khảo mới" bên dưới — đủ 20 file `<id>_harvest.png` (19 cây `crops.json` + `natures_essence` dự phòng), không còn lỗi texture.
- **Action thu hoạch khi Enter lên ô `ready`** — `interactWithFarmTile()` thêm nhánh thứ 3 (trước chỉ xử lý `empty`/`tilled`): gọi `FarmManager.harvest(tile)` (mới, reset `state→empty`/`cropId→null`/`plantedAt→null`, trả về `cropId` vừa hái), rồi `playHarvestFx()` hiện icon `<id>.png` (texture `crop_<id>_item`, preload thêm ở `PreloadScene.ts` — trước đó cố tình chưa load vì nghĩ để dành Inventory Sprint 4, giờ cần ngay cho hiệu ứng) tại đúng vị trí ô, bay lên 40px + mờ dần trong 600ms rồi tự `destroy()`. Ô đất tự động về lại `empty` (chưa cuốc) nhờ `syncFarmVisuals()` đọc state mới, không cần code thêm. **Chỉ là phản hồi hình ảnh** — chưa cộng gì vào inventory thật (chưa tồn tại).
  - **Bug phát hiện lúc verify bằng Puppeteer**: icon bay lên tồn tại đúng vị trí/kích thước/alpha (kiểm tra qua `scene.children.list`) nhưng **không thấy gì trong screenshot** — nguyên nhân: con trỏ tương tác (`interactionPointer`, hình thoi vàng 22×26, depth 999999) vẫn hiện đúng ngay tại ô vừa hái (còn trong bán kính tương tác, giờ có thể cuốc lại), depth cao hơn icon (999998 lúc đầu) nên che kín hoàn toàn icon nhỏ bên dưới suốt lúc bay lên. **Đã sửa**: đổi `HARVEST_FX_DEPTH = INTERACTION_POINTER_DEPTH + 0.5` (nổi trên con trỏ, dưới menu hạt giống). Verify lại bằng screenshot zoom đúng bounding box của icon (`object.getBounds()` quy đổi qua camera scroll/zoom) — xác nhận thấy rõ hình carrot orange/xanh, không còn bị hình thoi che.
- **Cắt lại toàn bộ sprite cây trồng theo ảnh tham khảo mới** (user thêm 4 ảnh vào `public/images/Crops/data/`: `basic 1.png`, `intermediate 1.png`, `bg-tran-high.png`, `bg-tran.png` — mỗi ảnh là 1 bảng nhiều hàng × 5 cột `Hạt giống/Mầm non/Đang lớn/Thu hoạch/Vật phẩm`, khớp đúng 4 tier trong `farming.md`). Yêu cầu: cắt theo cây, thay vào sprite cũ, chuẩn hoá về 200×200 để dễ tăng giảm kích thước sau này.
  - **Không có công cụ cắt ảnh sẵn trong dự án** — dùng tạm Python + Pillow (đã có sẵn trên máy, không cần cài thêm) viết script 1 lần, không lưu lại repo (giống tinh thần các lần dùng `sharp`/`pngjs` tạm trước đây).
  - **Xác định toạ độ từng ô bằng cách dò biên alpha (`getbbox()`) trong 1 cửa sổ x/y ước lượng trước bằng mắt** (chụp ảnh kèm lưới toạ độ đè lên để đọc chính xác) — không thể dò tự động hoàn toàn bằng 1 công thức chung cho cả 4 ảnh vì: (1) `basic 1.png`/`intermediate 1.png` có nhãn tên cây bằng chữ đè lên **cùng vùng Y** với ảnh cột 1 (hạt giống) của 1 số hàng, làm alpha-profile theo cột không tách được chữ khỏi ảnh; (2) mỗi hàng có toạ độ cột hơi lệch nhau (không phải lưới cố định tuyệt đối) do kích thước cây/hình khác nhau; (3) hàng `Dưa hấu` không có ảnh riêng cho giai đoạn "hạt giống" (bỏ trống) — đã xác nhận bằng cách phóng to vùng đó, không phải bug cắt ảnh, source thật sự thiếu.
  - **Ánh xạ 5 cột nguồn → 4 stage code cần** (`seed/sprout/growing/harvest` + `<id>.png` item icon riêng): cột 1→`_seed`, cột 2→`_sprout`, cột 3 (mới lớn, chưa ra quả/củ)→`_growing`, cột 4 (đã ra quả/củ, còn dính trên đất)→`_harvest`, cột 5 (vật phẩm cô lập, không có đất)→`<id>.png`. Ban đầu chọn nhầm cột 4 làm `_growing` và bỏ qua cột 4 hoàn toàn (tưởng game chỉ có 4 stage `seed/sprout/growing/ready` như trước) — phát hiện sai khi chạy thử game thấy lỗi console `Failed to process file... crop_<id>_harvest`, đọc lại `FarmManager.ts`/`PreloadScene.ts` mới biết 1 phiên làm việc khác (commit `7595e6d`, sau lần `/save` gần nhất) đã tách hẳn stage `harvest` (cây chín còn trên đất) ra khỏi `<id>.png` (item icon cô lập, dùng cho hiệu ứng bay lên khi hái) — đúng lúc đang thiếu file `_harvest.png` mà cảnh báo ở đầu file này nhắc tới. Sửa lại đúng: dùng cả cột 3 VÀ cột 4 (không bỏ cột nào), thêm hẳn ảnh `_harvest` mới cho 20 cây.
  - **Bug phát hiện + sửa kèm theo**: `updateSeedMenuSelection()`/`createSeedMenu()` trong `GameScene.ts` **vẫn còn gọi texture `crop_<id>_ready`** — key này không còn được `PreloadScene.ts` load từ sau commit tách stage `harvest` (đổi hẳn sang `_harvest`/`_item`), nghĩa là **menu chọn hạt bị lỗi ngầm từ commit đó tới giờ** (mọi icon trong menu đều ra texture "missing" mặc định của Phaser) dù không có warning nào lúc `/save` trước — bug này nằm ngoài phạm vi yêu cầu gốc nhưng phát hiện được trong lúc verify nên sửa luôn: đổi 2 chỗ gọi sang `this.cropItemTextureKey(id)` (trả về `crop_<id>_item`, đúng texture đã preload).
  - **Bug nhỏ khác**: 2 hàng đầu tiên của `bg-tran-high.png`/`bg-tran.png` (Trà xanh, Hoa ánh trăng) nằm sát ngay dưới hàng tiêu đề cột ("HẠT GIỐNG (SEED)"...) — cửa sổ cắt ban đầu ăn luôn 1 phần badge chữ tiêu đề vào góc trên ảnh hạt giống. Sửa bằng cách nâng mép trên cửa sổ cắt riêng cho 2 hàng này.
  - **Cây/stage KHÔNG được thay** (giữ nguyên asset cũ, có ghi chú lý do): `carrot` (không có trong ảnh mới, chỉ ghi "for reference"; vẫn còn tỉ lệ khung hình cũ, chưa chuẩn 200×200) — nhưng `carrot_harvest.png` **thiếu hẳn** (asset cũ chưa từng có file này) nên tạm copy y hệt `carrot_growing.png` làm placeholder, cần thay bằng ảnh thật sau. `watermelon_seed.png` — nguồn mới không có ảnh giai đoạn hạt giống cho dưa hấu, giữ nguyên ảnh phong cách pixel-art cũ (khác hẳn phong cách vẽ tay của bộ mới) chỉ resize khung 200×200 cho đồng bộ kích thước, chưa đồng bộ được phong cách — cần ảnh hạt giống mới cùng bộ khi có.
  - **`natures_essence`** (Tinh hoa thiên nhiên) cũng có đủ 5 cột trong `bg-tran.png` nên đã cắt đủ 4 file — lúc này còn craft-only nên chưa đụng `crops.json`/`PLANTABLE_CROPS` (chỉ cắt sẵn để dùng sau nếu làm UI craft). **Quyết định này đã bị đảo ngược ngay sau đó** (xem mục "⤷ ĐẢO NGƯỢC quyết định này sau" ở trên) — giờ trồng được như mọi cây khác, 4 file sprite vừa cắt đây chính là asset đang dùng cho việc đó.
  - Đã verify bằng `scripts/dev-check.cjs`: dựng contact-sheet (ghép toàn bộ 76 file mới thành 1 ảnh lưới để soi bằng mắt 1 lần thay vì mở từng file) xác nhận không còn ảnh lỗi/rỗng/lem chữ; chạy game thật (cuốc → mở menu chọn hạt → cuộn → trồng cà chua → tua nhanh 40 giờ bằng phím G) xác nhận: icon menu hiện đúng ảnh vật phẩm cô lập (không còn placeholder), cây lớn dần đúng thứ tự trên ô đất, tới giai đoạn chín hiện đúng ảnh `_harvest` (cà chua đỏ mọng còn dính trên đất). Không còn lỗi console liên quan crop texture (chỉ còn 404 `favicon.ico` không liên quan).

## Sprint 3 — Game Clock (chu kỳ ngày/đêm) ✅

- **[src/systems/TimeManager.ts](../../src/systems/TimeManager.ts)** (mới) — đồng hồ trong game, **tách biệt hoàn toàn** khỏi giờ THỰC dùng để lớn cây ở `FarmManager` (2 hệ thời gian độc lập, cố tình không liên quan tới nhau — đừng nhầm lẫn khi đọc lại code sau này). Tỉ lệ chọn: **1 giờ game = 60 giây thực** (1 ngày = 24 phút thực) — chọn số tròn để 1 giây thực = 1 phút game, dễ verify khi test (chờ N giây thực thì kim đồng hồ phải nhảy đúng N phút). Bắt đầu lúc 6:00 sáng ngày 1. Extend `Phaser.Events.EventEmitter` để phát `hourTick`/`dayChange`/`nightStart`/`dayStart` — chưa có ai subscribe (đúng scope Sprint 3, các hệ thống ăn theo giờ/đêm là việc của sprint sau), nhưng event bus đã sẵn sàng.
  - `getNightOverlayAlpha()` nội suy tuyến tính trong khung giờ hoàng hôn (19h→21h) và bình minh (4h→6h) thay vì bật/tắt đột ngột — đúng yêu cầu "Overlay tint màu tối dần" trong `dev-schedule.md`.
  - `debugAdvanceHours(hours)` — nhảy nhanh N giờ giả lập, gọi qua phím **T** (DEBUG) ở `GameScene`, tương tự phím G đã có cho farm — dùng để test chu kỳ ngày/đêm không phải chờ thật.
- **`GameScene.ts`**: `nightOverlay` là 1 `Rectangle` phủ kín camera (`setScrollFactor(0)`, không phải world object nên không cần theo dõi vị trí camera), alpha cập nhật mỗi frame theo `timeManager.getNightOverlayAlpha()`. `updateTimeHud()` ghi `"Ngày X — HH:MM"` vào registry cho `UIScene` đọc (cùng pattern với `selectedSeedName` ở Sprint 2).
- **`UIScene.ts`**: thêm dòng ngày/giờ ở góc trên-phải (tách khỏi cụm HP/MP/EXP/hạt giống bên trái).
- **Bug phát hiện khi verify bằng Puppeteer**: bấm nhanh nhiều phím liên tiếp không có khoảng nghỉ (`page.keyboard.press()` gọi dồn dập trong vòng lặp) khiến 1 lần bấm bắn ra tới 3-4 lần sự kiện `keydown` (không phải do code sai — test lại với khoảng nghỉ hợp lý giữa các lần bấm thì đúng 1-1 hoàn toàn chính xác, đây là đặc thù môi trường test tự động chứ người chơi bấm phím tay thật không gặp phải). Dù vậy vẫn **thêm `event.repeat` guard cho mọi phím tắt dạng "1 lần bấm = 1 hành động"** (Q, E, 1/2/3, G, T) trong `GameScene.ts` làm phòng vệ đúng đắn cho trường hợp OS/trình duyệt thật sự auto-repeat khi giữ phím hơi lâu — trước đó các phím này chưa có guard này.
- Đã verify bằng Puppeteer (phím T với khoảng nghỉ giữa các lần bấm): HUD hiện đúng "Ngày X — HH:MM", tăng giờ chính xác 1-1 theo số lần bấm, overlay tối dần đúng lúc 19h-21h (thấy rõ ảnh chụp tối hơn ở 21h), sáng lại đúng lúc bình minh, chuyển sang Ngày 2 đúng khi qua nửa đêm. Không lỗi console mới.
- **Đổi hẳn ảnh nền sang bản đêm lúc tối hẳn** — user tự vẽ thêm `public/images/BaseMap/Farm/base-map-night.png` (cùng bố cục/kích thước 1672×941 với bản ngày — đã verify trước khi dùng, không đoán chừng). Copy vào `public/assets/tilesets/farm/BaseMap_night.png`, load texture `farm_background_night` ở `PreloadScene.ts`.
  - `TimeManager.getNightOverlayAlpha()` tách thành `getNightFraction()` (0-1, nguồn chung) + `getNightOverlayAlpha()` (quy đổi sang alpha overlay) — để `GameScene` dùng chung 1 mốc % cho cả 2 hiệu ứng (crossfade nền + tối prop), tránh lệch nhịp chuyển cảnh nếu mỗi chỗ tự tính riêng.
  - `GameScene.ts`: thêm ảnh `backgroundNight` chồng ngay trên ảnh nền ngày (cùng gốc toạ độ, `depth` cao hơn 1), alpha = `getNightFraction()` mỗi frame — **crossfade dần** giữa 2 ảnh theo đúng khung giờ hoàng hôn/bình minh đã có sẵn từ overlay, không bật/tắt đột ngột. Vì cả 2 ảnh cùng kích thước nên toạ độ va chạm/ô đất/hàng rào/nhà không bị ảnh hưởng gì.
  - Player/hàng rào/nhà/ô đất chưa có bản vẽ riêng cho ban đêm — vẫn tối đi nhờ `nightOverlay` (lớp phủ alpha đen) phủ lên trên chúng, phối hợp cùng lúc với crossfade nền.
  - Đã verify bằng Puppeteer: ảnh nền chuyển mượt từ bản ngày sang bản đêm (thấy rõ trăng/sao ở bản đêm) đúng khung giờ 19h-21h, giữ nguyên bản đêm suốt đêm, chuyển ngược lại đúng lúc bình minh. Không lỗi console mới.

## Sprint 4 — Tưới nước/độ ẩm, Thu hoạch thật, Inventory ✅

- **[src/systems/InventoryManager.ts](../../src/systems/InventoryManager.ts)** (mới) — túi đồ dạng danh sách ô (`InventorySlot { itemId, quantity }`), tách biệt khỏi rendering giống triết lý `FarmManager`. `addItem()` lấp đầy stack đang có chỗ trống (cùng itemId, chưa đầy `stack_max`) trước khi tạo stack mới — verify bằng Puppeteer: thu hoạch 2 lần cùng loại cây (mushroom multi-harvest) ra đúng **1 slot cộng dồn** (`quantity: 2`), không tạo 2 slot riêng. `getStackMax()` tra `items.json` nếu có entry khớp id, mặc định `99` cho nông sản (crops.json không có field `stack_max` riêng, chưa cần thêm entry `Item` cho từng loại nông sản ở sprint này). Chưa giới hạn tổng số ô (chưa có lý do gameplay thật đòi hỏi).
- **`src/systems/FarmManager.ts`** — mở rộng `FarmTileRuntime` thêm `lastWateredAt`, `harvestCountRemaining`, `cycleHours`, `isRegrowCycle`:
  - **Độ ẩm tính LẠI từ `lastWateredAt` mỗi lần gọi `getMoisture()`** (không lưu số % trực tiếp) — cùng triết lý với `getVisualStage()` tính lại từ `plantedAt`, tránh lệch/trôi số. Công thức đúng `docs/gameplay/farming.md`: `100 - crop.moisture_decay_per_hour × giờ_đã_trôi`, kẹp trong [50, 100] (không bao giờ xuống dưới 50%). Trồng hoặc tưới (`water()`) đều set `lastWateredAt = now` → về 100% ngay.
  - **Cố tình KHÔNG dùng `TimeManager.onHourTick`** dù `dev-schedule.md` ghi vậy — mâu thuẫn với chính thiết kế đã chốt trước đó (comment sẵn có trong `FarmManager.ts`: thời gian lớn cây dùng GIỜ THỰC, tách biệt hoàn toàn khỏi đồng hồ gia tốc của `TimeManager`), và `farming.md` cũng ghi rõ "mọi thời gian tính bằng giờ thực". Độ ẩm cùng đơn vị giờ với lớn cây nên phải tính theo giờ thực giống hệt, không thể ăn theo đồng hồ gia tốc — tính lại theo `lastWateredAt` mỗi frame nhất quán hơn hẳn so với đăng ký event `hourTick` (tránh cộng dồn/lệch nếu miss tick).
  - **`harvest()` đổi return type** thành `{ cropId, quantity } | null` (trước chỉ trả `cropId`) — sản lượng = random `[yield_min, yield_max]` × (độ ẩm lúc hái / 100), tối thiểu 1, khớp đúng ví dụ farming.md ("Cà rốt gốc 4 củ, Độ ẩm 70% → 2-3 củ"). Verify: carrot fast-forward đủ 4h không tưới lại → moisture về đúng 50 (sàn), harvest ra `quantity: 2` (đúng dải kỳ vọng `round(3or4 × 0.5)`).
  - **Xử lý `multi_harvest`/`regrow_hours`**: hái xong còn lượt (`harvestCountRemaining > 1`) → tile quay lại `planted` với `cycleHours = crop.regrow_hours` (không phải `growth_hours`) và `isRegrowCycle = true` — `getVisualStage()` bỏ qua seed/sprout khi `isRegrowCycle`, chỉ hiện `growing` liên tục tới khi chín lại (cây hái nhiều lần không "nảy mầm lại", chỉ cần đủ 4 sprite sẵn có). Verify bằng Puppeteer với mushroom (`harvest_count: 3, regrow_hours: 3`): hái lần 1 → `harvestCountRemaining: 2, isRegrowCycle: true, cycleHours: 3`; fast-forward 3h → `ready` lại đúng; hái lần 2 → `harvestCountRemaining: 1`, tile vẫn `planted` (chưa hết lượt). Hết lượt hoặc cây thường (`multi_harvest: false`) thì tile về hẳn `empty` như logic cũ.
  - `debugFastForward()` (phím G) giờ đẩy lùi CẢ `plantedAt` VÀ `lastWateredAt` — mô phỏng đúng "N giờ trôi qua" ảnh hưởng cả lớn cây và giảm độ ẩm cùng lúc, không chỉ riêng lớn cây như bản cũ.
- **`GameScene.ts`**:
  - `interactWithFarmTile()` thêm nhánh `tile.state === 'planted'` → gọi `farmManager.water(tile)`, phát hiệu ứng vài giọt nước rơi (`playWaterFx()`, texture vẽ bằng code tạm — chưa có icon Bình tưới thật). Nhánh `ready` (thu hoạch) giờ gọi `inventoryManager.addItem()` rồi mới phát fx, fx thêm chữ `+N` bay lên cùng icon (trước chỉ có icon, không có số lượng vì chưa có gì để cộng).
  - **Overlay đất ẩm** (`syncFarmVisuals()`) — ảnh phủ mờ xanh-nâu lên ô đang có cây sống, alpha tỉ lệ thẳng với % moisture (50%→trong suốt, 100%→rõ nhất), texture vẽ bằng code tạm (`createMoistureOverlayTexture()`) — chưa có ảnh thật đổi màu/độ bóng theo `art-refs/ui/ui.md` (chưa có prompt riêng cho cái này, cần bổ sung).
  - **UI Túi đồ** — lưới cố định 6×5 = 30 ô (`createInventoryUI()`), mở/đóng bằng phím **I**, đứng yên player khi mở (giống menu hạt giống, không mở được đồng thời với menu hạt giống để tránh 2 bảng đè nhau). Icon dùng thẳng item icon (`crop_<id>_item`) đã preload, khung ô vẽ bằng code tạm (chưa có `ui/frames/item_slot` thật). Chưa có drag/click-để-dùng-item — bỏ qua có chủ đích vì chưa có hệ thống "dùng vật phẩm" nào tồn tại để nối vào (nông sản hiện chỉ để bán/craft, chưa có consumable nào dùng được), tương tự lý do seed menu vẫn "miễn phí" ở Sprint 2.
    - **Bug phát hiện lúc verify bằng Puppeteer**: icon trong túi đồ hiện to lộn xộn, 1 icon carrot to gần hết cả màn hình — **cùng loại bug đã gặp và ghi lại ở Sprint 2** (`setTexture()` không tự giữ `setDisplaySize()` cũ, mỗi cây có kích thước ảnh gốc khác nhau). Đã sửa: `refreshInventoryUI()` gọi lại `.setDisplaySize()` ngay sau `.setTexture()`, đúng bài học đã ghi "phải tự set lại size sau khi đổi texture — Phaser không tự làm hộ".
  - **Sửa luôn 1 bug tồn đọng từ trước** (phát hiện khi đọc lại code, không phải do thay đổi lần này): menu chọn hạt giống (`updateSeedMenuSelection()`, `createSeedMenu()`) vẫn dùng texture key cũ `crop_<id>_ready` — key này đã bị xoá từ lúc tách giai đoạn Harvest khỏi Item Icon (xem mục Sprint 2 bên dưới), khiến icon menu hạt giống thực ra đang hiện `__MISSING` từ lúc đó. Đổi qua đúng `cropItemTextureKey()` (`crop_<id>_item`).
- **`UIScene.ts`**: cập nhật hint text thành `"Hạt giống: X (Enter: cuốc/trồng/tưới/hái · ←/→ chọn hạt · I: túi đồ)"`.
- **Thanh trạng thái cây treo phía trên ô** (`GameScene.syncGrowthStatusUI()`, user yêu cầu thêm sau khi xong phần trên, có gửi ảnh tham khảo 1 game farming khác) — chỉ hiện khi ô đang `planted` (cây sống, chưa chín), tự ẩn khi chín/đã hái/chưa trồng. 2 phần, đúng yêu cầu "1 thanh sản lượng, bên trên là thời gian chín":
  - **Thanh dưới** = % moisture hiện tại (`FarmManager.getMoisture()`) quy thẳng sang tỉ lệ đầy — đúng "sản lượng giảm nếu không tưới" vì sản lượng cuối cùng khi hái tỉ lệ thẳng với đúng số này (xem `harvest()` ở trên). Màu nội suy đỏ-cam (sàn 50%) → xanh lá (đầy 100%), vẽ bằng `Rectangle` đổi `width`/`setFillStyle()` mỗi frame (không cần asset).
  - **Chữ trên** = đếm ngược thời gian thực còn lại tới lúc chín (`plantedAt + cycleHours` − hiện tại), định dạng gọn `Xh Ym` / `Ym Zs` / `Zs` tuỳ còn nhiều hay ít.
  - Tạo/xoá theo tile giống `moistureOverlays` (Map theo `FarmTilePlacement.id`, tái dùng object mỗi frame, không tạo mới liên tục).
  - Đã verify bằng Puppeteer: kiểm cả giá trị số (`barFillWidth`, `barFillColor` khớp đúng công thức nội suy màu, `timerText`) lẫn screenshot zoom cận cảnh — lúc đầu chụp bị player đứng che khuất hoàn toàn (đứng ngay trên ô để tương tác), di chuyển player ra xa rồi chụp lại thấy rõ cả chữ đếm giờ và thanh màu đúng vị trí phía trên cây.
  - **Cập nhật sau (user phản hồi): chỉ hiện thanh ở ô đang TRỎ VÀO, không hiện tràn lan ở mọi ô đang trồng cùng lúc** — bản đầu hiện thanh cho MỌI ô `planted` cùng lúc (đúng yêu cầu ban đầu "hiện khi đang lớn"), nhưng nhiều ô trồng cùng lúc thì rối cả màn hình toàn số đếm giờ (đúng như ảnh user gửi). `syncFarmVisuals()` tính `pointedTileId` 1 lần mỗi frame (dùng lại đúng bán kính/toạ độ `findNearestTile()` mà `findInteractionTarget()`/Enter đã dùng, để "thấy thanh hiện" luôn khớp đúng ô "Enter tương tác được"), `syncGrowthStatusUI()` thêm điều kiện `isPointedAt` — chỉ tạo/hiện khi ĐÚNG ô đang đứng gần, huỷ hẳn khi đứng ra xa (không giữ ẩn, để không tồn hàng chục object cùng lúc). Đã verify bằng cách gọi trực tiếp `syncFarmVisuals()` (bỏ qua hệ va chạm/update loop để tránh nhiễu từ `resolvePolygonCollision` khi teleport player bằng script test): đứng xa mọi ô trồng → 0 thanh hiện; đặt đúng lên 1 ô (bù offset body/feet) → đúng 1 thanh hiện, đúng id ô đó; đứng ra xa lại → tự về 0.
  - **Cập nhật thêm ngay sau đó (user phản hồi kèm ảnh: "text thời gian ấy phải hiện trước người chứ")**: `GROWTH_STATUS_DEPTH` cũ (`CROP_DEPTH + 0.1`, cùng tier với đất/cây) THẤP HƠN player (`depth = player.y`, luôn dương và lớn hơn nhiều) — sprite player che khuất gần hết thanh dù chỉ hiện đúng 1 ô. Đổi `GROWTH_STATUS_DEPTH = INTERACTION_POINTER_DEPTH + 0.2` (cùng tier con trỏ tương tác, nổi trên MỌI thứ trong world kể cả player). Riêng đổi depth CHƯA đủ — vị trí `barY`/`textY` cũ (`tile.y - tile.height/2 - 9`) chỉ cách tile ~21px, rơi ngay giữa THÂN/VAI player (cao ~67px từ chân), nằm THẤP HƠN đỉnh đầu nên dù nổi trên vẫn bị dính ngay trong vùng thân người, gần như không nhận ra là "hiện trước". Đổi tiếp `barY = tile.y - 74` (dựa theo `displayHeight` thật của player, đo qua Puppeteer: đỉnh đầu ≈ chân - 64px, cộng thêm khoảng hở) — hiện rõ phía trên đỉnh đầu, NHƯNG user gửi ảnh phản hồi tiếp: quá xa, trôi lên gần sát mép trên màn hình, không liên hệ gì tới con trỏ/player nữa.
    - **Bài học test phát hiện lúc này**: teleport player bằng `setPosition`+`body.reset` thẳng vào ĐÚNG tâm 1 ô đất (để bù offset body/feet, xem cách làm quen thuộc ở các test trước) vô tình đặt `feetY` trùng đúng 1 điểm bị `resolvePolygonCollision` coi là "đang nằm trong polygon va chạm" (dù ô đó vẫn đi lại/tương tác bình thường trong gameplay thật — chỉ có FEET đứng CHÍNH XÁC tại tâm ô mới trúng, không xảy ra khi đi bộ tự nhiên) → bị đẩy bật ra ~30px ngay trong 1-2 khung hình kế tiếp, khiến bar bị huỷ TRƯỚC KHI kịp render ra màn hình chụp. Sửa cách test từ đây về sau: KHÔNG teleport vào tâm ô, dùng thẳng vị trí spawn mặc định (890,430, đã biết ổn định không dính va chạm) + tìm/trồng đúng ô GẦN NHẤT từ vị trí đó.
  - **Cập nhật lần 3 (user chỉnh lại ý chính xác hơn): "nằm trên ngay trên con trỏ ấy"** — không phải "cao hơn đầu player" như lần sửa trước hiểu nhầm, mà phải bám SÁT theo đúng vị trí con trỏ tương tác (`interactionPointer`), không phải 1 khoảng cố định lớn tính từ đỉnh đầu. Đổi `barY`/`textY` sang tính từ chính công thức toạ độ Y con trỏ ở `updateInteractionPointer()` (`tile.y - tile.height/2 - 6`, bỏ qua phần `bounce` vì chỉ cần vị trí cơ sở) rồi lùi thêm 1 khoảng nhỏ (16-26px) — bar/text giờ nằm sát ngay phía trên con trỏ, đúng ý user. Đã verify bằng Puppeteer + screenshot zoom 3x (camera `setZoom`/`centerOn` để nhìn rõ vùng nhỏ quanh player): thấy rõ "4h3m" + thanh xanh nằm ngay sát trên đỉnh con trỏ vàng, không còn trôi lên xa nữa.
- **Ẩn ảnh đất `tilled` khi ô đang có cây** (`syncFarmVisuals()`, user yêu cầu) — mọi sprite cây (seed/sprout/growing/harvest) đã tự vẽ sẵn 1 mô đất nhỏ ở đáy riêng (đúng "Quy tắc bám đất" trong `art-refs/items/crops.md`), hiện thêm ảnh đất `tilled` full-size bên dưới nữa bị đè/chồng hình. `soilImage.setVisible(tile.cropId === null)` — chỉ hiện ảnh đất khi ô KHÔNG có cây (empty/tilled trống), ẩn hẳn khi `planted`/`ready`, hiện lại ngay khi hái xong (`cropId` về `null`). Đã verify bằng Puppeteer (đọc trực tiếp `soilImage.visible` qua 3 mốc: tilled-trống → true, đang trồng → false, sau khi hái → true) + screenshot xác nhận không còn 2 lớp đất chồng lên nhau.
- Đã verify toàn bộ luồng bằng Puppeteer (2 scenario riêng — cây thường + cây multi-harvest) + type-check (`tsc --noEmit`) + Prettier: trồng → tưới → fast-forward → chín đúng lúc → hái ra đúng số lượng theo % ẩm → vào đúng inventory, stack đúng → mở túi đồ hiện đúng icon+số lượng. Chỉ còn lỗi console đã biết từ trước (404 `<id>_harvest.png` chưa gen), không có lỗi mới.
- **Giếng nước** (user yêu cầu thêm, hoàn tất phần nông trại trước khi qua Sprint 5) — đúng `docs/gameplay/farming.md` mục "Trang trí nông trại": *"Giếng nước — Tự động tưới 3×3 ô xung quanh mỗi sáng"*.
  - **[src/data/wellPlacement.ts](../../src/data/wellPlacement.ts)** (mới) — vị trí (780, 600), khoảnh cỏ mở duy nhất còn trống sát 1 cụm ô đất (cụm dưới, 2×4 ô) — 2 cụm ô trên đã kín cả 2 bên bởi đường mòn/nhà, không còn chỗ đặt thêm công trình. Farm không phải 1 lưới liền mạch mà chia 3 cụm rời (`farmTiles.ts`) nên "3×3 xung quanh" không tính được theo lưới thật — chọn `WELL_AUTO_WATER_RADIUS = 85` (world-px) sau khi verify bằng script Node: phủ đúng hết 8 ô của cụm dưới, không lan sang 2 cụm trên (cách xa hơn 190px).
  - **`GameScene.ts`**: `placeWell()` đặt ảnh + bóng đổ (cùng công thức `addGroundShadow()` với nhà/hàng rào). Texture giếng vẽ bằng code tạm (`createWellTexture()` — vòng đá xám + mặt nước xanh + mái gỗ nhỏ) vì `art-refs/world/buildings.md` đã có prompt "Hồ Chứa Nước / Giếng Làng" nhưng chưa gen ảnh thật.
  - **Logic tự động tưới**: `TimeManager` phát event `dayStart` mỗi khi qua mốc bình minh (~6h, đêm→ngày) — đăng ký `this.timeManager.on('dayStart', () => this.autoWaterNearWell())` trong `create()`. `autoWaterNearWell()` lặp toàn bộ tile của `FarmManager`, tile nào cách tâm giếng ≤ `WELL_AUTO_WATER_RADIUS` thì gọi `farmManager.water(tile)` (hàm này tự bỏ qua tile không phải `planted`, không cần tự check state) rồi phát `playWaterFx()` để thấy rõ giếng vừa hoạt động.
  - **Lưu ý khi test bằng debug**: `TimeManager.debugAdvanceHours(N)` nhảy 1 lần bằng N giờ CHỈ so sánh state trước/sau — nếu N đủ lớn để đi hết 1 vòng đêm→ngày trong 1 lần gọi duy nhất (ví dụ gọi thẳng 24), điều kiện đêm/ngày ở đầu và cuối trùng nhau nên KHÔNG bắn `nightStart`/`dayStart` gì cả (bỏ sót cả chu kỳ ở giữa) — đây là đặc điểm sẵn có của `TimeManager`, không phải bug mới. Phải gọi lặp lại nhiều lần 1 giờ (giống cách phím **T** thật hoạt động) để đi qua đúng từng mốc chuyển ngày/đêm, verify đã làm đúng cách này (24 lần gọi `debugAdvanceHours(1)`).
  - Đã verify bằng Puppeteer: trồng 2 ô cùng lúc — 1 trong bán kính giếng (cụm dưới), 1 ngoài bán kính (cụm trên) — để độ ẩm giảm về ~75% (2h), rồi cho qua đúng 1 chu kỳ đêm→ngày (24 lần `debugAdvanceHours(1)`). Kết quả: ô trong bán kính về lại ~100% (giếng tự tưới), ô ngoài bán kính vẫn ~75% (không đổi) — đúng thiết kế. Screenshot xác nhận giếng hiện đúng vị trí, không đè lên ô đất/đường mòn/hàng rào nào. Không lỗi console mới.
- **Rà lại `docs/gameplay/farming.md` so với code — phát hiện 3 lỗ hổng logic còn thiếu, đã sửa cả 3** (user yêu cầu đọc spec trước khi qua Sprint 5). Cả 3 field liên quan (`needs_water_tile`, `night_only`, state `withered`) đã có sẵn trong `types.ts`/`crops.json` từ Sprint 0-2 nhưng chưa từng được `FarmManager` kiểm tra thật:
  - **`needs_water_tile` (Hoa Sen)** — trước đây 12 ô `water_pot` trên map bị `FarmManager` loại hẳn khỏi danh sách quản lý (constructor chỉ lọc `type === 'untilled'`). Đã sửa: `FarmManager` giờ quản lý CẢ `water_pot` (thêm field `tileType` vào `FarmTileRuntime`, đọc từ `FarmTilePlacement.type`) — ô `water_pot` bắt đầu sẵn ở state `tilled` (không cần cuốc, vì là nước sẵn). `plant()` chặn 2 chiều: `crop.needs_water_tile !== (tile.tileType === 'water_pot')` → từ chối trồng (Hoa Sen CHỈ trồng được lên `water_pot`, cây khác CHỈ trồng được lên `untilled`). `GameScene.placeFarmTiles()`/`syncFarmVisuals()` cũng cập nhật: ô `water_pot` giờ được track trong `soilImages` để ẩn/hiện theo có cây (giống ô đất thường) nhưng KHÔNG đổi texture theo state (mãi là nước, không có nấc cuốc).
  - **`night_only` (Hoa Ánh Trăng)** — `harvest()` đổi signature nhận thêm `currentHour: number` (gọi truyền `this.timeManager.getHour()` từ `GameScene`, không tự import `TimeManager` để giữ 2 hệ thời gian tách biệt như thiết kế). Cây `night_only` chỉ hái được khi `hour >= 18 || hour < 6` (đúng "sau 18:00 game time" — cố tình KHÔNG dùng `TimeManager.getIsNight()` vì mốc đó là 19-21h/4-6h, lệch 1h so với spec). Hái sai giờ → trả `null` giống ô chưa chín, tile giữ nguyên `ready`.
  - **State `withered` (héo)** — có trong `FarmTileState` từ tận spec Sprint 2 gốc nhưng chưa bao giờ được gán. farming.md: *"một số cây thu hoạch nhiều lần trước khi héo"* — sửa `harvest()`: cây `multi_harvest` hái hết lượt cuối (`harvestCountRemaining` không còn > 1) thì chuyển `withered` (giữ `cropId` để biết vẽ cây gì, xoá `plantedAt`/`lastWateredAt` vì không còn gì để tính) thay vì về `empty` ngay — cây thường (không multi-harvest) vẫn về `empty` thẳng như cũ, không héo (spec chỉ nói héo gắn với multi-harvest). Thêm `clearWithered(tile)` (về `empty` hẳn) + nhánh Enter mới trong `GameScene.interactWithFarmTile()` cho state `withered` — quyết định UX do user chọn (2 lựa chọn: Enter dọn tay / tự dọn sau X giờ) là **Enter dọn tay**, nhất quán với cách Enter xử lý theo state ở mọi ô khác. `getVisualStage()` trả `growing` cho `withered` (chưa có sprite héo riêng cho 19 cây) — `GameScene.syncFarmVisuals()` tự `setTint(0x9a9a9a)` (xám) để phân biệt với cây đang lớn thật, bỏ tint (`0xffffff`) khi không héo.
  - **Đã xử lý nốt giới hạn UX nói trên (user yêu cầu ngay sau đó)**: menu chọn hạt giống giờ tự lọc theo đúng loại ô mục tiêu — thêm `GameScene.getCompatibleCropIds(tileType)` (so `crop.needs_water_tile` với `tileType === 'water_pot'`, đúng điều kiện `plant()` đang dùng để từ chối) + field `seedMenuCropIds` (danh sách ĐANG hiện trong menu, tính lại mỗi lần `openSeedMenu()`, mặc định = đủ 19 cây trước khi có tile mục tiêu). Mọi chỗ trước đây đọc thẳng `PLANTABLE_CROP_IDS` (`moveSeedMenuSelection`, `confirmSeedMenu`, `updateSeedMenuSelection`, `getSeedMenuWindowStart`) đổi qua đọc `this.seedMenuCropIds` — logic cửa sổ trượt/ẩn ô rỗng đã có sẵn từ trước tự nhiên xử lý đúng danh sách 1 cây (Hoa Sen trên ô nước) không cần sửa thêm gì (đã có nhánh `total <= SEED_MENU_VISIBLE_SLOTS` từ Sprint 2). Giờ mở menu trên ô `water_pot` CHỈ hiện Hoa Sen, mở trên ô đất thường hiện đủ 18 cây còn lại (không hiện Hoa Sen) — không còn combo nào chọn được mà chắc chắn bị từ chối.
    - Đã verify bằng Puppeteer: gọi trực tiếp `openSeedMenu()` trên tile đất thường → `seedMenuCropIds` đúng 18 cây (không có lotus); trên tile nước → đúng `["lotus"]`; gọi `confirmSeedMenu()` qua đúng luồng UI thật (không gọi tắt `farmManager.plant()`) → tile nước có `cropId: "lotus"` sau khi xác nhận. Screenshot xác nhận panel chỉ hiện 1 icon "Hoa Sen (1/1)", các ô icon còn lại ẩn đúng.
  - Đã verify bằng Puppeteer (đọc trực tiếp state qua `page.evaluate`, không chỉ suy luận): trồng Hoa Sen lên `water_pot` → thành công; trồng Hoa Sen lên ô thường → thất bại; trồng Cà rốt lên `water_pot` → thất bại. Hái Hoa Ánh Trăng lúc `hour=6` (ngày) → thất bại, giữ `ready`; lúc `hour=20` (đêm) → thành công. Nấm (`harvest_count:3`) hái đủ 3 lần: lần 1-2 tile quay lại `planted` (regrow), lần 3 (cuối) tile chuyển `withered` giữ `cropId`; `clearWithered()` đưa về `empty` đúng. Screenshot xác nhận cây héo hiện tint xám rõ rệt so với cây xanh bình thường, và nền nước `water_pot` ẩn đúng khi có cây trồng lên. Không lỗi console mới ngoài 404 `harvest.png` đã biết.
  - **Bảng "Công Cụ Nông Trại" (bulk actions)** — user yêu cầu 4 nút cuốc/tưới/gieo/thu hoạch TẤT CẢ ô, kèm 2 ô input lọc theo khoảng ID (từ–đến, để trống = tất cả); xác nhận qua AskUserQuestion đây là tính năng thật cho người chơi (không phải debug tool). Phím **F** mở/đóng (loại trừ lẫn nhau với menu hạt giống/túi đồ, đồng bộ Esc/click-outside/freeze player giống 2 panel kia). `GameScene` thêm `createBulkActionsUI()` (Graphics bg + Text + 4 nút Rectangle/Text, KHÔNG `.setInteractive()` — xem bug hit-testing dưới) + `createRangeInput()` dựng 2 `this.add.dom(x,y,'input')` kiểu `number`. 4 hàm `tillAllInRange()/waterAllInRange()/plantAllInRange()/harvestAllInRange()` đọc range qua `getBulkActionRange()` (chuỗi rỗng → `-Infinity`/`Infinity`, tức không giới hạn) rồi lọc `farmManager.getTiles()` theo `id`; `plantAllInRange()` dùng `this.selectedCropId` đang chọn, `harvestAllInRange()` cộng thẳng vào inventory + gọi `refreshInventoryUI()`.
    - **3 bug thật gặp khi làm, cả 3 đều tự phát hiện qua verify Puppeteer (không chỉ `tsc`)**:
      1. *"No DOM Container set in game config"* — Phaser cần CẢ `dom.createContainer: true` VÀ `config.parent` mới thật sự tạo DOM container (`CreateDOMContainer.js`: `if (!config.parent || !config.domCreateContainer) return`), thiếu `parent` dù đã bật `createContainer` vẫn ném lỗi. Sửa: thêm `parent: document.body` vào `main.ts`.
      2. Click chuột thật vào nút không kích hoạt gì (status text không đổi) dù `pointerdown` cấp scene bắn đúng toạ độ — hit-testing của Phaser cho GameObject `.setInteractive()` nằm trong Container `scrollFactor(0)` bị sai một khi camera đã lệch khỏi world origin (đọc `DOMElementCSSRenderer.js` xác nhận cùng dạng lỗi ma trận camera). Sửa: bỏ hẳn `.setInteractive()`/`on('pointerdown'...)` trên 4 nút, lưu toạ độ layout vào `bulkButtonLayout`, thêm `handleBulkActionsClick()` tự tính hit-test bằng tay trong handler `pointerdown` toàn cục đã có sẵn (giống cách seed-menu/inventory đang xử lý click-outside).
      3. Ô input hiện sai vị trí trên màn hình (lệch ~50-85px so với toạ độ đặt) — `DOMElement.updateSize()` đo `clientWidth/Height` NGAY LÚC TẠO, tức trước khi các dòng `.style.width = '70px'` chạy, nên Phaser dùng nhầm kích thước mặc định của `<input>` để tính offset theo origin. Sửa: gọi lại `input.updateSize()` ngay sau khi set xong style.
    - Đã verify bằng Puppeteer: cuốc range `0-2` → đúng 3 ô (0,1,2) chuyển `tilled`, còn lại giữ nguyên; xoá range (để trống) rồi cuốc lại → toàn bộ ô `untilled` còn lại chuyển `tilled` (đúng nghĩa "để trống = tất cả"); gieo tất cả → 32/32 ô có `cropId` đúng hạt đang chọn; fast-forward rồi thu hoạch tất cả → 32/32 vào đúng inventory. Click chuột thật (`page.mouse.click`) vào đúng toạ độ nút "Cuốc Tất Cả" → status text đổi thành "Đã cuốc 32 ô" (xác nhận fix bug #2). Gõ phím thật (`page.keyboard.type('7')`) vào input → giá trị input nhận đúng `"7"`, đồng thời phím tắt game (`G` fast-forward) bị chặn lúc input đang focus (`keyboard.enabled === false`) và tự bật lại sau khi blur — xác nhận cơ chế tắt hotkey lúc gõ input hoạt động đúng.

## Sprint 5 — Combat cơ bản ✅ (user yêu cầu "làm tất đi", asset thật để user bổ sung sau)

Đúng "Done when" trong `dev-schedule.md`: chuyển màn Farm↔Bãi Tập Luyện/Đồng Cỏ, đánh Người Rơm đúng 5 hit chết + hồi sinh 15s, đánh quái thật nhận EXP/gold/drop đúng bảng, player chết mất 10% Đồng + respawn 50% HP/MP. Toàn bộ **asset còn là placeholder vẽ bằng code** — user yêu cầu cứ làm hết, cái nào chưa có frame thật thì tạo tạm, bổ sung ảnh thật sau. Đã đọc kỹ `docs/gameplay/combat.md` (17 case xử lý chiến đấu) + `docs/gameplay/mechanics.md` (mục "Người Rơm"/"Hệ thống Chuyển Màn"/"Cơ chế Kẻ Địch") trước khi code, theo đúng yêu cầu của 2 file đó.

- **Hệ thống chuyển màn generic (Exit Zone/Entry Point/fade)** — nền tảng dùng lại được cho mọi map sau (Village/Map 2-7), không riêng gì Bãi Tập Luyện:
  - **[src/data/mapTransitions.ts](../../src/data/mapTransitions.ts)** (mới) — `ExitZoneDef` (rect hình chữ nhật, đơn giản hơn polygon vì chỉ cần đặt ở rìa map phẳng, không cần bám địa hình uốn lượn như sông) + `targetScene`/`entryPoint`. Định nghĩa 2 CHIỀU độc lập (`FARM_EXIT_ZONES`/`TRAINING_GROUND_EXIT_ZONES`/`GRASSLAND_EXIT_ZONES`) — Entry Point lúc TỚI luôn lệch khỏi Exit Zone lúc ĐI (tránh tự kích hoạt lại ngay khi vừa rơi vào map mới).
  - **Toạ độ Exit Zone trên Farm đo bằng Puppeteer thật** (không đoán bằng mắt): dò qua `EditorScene` — lấy `zones[i].data.points` dựng `Phaser.Geom.Polygon` rồi test hàng loạt điểm ứng viên bằng `Polygon.Contains()` cho tới khi tìm được vùng mở đủ rộng không đè lên `FARM_COLLISION_ZONES` nào (sông/hàng rào/nhà) — đúng phương pháp đã dùng để tìm toạ độ suối/cầu trước đây.
  - **Đổi TẤT CẢ Exit Zone từ "đi ra rìa map" sang cổng dịch chuyển hình vuông 50×50 đứng giữa map** (2 lần yêu cầu liên tiếp sau khi Sprint 5 xong: lần 1 chỉ đổi riêng cổng Đồng Cỏ, lần 2 user yêu cầu đồng bộ luôn 3 cổng còn lại theo đúng kiểu đó — không để map nào còn kiểu cũ). `data/mapTransitions.ts` thêm helper `squareZone(centerX, centerY)` tính rect 50×50 từ toạ độ TÂM (dễ đọc/sửa hơn tự trừ nửa cạnh ở từng chỗ khai báo) — áp dụng cho cả 4 Exit Zone: Farm→Bãi Tập Luyện `(240,430)` (dời từ rect 140×90 rìa tây cũ), Farm→Đồng Cỏ `(620,530)` (giữ nguyên từ lần đổi trước), Bãi Tập Luyện→Farm `(350,520)` (dời từ rect cạnh dưới map cũ), Đồng Cỏ→Farm `(45,400)` (dời từ rect cạnh trái map cũ). Toạ độ mới đều verify walkable bằng đúng phương pháp trên trước khi chốt (riêng `(240,430)` verify lại vì đổi từ rect cũ đã test sẵn sang tâm mới).
  - **[src/systems/PortalVisual.ts](../../src/systems/PortalVisual.ts)** (mới, tách từ `GameScene.createGrasslandPortalTexture()`/`placeGrasslandPortal()` cũ chỉ vẽ được 1 cổng cho riêng Đồng Cỏ) — `createPortalTexture()` vẽ 1 vòng xoáy tím-xanh bằng canvas dùng CHUNG cho mọi cổng trong game (gradient tròn + viền sáng + 3 cánh xoắn ốc mờ, texture chia sẻ qua Phaser texture manager global — vẽ 1 lần, mọi scene tái dùng). `placePortalVisual(scene,x,y,size)` đặt 1 cổng tại toạ độ bất kỳ + 2 tween song song (xoay 360°/3s + phóng to-thu nhỏ nhẹ). `placePortalAtZone(scene, zone)` tiện dụng hơn — tự suy toạ độ+kích thước cổng từ chính `ExitZoneDef.rect` (không cần khai báo toạ độ cổng riêng, tránh lệch với vùng kích hoạt thật). `GameScene.create()` giờ chỉ `for (const zone of FARM_EXIT_ZONES) placePortalAtZone(this, zone)` — thêm Exit Zone mới vào mảng là tự có cổng, không cần sửa `GameScene` thêm. `TrainingGroundScene`/`GrasslandScene` gọi tương tự cho đúng 1 Exit Zone của map đó. **Placeholder, chưa có prompt art-refs nào cho "cổng dịch chuyển"** — cần thêm khi gen ảnh thật.
    - Đã verify bằng Puppeteer cả 4 cổng: đứng cạnh cổng nhưng NGOÀI vùng 50×50 → không kích hoạt; bước đúng tâm mỗi cổng → chuyển scene đúng cặp, đúng Entry Point; quay lại không bị hút ngược lại cổng vừa dùng (`isTransitioning` về `false` ngay). Ảnh chụp xác nhận cả 4 cổng hiện đúng 1 kiểu hình ảnh giống hệt nhau (vòng xoáy tím-xanh xoay tròn) ở cả Farm, Bãi Tập Luyện, và Đồng Cỏ — không còn map nào dùng kiểu "vùng vô hình ở rìa map" cũ.
  - **[src/systems/SceneTransition.ts](../../src/systems/SceneTransition.ts)** (mới) — `checkExitZones()` (thuần, không phụ thuộc Phaser scene) + `fadeOutToScene()`/`fadeInScene()` dùng `cameras.main.fadeOut/fadeIn` (400ms) + sự kiện `FADE_OUT_COMPLETE`/`FADE_IN_COMPLETE`. `fadeOutToScene()` tự `scene.stop('UIScene')` trước khi `scene.start()` map đích — vì `scene.start()` chỉ dừng CHÍNH scene gọi nó, không đụng scene khác đang `launch()` song song; map đích tự `launch('UIScene')` lại.
  - Mỗi scene (`GameScene`/`TrainingGroundScene`/`GrasslandScene`) tự có field `isTransitioning` (đúng pattern `seedMenuOpen` đã có) khoá `player.update()`/tấn công trong lúc fade — đúng yêu cầu `mechanics.md`. **Bài học**: field khởi tạo `= true` chỉ chạy đúng 1 lần lúc Phaser tạo instance scene, KHÔNG tự reset mỗi lần `create()` chạy lại (`scene.start()` tái dùng cùng 1 instance) — phải gán tay `this.isTransitioning = true` ở đầu `create()` mỗi lần, nếu không lần quay lại thứ 2 sẽ "kế thừa" nhầm giá trị `false` của lần trước, không khoá input lúc fade-in.
  - `GameScene.create(data)`/`TrainingGroundScene.create(data)`/`GrasslandScene.create(data)` đều nhận `data.spawnX/spawnY` (từ Entry Point) để đặt vị trí player — có giá trị mặc định (spawn gốc của map đó) khi không có `data` (vd load thẳng Farm lúc boot game lần đầu).
  - Đã verify bằng Puppeteer (teleport player bằng `setPosition`, không `body.reset()` — bài học cũ) vào đúng 2 Exit Zone: chuyển scene đúng cặp, `isTransitioning` về `false` sau khi fade-in xong, camera/UIScene tự khởi động lại đúng ở map mới.
- **Chỉ số chiến đấu người chơi — singleton dùng CHUNG mọi scene** (khác `FarmManager`/`InventoryManager` cũ vốn per-GameScene): mỗi scene tạo `Player` MỚI khi chuyển map, nhưng HP/MP/EXP/Level/Gold phải giữ nguyên xuyên suốt.
  - **`src/data/types.ts`**: thêm `PlayerStats` (level/exp/exp_to_next/hp/max_hp/mp/max_mp/atk/def/gold/weapon_id) + field `player_stats` trong `SaveState`. `SaveManager.createDefaultSaveState()` set giá trị khởi điểm (HP 500, MP 200, ATK 10, DEF 5, weapon mặc định `iron_sword`) — **chưa wiring save/load thật** (Sprint 6 "chốt Alpha" mới làm UI/trigger save-load), chỉ chuẩn bị sẵn type + giá trị mặc định.
  - **[src/systems/CombatSystem.ts](../../src/systems/CombatSystem.ts)** (mới) — `computeDamage(atk, damageMultiplier, def, critChance)` thuần, đúng công thức case 1 combat.md (`ATK × multiplier × 100/(100+DEF)`, Crit ×1.5 roll riêng, dmg tối thiểu luôn ≥1). Nguồn DUY NHẤT tính damage — mọi nơi khác (Monster nhận contact damage từ chính nó, scene tính damage đánh vào Monster) đều gọi qua đây, không tự tính lặp lại.
  - **[src/systems/CombatManager.ts](../../src/systems/CombatManager.ts)** (mới) — extend `Phaser.Events.EventEmitter`, export sẵn 1 instance singleton `combatManager` (khởi tạo từ `createDefaultSaveState().player_stats`). `getTotalAtk()`/`getWeaponMultiplier()` cộng thêm bonus vũ khí đang cầm (tra `weapons.json` theo `weapon_id`). `takeDamage()`/`heal()`/`gainExp()` (tự xử lý lên nhiều cấp cùng lúc nếu dư EXP, tăng trưởng chỉ số tạm mỗi cấp — **chưa có UI phân bổ 10 điểm thuộc tính tự do thật** theo `mechanics.md`, đó là việc sprint UI polish sau, ngoài "Done when" Sprint 5) /`respawnAtVillage()` (case 13: trừ 10% Đồng, hồi 50% HP/MP — **chưa dựng vật phẩm Đồng rơi ra đất nhặt lại được**, chỉ trừ thẳng, ghi rõ để không tưởng nhầm đã đủ case). Emit `stats-changed`/`player-died`/`level-up` cho scene nghe.
  - **`InventoryManager` đổi thành singleton dùng chung** (`export const inventoryManager`) — trước đó mỗi `GameScene.create()` tự `new InventoryManager()`, nghĩa là túi đồ sẽ RỖNG LẠI mỗi khi quay lại Farm nếu không sửa. Quái ở `GrasslandScene` rớt đồ giờ cộng đúng vào CÙNG 1 túi đang dùng ở Farm. `GameScene.ts` đổi mọi `this.inventoryManager.xxx` sang gọi thẳng `inventoryManager.xxx` (import), bỏ hẳn field private.
  - **[src/systems/CombatHud.ts](../../src/systems/CombatHud.ts)** (mới) — `syncCombatHudToRegistry(scene)` ghi `playerHp/playerMaxHp/playerMp/playerMaxMp/playerLevel/playerExp/playerExpToNext/playerGold` vào `registry` (đúng pattern `selectedSeedName`/`gameTimeText` đã có), tự đăng ký nghe `combatManager.on('stats-changed', ...)` + tự gỡ khi scene `SHUTDOWN` — bài học cũ (listener treo trên object toàn cục sống lâu hơn scene) áp dụng y hệt ở đây vì `combatManager` là singleton sống xuyên suốt, không phải global registry.events lần này nhưng cùng nguyên tắc. Cả 3 scene (`GameScene`/`TrainingGroundScene`/`GrasslandScene`) đều gọi hàm này 1 lần trong `create()`.
  - **`UIScene.ts`**: 3 dòng HP/MP/Lv-EXP đổi từ text tĩnh "500/500" sang đọc thật từ `registry`, tự cập nhật qua `changedata-playerXxx` — bỏ hẳn số giả đã có từ Sprint 0.
  - Đã verify bằng Puppeteer: đánh quái thật → HUD tăng đúng EXP/Gold theo `monsters.json`; nhận damage → HP giảm đúng; chết → HP về 50%, Gold trừ đúng 10% (làm tròn), scene chuyển về Farm.
- **`TrainingDummy` (Người Rơm) — cơ chế HOÀN TOÀN KHÁC `Monster`** (đúng mechanics.md, không dùng `monsters.json`/công thức damage):
  - **[src/data/trainingDummyPlacement.ts](../../src/data/trainingDummyPlacement.ts)** (mới) — 5 vị trí cố định, đúng pattern `wellPlacement.ts` (KHÔNG cho vào `monsters.json` vì không phải kẻ địch thật).
  - **[src/entities/TrainingDummy.ts](../../src/entities/TrainingDummy.ts)** (mới) — static body, đứng yên tuyệt đối. `takeHit()` chỉ đếm (không nhận số damage nào) — đủ 5 lần thì `die()` (ẩn + tắt body + hiệu ứng rơm vỡ bằng 10 `tween` bay ra ngẫu nhiên, không cần `ParticleEmitter` thật), `delayedCall(15000)` hồi sinh đúng vị trí ban đầu, reset đếm về 0. Text `"x/5"` treo trên đầu, ẩn lúc chết.
  - Đã verify bằng Puppeteer: đánh đủ 5 hit → `deadState:true, visible:false`; đợi >15s thực → tự hồi sinh đúng `hitCount:0, visible:true`, đúng vị trí spawn ban đầu.
- **`Monster` (quái thật, khác hẳn Người Rơm)**:
  - **[src/entities/Monster.ts](../../src/entities/Monster.ts)** (mới) — đọc HP/ATK/DEF thật từ `monsters.json` (id `wild_rabbit`, Map 2 — Đồng Cỏ). AI đơn giản: patrol ngẫu nhiên quanh điểm spawn trong bán kính nhỏ khi chưa thấy player, đuổi theo (`chase()`) khi trong `DETECT_RADIUS` (~220px, quy đổi tạm cho "5-8 ô" vì chưa có lưới tile thật ở map chiến đấu), buông đuổi khi ra khỏi `LOSE_AGGRO_RADIUS` (~380px, "~12 ô"). Có thanh HP nhỏ phía trên (2 `Rectangle`, tự update theo `%hp`). Chạm gần player (`CONTACT_RANGE`) thì tự gọi thẳng `combatManager.takeDamage()` (có cooldown 1s tránh trừ máu mỗi frame) — quái là bên GÂY damage cho player nên không cần scene làm trung gian, khác chiều player đánh quái (scene tính `computeDamage()` rồi gọi `monster.takeDamage(dmg)`).
  - **Chết thật** (case 12 combat.md): cộng EXP/gold (roll trong khoảng min-max) qua `combatManager`, roll `drop_table` cộng thẳng vào `inventoryManager` singleton (item rớt thật, không chỉ hiệu ứng hình ảnh) + hiện text bay lên báo số EXP/Gold vừa nhận, ẩn + tắt body, hồi sinh sau 5 phút thực (`RESPAWN_MS` cố định — **chưa phân biệt "hồi sinh khi vào lại khu vực" như mô tả đầy đủ trong `mechanics.md`**, đơn giản hoá vì chỉ có 1 map chiến đấu thật ở Sprint 5, để dành khi có nhiều map).
  - **[src/scenes/GrasslandScene.ts](../../src/scenes/GrasslandScene.ts)** (mới, Map 2 — Đồng Cỏ bản TỐI GIẢN chỉ đủ test combat — polish toàn bộ map 2-7 là Sprint 12) — spawn 2 `wild_rabbit`, nền cỏ vẽ bằng code (chấm mờ rải rác cho đỡ phẳng). Player chưa có `crit_chance` thật trong `PlayerStats` (chưa có UI phân bổ điểm) — dùng tạm số cố định nhỏ (5%) cho đòn thường để case "Chí mạng" thật sự xảy ra được lúc test/chơi, không phải luôn 0%.
  - **Case 13 (player chết)**: `combatManager.on('player-died', ...)` (tự gỡ khi scene shutdown) → `respawnAtVillage()` + fade về `GameScene`. **Chưa có Map 0 (Làng) thật** để respawn đúng nghĩa "tại làng" — tạm dùng điểm spawn mặc định của Farm (890,430) thay thế, cần đổi lại thành Village khi map đó được dựng (chưa nằm trong lịch trình gần).
  - Đã verify bằng Puppeteer: đánh chết quái → HP quái về 0/ẩn, EXP/Gold cộng đúng bảng `monsters.json` (wild_rabbit exp:15, gold 10-25), item rớt thật vào `inventoryManager` (`monster_core`/`rabbit_fur` đúng theo `drop_table`); quái đuổi theo + gây contact damage thật lên player trong lúc giao tranh (HP giảm quan sát được qua HUD).
- **`Weapon` — chưa tách thành component/class riêng**: vì chưa có hệ thống trang bị/inventory vũ khí thật (đổi tay vũ khí là việc Sprint 11 "hệ đổi class tại nhà"), "đang cầm" hiện chỉ là 1 field `weapon_id` cố định (`iron_sword`) trong `PlayerStats`, tra bonus ATK/multiplier qua `combatManager.getTotalAtk()`/`getWeaponMultiplier()`. Đủ để công thức damage đúng, nhưng CHƯA có chỗ đổi vũ khí trong game.
- **Đòn đánh thường — hitbox vào `Player.ts`** (không tạo `Weapon` class riêng như trên): phím Space đã có animation `attack` từ trước (Sprint 1), giờ thêm `this.emit('attack')` ngay lúc bắt đầu animation + `getAttackHitboxBounds()` (hình chữ nhật 42×42 đặt cách tâm player 34px theo đúng hướng `facing` đang quay mặt). Scene nghe `player.on('attack', ...)` rồi tự `Phaser.Geom.Rectangle.Overlaps()` so với `getBounds()` của Người Rơm/quái trong danh sách — tính hit NGAY lúc bấm (chưa rig theo frame nào trong animation là lúc "lưỡi kiếm chạm tới" thật, vì animation attack chỉ có 1 bản dùng chung mọi hướng, chưa đủ chi tiết để rig).
  - **Bug phát hiện lúc test bằng Puppeteer**: dùng `page.keyboard.press('Space')` (keydown+keyup gần như đồng thời) bắn liên tiếp làm MẤT một số lần bấm (`JustDown()` không nhận ra, có thể do race condition giữa CDP input dispatch và vòng lặp input của Phaser) — không phải bug code game, so sánh với `page.keyboard.down('Space')` + đợi 50ms + `up('Space')` (tách rời keydown/keyup) chạy đúng 100% (5/5 lần bấm = 5/5 lần trúng). **Bài học cho lần sau viết test bấm phím liên tục nhanh trong `dev-check.cjs`: ưu tiên `down()`+sleep ngắn+`up()` thay vì `press()` nếu thấy số lần trúng ít hơn số lần bấm** — cùng nhóm bài học "đặc thù môi trường test tự động" đã ghi nhận trước đây (khác chiều: lần trước là bấm dồn dập gây RA THỪA sự kiện, lần này là THIẾU sự kiện).
- **[src/scenes/TrainingGroundScene.ts](../../src/scenes/TrainingGroundScene.ts)** (mới, Bãi Tập Luyện) — map placeholder 700×550, nền be/nâu kẻ ô mờ (phân biệt rõ với cỏ Farm), spawn 5 `TrainingDummy` theo `trainingDummyPlacement.ts`, world bounds = kích thước map (không cần polygon phức tạp như Farm vì đây là sân trống hình chữ nhật đơn giản).
- **[src/systems/CombatTextures.ts](../../src/systems/CombatTextures.ts)** (mới) — texture PLACEHOLDER vẽ bằng code (canvas), đúng tinh thần các texture tạm khác đã có (`shadow_oval`, `moisture_overlay`...): `training_dummy` (cọc gỗ + bó rơm bầu dục + dây thừng), `wild_rabbit` (thân bầu dục xám + 2 tai dài + mắt đỏ), `hit_spark` (tia sáng bung khi trúng đòn), `straw_particle` (mảnh rơm vỡ), `slash_fx` (cung chém trắng, hiện chưa gắn vào animation attack — để dành khi rig theo frame thật). **Người Rơm chưa có prompt art-refs nào** (không phải kẻ địch nên không có trong `monsters.md`) — cần thêm mới khi gen ảnh thật; Thỏ Hoang đã có prompt sẵn ở `art-refs/enemies/monsters.md`.
- **`docs/data/asset-manifest.md`/`art-refs/` chưa cập nhật đường dẫn asset combat mới** (Người Rơm chưa có mục nào) — để dành cho phiên riêng làm asset thật, ngoài phạm vi "code trước, asset sau" của yêu cầu lần này.
- **Hotbar 5 ô chiêu thức** (user yêu cầu ngay sau Sprint 5, gửi kèm ảnh tham khảo 1 game khác có lưới ô gán chiêu) — phím SỐ 1-5 CHỌN ô (chỉ tô sáng viền vàng, chưa đánh), Enter đánh chiêu của ô đang chọn. Tách khỏi đòn thường (Space, vẫn luôn miễn phí/không đổi) — 2 cách tấn công độc lập, đúng phân biệt "Đòn thường" vs "Kỹ năng" trong `docs/gameplay/mechanics.md`.
  - **[src/systems/SkillHotbar.ts](../../src/systems/SkillHotbar.ts)** (mới) — `getHotbarSkills(weaponSkillClass)` lọc `skills.json` theo đúng hệ vũ khí đang cầm (`class` khớp `weapon.skill_class`) + bỏ hẳn Passive (tự động có hiệu lực, không cần ô riêng), sắp theo `skill_index`, đệm `null` cho đủ 5 ô — V1 chỉ có `quick_slash` (hệ Kiếm Sĩ) nên slot 1 = "Chém nhanh", slot 2-5 trống, tự lấp đầy khi Sprint 11 thêm chiêu 2-6 mà không cần sửa lại chỗ này. `SkillHotbar` (class) quản lý UI (5 ô vuông + số 1-5 + tên chiêu, viền vàng cho ô đang chọn) và state (`selectedIndex`, `cooldownReadyAt[]` mỗi ô). `tryCast(spendMp)` gộp đúng cả case 7 (đang cooldown → chặn) + case 8 (không đủ MP → chặn, không trừ âm — `CombatManager.spendMp()` mới thêm, trả `false` nếu không đủ) trong 1 hàm, bắt đầu cooldown NGAY khi cast thành công (đúng "cooldown tính từ lúc kích hoạt" trong mechanics.md), trả `null` nếu bất kỳ điều kiện nào không đạt — nơi gọi không được đánh gì khi nhận `null`.
  - **`bindSkillHotbarInput()`** (cùng file) — hàm dùng chung cho cả `TrainingGroundScene` lẫn `GrasslandScene` (tránh lặp code bind phím): 1-5 chọn ô, Enter gọi `player.canAttack()` trước (case: animation đang khoá thì không được trừ MP/cooldown cho 1 đòn không ra được) rồi mới `hotbar.tryCast()` + `player.startAttack({damageMultiplier})`.
  - **`Player.ts` refactor**: tách logic bắt đầu tấn công (animation + khoá di chuyển + emit sự kiện) từ chỗ gọi cứng trong nhánh Space ở `update()` thành method public `startAttack(payload?: {damageMultiplier})` — Space (đòn thường) gọi không truyền gì (multiplier mặc định 1), Enter+hotbar gọi kèm `damage_multiplier` của chiêu. Thêm `canAttack()` (đọc `!isAttacking`) để scene kiểm tra TRƯỚC khi trừ MP. Event `attack` giờ mang theo payload `{damageMultiplier}` thay vì rỗng.
  - **`GrasslandScene.handlePlayerAttack()`** đổi nhận tham số `skillMultiplier` (từ payload) thay vì hardcode 1 — nhân thêm với `combatManager.getWeaponMultiplier()` (2 hệ số độc lập: multiplier của CHIÊU × multiplier của VŨ KHÍ). `TrainingGroundScene` không cần đổi gì (Người Rơm chỉ đếm hit, không quan tâm damage/multiplier).
  - `CombatManager` thêm `getWeaponSkillClass()` (tra `weapons.json` theo `weapon_id` đang cầm, trả `skill_class` — mặc định `'swordsman'` nếu không tra được) để `SkillHotbar` biết lọc đúng bộ chiêu.
  - **Chưa có icon riêng cho từng chiêu** (`<id>_icon.png` theo asset-manifest.md mục 5 chưa gen) — dùng tạm tên chiêu viết chữ trong ô thay icon, đủ phân biệt ô có chiêu/ô trống.
  - Đã verify bằng Puppeteer: chọn ô trống (2-5) rồi Enter → không đánh gì, MP không đổi (case: ô trống thì không có gì để cast); chọn lại ô 1 ("Chém nhanh") đứng cạnh Người Rơm rồi Enter → trừ đúng 10 MP (200→190), Người Rơm nhận đúng 1 hit. Ảnh chụp xác nhận hotbar hiện đúng 5 ô ở giữa-dưới màn hình, ô đang chọn viền vàng, tên "Chém nhanh" hiện rõ ở ô 1, 4 ô còn lại hiện "—" xám mờ.
- **Chưa làm trong Sprint 5 (đúng lịch những sprint sau, không phải thiếu sót)**: đủ 5 hệ vũ khí + chiêu 2-10 mỗi hệ (Sprint 11 "hệ đổi class tại nhà"), UI phân bổ 10 điểm thuộc tính mỗi lần lên cấp, vật phẩm Đồng rơi ra đất nhặt lại được khi chết, Map 0 (Làng) thật để respawn đúng nghĩa, hồi MP theo trạng thái trong/ngoài combat (chưa có nhiều chiêu tiêu MP để thấy rõ nhu cầu hồi nhanh), icon chiêu thức thật, đồng hồ đếm ngược cooldown trực quan đầy đủ (đã có cơ chế chặn + hiển thị số giây, nhưng chưa test với chiêu cooldown > 0 thật vì `quick_slash` cooldown = 0).
- **🐛 Bug nghiêm trọng phát hiện + sửa ngay sau Sprint 5 (lúc làm Gà Quay)**: `GameScene.create()` chạy lại MỖI LẦN quay về Farm từ Bãi Tập Luyện/Đồng Cỏ (`scene.start()` tái dùng lại CÙNG 1 instance scene, không phải instance mới) — nhưng code cũ vô điều kiện `this.farmManager = new FarmManager(...)` và `this.timeManager = new TimeManager()` mỗi lần `create()` chạy, XOÁ SẠCH toàn bộ tiến độ ô đất (cuốc/trồng/độ ẩm/thời gian trồng) + đồng hồ trong game (nhảy về lại Ngày 1 — 06:00) mỗi khi đi đánh quái/tập luyện rồi quay lại — ảnh hưởng toàn bộ Sprint 2-4, không chỉ Sprint 5. Phát hiện được vì tính năng Gà Quay cần đúng cơ chế "giữ trạng thái qua lần `create()` lại" để biết đã ăn hay chưa, test thử mới lộ ra bug lớn hơn đang ẩn sẵn.
  - **Đã sửa**: đổi `this.farmManager = new FarmManager(...)` → `if (!this.farmManager) this.farmManager = new FarmManager(...)`, tương tự cho `timeManager` + gộp việc đăng ký 2 listener `dayStart` (tưới giếng, hồi Gà Quay) vào chung 1 biến `isFirstTimeSetup` để cũng chỉ đăng ký đúng 1 lần (không thì mỗi lần quay về Farm sẽ cộng dồn thêm 1 listener mới, khiến giếng tưới N lần lặp lại vào các sáng sau, N = số lần đã quay về). Các phần VISUAL (ảnh đất/cây/nhà/hàng rào/cổng dịch chuyển) vẫn phải tạo lại mỗi lần `create()` như cũ vì Phaser tự huỷ hết GameObject của scene khi `scene.start()` — chỉ STATE thuần (không phải GameObject, chỉ là field JS thường) mới cần giữ nguyên.
  - Đã verify bằng Puppeteer: cuốc 1 ô + tua nhanh giờ, đi 1 vòng Bãi Tập Luyện rồi quay lại → ô đất vẫn `tilled` (trước khi sửa: về lại `empty`), đồng hồ tiếp tục chạy đúng (trước khi sửa: nhảy về `06:00 Ngày 1`).
- **Gà Quay ở nông trại** (user yêu cầu: 1 vật hồi phục toàn phần đứng cố định, "lúc nào quay về cũng thấy được", ăn là mất, hồi đầy HP/MP) — **2 lần chốt cơ chế tái tạo**: lần 1 hiểu nhầm thành tái tạo mỗi SÁNG GAME (`dayStart`, đúng nhịp giếng tự tưới có sẵn); user sửa lại ngay: "chỉ cần ra map khác rồi vào lại nông trại là gà lại hiện chứ không phải chờ đến ngày 2" — nghĩa là tái tạo theo LẦN VÀO LẠI SCENE, không liên quan đồng hồ trong game. Đã sửa đúng theo ý sau.
  - **[src/data/roastChickenPlacement.ts](../../src/data/roastChickenPlacement.ts)** (mới) — vị trí cố định `(550,420)`, khoảnh cỏ mở giữa nhà và bờ sông tây, không đè ô đất/hàng rào/cổng dịch chuyển nào — verify walkable bằng đúng phương pháp Puppeteer đã dùng cho Exit Zone. Bán kính tương tác `ROAST_CHICKEN_INTERACT_RADIUS = 40`.
  - **`GameScene.placeRoastChicken()`**: NGƯỢC LẠI hoàn toàn với `farmManager`/`timeManager` phía trên (những cái đó cố ý GIỮ NGUYÊN qua các lần `create()` lại) — Gà Quay phải chủ động `this.roastChickenAvailable = true` ngay đầu hàm, tức RESET mỗi lần `create()` chạy (dù lần đầu boot game hay quay về từ Bãi Tập Luyện/Đồng Cỏ), không đọc giá trị cũ. `tryEatRoastChicken()` kiểm khoảng cách bằng đúng điểm chân player (`body.center.x`/`body.bottom`, giống mọi tương tác khác) + còn available thì gọi `combatManager.fullRestore()` (mới, hồi đầy CẢ HP lẫn MP cùng lúc, khác `heal()` chỉ hồi HP 1 lượng nhất định) rồi ẩn ảnh + đánh dấu hết. Enter ưu tiên thử ăn Gà Quay TRƯỚC khi rơi xuống nhánh xử lý ô đất (đặt xa khu ruộng nên hiếm khi xung đột thật). Con trỏ chỉ dẫn (`findInteractionTarget()`) cũng thêm nhánh báo Gà Quay, ưu tiên cao nhất, chỉ hiện khi còn available.
  - **`createRoastChickenTexture()`** (placeholder vẽ bằng code, chưa có prompt art-refs) — thân bầu dục nâu vàng cắm que xiên gỗ, 2 đùi gà nhô 2 bên, 1 vệt bóng sáng nhẹ cho có độ ngậy.
  - **`CombatManager.respawnAtVillage()`**: user yêu cầu thêm TRẦN cho case 13 (mất 10% Đồng khi chết) — thêm `MAX_DEATH_GOLD_LOSS = 30_000`, `goldLost = min(round(gold×10%), 30000)` thay vì trừ thẳng 10% không giới hạn như Sprint 5 gốc.
  - Đã verify bằng Puppeteer (cả 2 lần chốt cơ chế): đứng gần Gà Quay lúc đang mất máu/mana → Enter → hồi đúng ĐẦY 100% cả HP lẫn MP, `roastChickenAvailable` chuyển `false`, ảnh biến mất; bấm Enter lần 2 ngay tại chỗ (chưa rời Farm) → không hồi gì thêm (đúng ý cuối: KHÔNG tự có lại nếu chỉ đứng yên tại chỗ); đi 1 vòng Bãi Tập Luyện rồi quay lại Farm NGAY (vẫn Ngày 1, chưa qua đêm) → `roastChickenAvailable` đã về `true`, ảnh hiện lại đúng vị trí — đúng yêu cầu cuối "ra map khác vào lại là có".
- **Con trỏ ngắm mục tiêu + F2 đổi mục tiêu** (user yêu cầu ngay sau Gà Quay: "thêm con trỏ trỏ vào quái để biết đang chọn đánh quái nào, F2 chuyển sang mục tiêu khác nếu có gì ở gần đó") — chỉ áp dụng cho `GrasslandScene` (quái)/`TrainingGroundScene` (Người Rơm), KHÔNG áp dụng cho Farm (Farm đã có sẵn 1 hệ con trỏ tương tác khác dành cho ô đất/nhà/Gà Quay — 1-nearest, không cần cycle — quyết định không gộp 2 hệ khác bản chất vào chung 1 chỗ để tránh rủi ro regression).
  - **[src/systems/TargetSelector.ts](../../src/systems/TargetSelector.ts)** (mới) — class generic `TargetSelector<T extends Targetable>` dùng chung cho cả Monster lẫn TrainingDummy (interface `Targetable` chỉ cần `id`/`x`/`y`/`isValid()`). `update(candidates, playerX, playerY)` gọi mỗi frame: tự bỏ chọn nếu mục tiêu cũ hết hợp lệ (chết), tự chọn GẦN NHẤT nếu chưa có gì đang chọn, giữ nguyên lựa chọn cũ nếu vẫn còn hợp lệ (không tự nhảy sang con khác chỉ vì nó gần hơn). `cycleNext(candidates)` (gọi khi bấm F2) chuyển sang candidate kế tiếp theo đúng thứ tự mảng truyền vào, xoay vòng về đầu khi hết danh sách, không làm gì nếu ≤ 1 candidate.
  - `Monster`/`TrainingDummy` thêm field `readonly id: number` (truyền từ index lúc spawn: `MONSTER_SPAWNS.map((pos, index) => new Monster(..., index))`, tương tự `TRAINING_DUMMY_PLACEMENTS.map((p, index) => new TrainingDummy(..., index))`) + method `isValid()` (alias của `isAlive()` đã có sẵn, chỉ đổi tên để khớp interface `Targetable` dùng chung).
  - **[src/systems/CombatTextures.ts](../../src/systems/CombatTextures.ts)** thêm `TARGET_RETICLE_TEXTURE`/`createTargetReticleTexture()` — reticle đỏ kiểu "lock-on" (4 dấu ngoặc góc + chấm tâm), cố tình khác hẳn hình thoi vàng của con trỏ tương tác Farm (`INTERACTION_POINTER_TEXTURE`) để phân biệt rõ "đang ngắm để đánh" với "đang có thứ tương tác được".
  - `GrasslandScene`/`TrainingGroundScene` đều thêm 1 `Image` reticle (depth cực cao, luôn nổi trên mọi thứ) + phím F2 (`Phaser.Input.Keyboard.KeyCodes.F2`) trong `update()`: `JustDown(f2Key)` gọi `targetSelector.cycleNext(...)`, sau đó `targetSelector.update(...)` mỗi frame để đặt vị trí reticle theo mục tiêu đang chọn (ẩn hẳn nếu không còn mục tiêu nào sống). Cập nhật text hướng dẫn ở góc màn hình thêm "F2: đổi mục tiêu".
  - Đã verify bằng Puppeteer (cả 2 scene, dùng `down()`+sleep+`up()` cho F2 theo đúng bài học đã ghi ở mục "Bug phát hiện lúc test" phía trên): mặc định reticle tự khoá vào mục tiêu GẦN NHẤT; bấm F2 → reticle chuyển sang mục tiêu khác trong danh sách; giết hết toàn bộ quái trong `GrasslandScene` (gọi thẳng `takeDamage(9999)`) → reticle tự ẩn (`visible = false`) vì không còn candidate hợp lệ nào.

## Sprint 6 — Save/Load hoàn chỉnh ✅ (chốt Alpha)

- **Lưu/tải thật ra `localStorage`** — trước Sprint 6, `SaveManager.ts` chỉ có bộ khung (`createDefaultSaveState/saveGame/loadGame/hasSaveGame/clearSaveGame`) nhưng KHÔNG CHỖ NÀO trong game thật sự gọi `saveGame()`/`loadGame()` — `createDefaultSaveState()` chỉ dùng để seed số mặc định cho `combatManager` lúc khởi tạo module. `SaveState`/`FarmTile` trong `data/types.ts` cũng chưa từng khớp implementation thật của `FarmManager` (viết trước khi `FarmManager` được dựng, dùng `x/y`+`moisture` tính sẵn — khác hẳn `FarmTileRuntime` thật dùng `id`+mốc thời gian). Sprint 6 viết lại `SaveState`/`FarmTileSaveState` khớp đúng field runtime thật, xem `docs/data/data-schema.md` (đã cập nhật kèm cảnh báo divergence so với bản gốc).
  - **[src/data/types.ts](../../src/data/types.ts)**: `FarmTile` (cũ, không dùng ở đâu) đổi thành `FarmTileSaveState` (id, state, cropId, plantedAt, lastWateredAt, harvestCountRemaining, cycleHours, isRegrowCycle — khớp 1-1 `FarmTileRuntime` trừ metadata vị trí tĩnh). Thêm `PlayerPositionState` (`scene/x/y`), `GameTimeState` (`day/hour`). `SaveState` thêm `inventory`/`game_time`/`player_position`, giữ `animals`/`buildings_built`/`farm_decorations` = `[]` (chưa implement, Sprint 8+).
  - **Mỗi manager tự thêm `serialize()`/`loadState()` (hoặc `loadStats()`/`loadSlots()`) riêng** — không tập trung logic save/load vào 1 chỗ, để mỗi hệ thống tự chịu trách nhiệm đúng cấu trúc runtime của mình (đúng triết lý tách biệt state khỏi rendering đã dùng cho `FarmManager`/`InventoryManager` từ trước):
    - `FarmManager.serialize()`/`loadState(saved)`: khớp lại đúng ô theo `id` (bỏ qua id không khớp, phòng hờ đổi layout map — không throw giữa lúc load).
    - `TimeManager.serialize()`/`loadState(day, hour)`: lưu `hour` dạng số thực (không `Math.floor`) để không mất phần phút.
    - `InventoryManager.serialize()`/`loadSlots(saved)`: `loadSlots()` THAY TOÀN BỘ túi đồ (xoá sạch rồi nạp lại), không cộng dồn.
    - `CombatManager.loadStats(stats)`: thay toàn bộ `PlayerStats`, emit `stats-changed` để HUD tự cập nhật.
  - **[src/scenes/GameScene.ts](../../src/scenes/GameScene.ts)** — nơi DUY NHẤT gọi save/load thật (Bãi Tập Luyện/Đồng Cỏ không có farm/time state nên không cần):
    - Load: gộp vào ĐÚNG chỗ đang guard `isFirstTimeSetup` (tái dùng luôn tên biến đã có sẵn cho `farmManager`/`timeManager`, xem bug persistence đã sửa ở mục trên) — đọc `localStorage` TRƯỚC khi tạo `FarmManager` mới, áp `farm_tiles`/`player_stats`/`inventory`/`game_time` ngay, và override `lastKnownPosition` (field mới, giữ toạ độ Farm gần nhất — xem lý do dưới) làm toạ độ spawn ban đầu. Chỉ chạy ĐÚNG 1 LẦN lúc boot game thật (không phải mỗi lần quay về Farm) — dùng lại chính guard đã có, không cần cờ mới.
    - **Không làm màn hình "Continue/New Game" riêng** (dev-schedule.md ghi tuỳ chọn "nếu có save") — có save thì tự load thẳng luôn, im lặng, ưu tiên đúng "Done when" thay vì UI chrome ngoài phạm vi. Muốn chơi mới phải tự xoá `localStorage` (chưa có nút trong game) — ghi rõ ở đây để không hiểu lầm là thiếu sót ngoài ý muốn.
    - **`lastKnownPosition` (field mới, plain state — không phải GameObject)**: `this.player` (GameObject) bị Phaser huỷ ngay khi rời Farm sang Bãi Tập Luyện/Đồng Cỏ (`scene.start()`), nên KHÔNG đọc `this.player.x/y` được lúc lưu nếu đang ở map khác — field này được cập nhật mỗi frame lúc còn ở Farm (trong `update()`), giữ nguyên qua `create()` giống `farmManager`/`timeManager`, dùng làm nguồn vị trí AN TOÀN đọc bất cứ lúc nào.
    - **Chỉ resume vào lại `GameScene` (Farm)** dù lưu lúc đang đứng ở Bãi Tập Luyện/Đồng Cỏ — đơn giản hoá chủ đích (xem field `PlayerPositionState.scene` luôn là `'GameScene'`), vì "Done when" Sprint 6 chỉ yêu cầu giữ đúng farm + inventory + stats, không yêu cầu giữ đúng map chiến đấu đang đứng. Load thẳng vào combat scene cần `FarmManager`/`TimeManager` đã tồn tại từ trước (chưa có nếu chưa từng vào Farm) nên để dành, không nằm trong scope Alpha.
    - **`getSaveSnapshot()`/`saveProgress()`** (2 method mới) — `saveProgress()` gọi được BẤT CỨ LÚC NÀO kể cả khi `GameScene` đang dormant (không active), vì mọi field đọc vào đều là state thuần (farmManager/timeManager) hoặc singleton (combatManager/inventoryManager), không đụng GameObject nào có thể đã bị huỷ.
    - **Autosave theo NHIỀU điểm** (đúng dev-schedule.md: "theo khoảng thời gian HOẶC theo hành động quan trọng" — làm cả 2 cho chắc): định kỳ 30s thật (`this.time.addEvent`, đăng ký 1 lần lúc `isFirstTimeSetup`); `window.addEventListener('beforeunload', ...)` (lưới an toàn CHÍNH cho "tắt game" — đăng ký 1 lần, KHÔNG gỡ vì đây đúng là lúc cần fire); `this.events.once(SHUTDOWN, ...)` (rời Farm sang map khác, đăng ký lại MỖI `create()` vì `.once()` tự gỡ sau khi bắn); `combatManager.on('level-up'/'player-respawned', ...)` (đăng ký 1 lần lên singleton — bắt được cả lúc đang đánh quái ở map khác, `GameScene` không active); và trực tiếp ngay sau mỗi hành động nông trại quan trọng (`interactWithFarmTile()` — cuốc/tưới/hái/dọn héo/ăn Gà Quay; `confirmSeedMenu()` — trồng; `handleBulkActionsClick()` — 4 nút hàng loạt).
  - **Đã verify bằng Puppeteer** (`scripts/dev-check.cjs`, dùng thẳng `window.__game`/`window.__combatManager`/`window.__inventoryManager` đã expose từ Sprint 5):
    1. Cuốc+trồng 1 ô, cộng 500 Đồng, cộng 7 Cà rốt vào túi, tua đồng hồ +3h, gọi `saveProgress()` → `localStorage` có save → **reload trang thật** (`page.reload()`, không phải đổi scene) → ô đất/Đồng/túi đồ/ngày giờ đều đúng y hệt lúc lưu.
    2. Sau khi load, cuốc thêm 1 ô khác rồi đi 1 vòng Bãi Tập Luyện quay lại Farm → CẢ ô đã load lẫn ô mới cuốc thêm đều còn đúng (không bị load-lại-đè state cũ, đúng guard `isFirstTimeSetup` chỉ chạy 1 lần).
    3. Reset `localStorage`, đứng đúng vị trí 1 ô (bù lệch `body.center/bottom` theo đúng cách đã làm ở các test tương tác trước — xem ghi chú "Player body/feet coordinate gotcha"), bấm Enter THẬT (không gọi `saveProgress()` tay) → ô chuyển `tilled` VÀ `localStorage` cập nhật ngay theo đúng state mới — xác nhận hook autosave-theo-hành-động trong `interactWithFarmTile()` tự chạy đúng, không cần can thiệp thủ công.
  - **Chưa làm** (ngoài "Done when" Alpha, để dành sau): màn hình "Continue/New Game" thật (nút trong game, không phải tự xoá `localStorage`), resume thẳng vào Bãi Tập Luyện/Đồng Cỏ, nhiều save slot (hiện chỉ 1 slot cố định `shinobi_valley_save`), lưu `fertilizer_applied` (chưa có hệ phân bón thật, Sprint 7), lưu `animals`/`buildings_built` (chưa implement, Sprint 8+).

## Bảng nhân vật (Trang bị + Tiềm năng) ✅ — ngoài lịch trình, user yêu cầu trực tiếp

User gửi ảnh chụp UI 1 game khác, yêu cầu "tạo các tab tương tự như này: hành trang, tiềm năng (cộng mỗi khi tăng cấp), kĩ năng, thông tin, trang bị". 2 trong 5 tab (Tiềm năng, Trang bị) là hệ thống MỚI hoàn toàn — đã hỏi lại user 2 câu trước khi làm: (1) làm tới đâu ở tab Trang bị khi data chỉ có 1 vũ khí + 1 giáp → chọn "làm đủ khung UI + equip thật, thêm placeholder item cho 5 slot thiếu"; (2) có đổi luôn cách lên cấp hiện tại (tự cộng cứng stat) sang đúng "10 điểm tự do" trong `progression.md` không → chọn "đổi luôn theo đúng progression.md".

- **[src/systems/CharacterPanel.ts](../../src/systems/CharacterPanel.ts)** (mới) — class `CharacterPanel` gộp 5 tab (Hành trang/Tiềm năng/Kỹ năng/Thông tin/Trang bị), style bảng nâu gold khác hẳn màu xanh ngọc của các panel Farm cũ (menu hạt giống/bảng công cụ) để phân biệt "bảng nhân vật dùng chung mọi map" với "bảng chỉ có ở Farm". Đổi tab bằng click vào tên tab trên cùng.
  - **KHÔNG dùng `.setInteractive()`** cho bất kỳ nút/slot nào — object tương tác lồng trong Container `scrollFactor(0)` bị lệch hit-test khi camera đã cuộn khỏi world (0,0), bug đã phát hiện từ trước ở bảng Công Cụ Nông Trại (`GameScene.createBulkActionsUI()`). Tự ghi lại toạ độ từng vùng bấm (`ClickRegion`) + tự hit-test thủ công trong `handlePointerDown()`, đúng pattern đã dùng.
  - **Sống ở `UIScene`, KHÔNG sống ở scene gameplay đang active** — phát hiện bug thật lúc verify bằng Puppeteer: tạo panel bên trong `GameScene` lúc đầu, chữ HUD (`Lv./EXP...` của `UIScene`, launch SAU nên luôn vẽ đè lên) lộ ra xuyên qua panel dù panel có background gần đục 0.95 alpha — vì Phaser xếp thứ tự vẽ GIỮA CÁC SCENE theo thứ tự scene (`launch()`/`start()` sau vẽ đè lên trước), KHÔNG theo `depth` (depth chỉ so sánh được giữa object CÙNG 1 scene). Sửa bằng cách chuyển hẳn việc tạo `CharacterPanel` + bind phím C/Esc/click vào `UIScene.create()` — 3 scene gameplay (Game/Grassland/TrainingGround) chỉ ĐỌC lại instance qua `getCharacterPanel()` (tự `this.scene.get('UIScene') as UIScene`, có thể `undefined` đúng 1 khung hình đầu lúc boot, luôn dùng `?.`).
  - **Tab Hành trang**: lưới 8 cột, dùng lại đúng texture `crop_<id>_item` đã preload; item không có icon riêng (đồ rớt từ quái như `monster_core`/`rabbit_fur` — chưa từng có texture nào load cho chúng, lỗ hổng từ Sprint 5) hiện icon xám placeholder mới thêm (`createPlaceholderIconTexture()`) thay vì vỡ hình.
  - **Tab Tiềm năng**: hiện `free_points` chưa dùng + 8 dòng chỉ số (HP/MP/ATK/DEF/MoveSpeed/Crit/AttackSpeed/Luck) kèm nút `[+1 · Nđ]` — mờ (không đăng ký click) nếu không đủ điểm, KHÔNG ẩn hẳn để người chơi biết cần bao nhiêu.
  - **Tab Kỹ năng**: liệt kê `skills.json` lọc theo hệ vũ khí đang cầm (giống `getHotbarSkills()`), CHỈ XEM — gán chiêu vào ô hotbar vẫn dùng phím 1-5 ở `SkillHotbar` như cũ, 2 hệ tách biệt hoàn toàn.
  - **Tab Thông tin**: bảng chỉ số tổng hợp (gốc + bonus trang bị/vũ khí qua `getTotal*()`), chỉ xem, không có nút phân bổ (khác Tiềm năng).
  - **Tab Trang bị**: 7 ô slot (Vũ khí + 6 slot giáp) — click 1 ô XOAY VÒNG qua toàn bộ item hợp lệ của slot đó (kể cả "gỡ ra" với slot giáp), không làm picker list riêng vì mỗi slot hiện chỉ có 1-2 item thật. Cột phải hiện tổng bonus đang có từ trang bị đang mặc.
  - **Phím C** mở/đóng ở CẢ 3 scene (Game/Grassland/TrainingGround); **phím I** ở Farm mở thẳng tab Hành trang (giữ đúng thói quen bấm cũ Sprint 4) — thay thế HOÀN TOÀN bảng túi đồ riêng cũ (`createInventoryUI()`/`openInventory()`/`closeInventory()`/`refreshInventoryUI()`/`isPointerInsideInventory()` đã xoá sạch khỏi `GameScene.ts`, cùng các hằng số `INVENTORY_DEPTH`/`INVENTORY_GRID_*`/`INVENTORY_SLOT_*`).
- **`docs/gameplay/equipment.md`/`docs/planning/progression.md`** — 2 file spec đã có sẵn TỪ TRƯỚC (chưa từng code), lần này mới thật sự implement đúng theo đó. **`data/types.ts`**: `Armor` thêm 4 field bonus (`atk_bonus`/`mp_bonus`/`move_speed_bonus`/`luck_bonus`/`crit_bonus_percent`, mặc định 0 — bản gốc chỉ có `def`/`hp_bonus`, đủ cho `chest` nhưng thiếu chỉ số chính của 5 slot còn lại theo bảng "Chỉ số gốc theo slot"). `PlayerStats` thêm `move_speed`/`crit`/`attack_speed`/`luck`/`free_points`/`equipped_armor` — các field chỉ số cũ (`max_hp`/`max_mp`/`atk`/`def`...) giờ là chỉ số GỐC, tổng thật phải qua `CombatManager.getTotal*()`.
- **[public/data/armor.json](../../public/data/armor.json)** — thêm 5 item placeholder Common rank cho 5 slot chưa có data (`leather_helmet`/`leather_gloves`/`leather_boots`/`silver_ring`/`silver_necklace`, `unlock_level: 1`), số liệu lấy đúng cột Common trong bảng "Chỉ số gốc theo slot" equipment.md. `iron_chestplate` cũ thêm đủ 4 field mới (đều 0, không đổi số liệu gốc).
- **`CombatManager.ts`** — thêm nhiều method: `getTotalMaxHp/MaxMp/Crit/AttackSpeed/MoveSpeed/Luck()` (gốc + bonus armor/weapon, dùng chung 1 helper `getEquippedArmorList()` lọc bỏ slot trống/id không tra được), `equipArmor(slot, itemId|null)`/`equipWeapon(weaponId)` (chặn nếu item không khớp đúng slot, KHÔNG chặn theo `unlock_level`/sở hữu — đúng tinh thần "chưa có cơ chế chặn thật" đã áp dụng cho menu hạt giống Farm), `allocatePoint(stat)` (trừ/cộng theo đúng bảng chi phí trong progression.md, trả `false` nếu không đủ điểm). `heal()`/`fullRestore()`/`respawnAtVillage()` đổi sang dùng `getTotalMaxHp/MaxMp()` thay vì đọc thẳng `stats.max_hp/max_mp` (đọc thẳng giờ chỉ còn là số GỐC, sai nếu có trang bị).
  - **`gainExp()` đổi hành vi lên cấp** (user xác nhận đổi luôn) — KHÔNG còn tự cộng cứng `max_hp/+50, max_mp/+20, atk/+3, def/+1` mỗi cấp (`LEVEL_UP_GROWTH` cũ, Sprint 5) — thay bằng cộng `FREE_POINTS_PER_LEVEL = 10` điểm vào `stats.free_points`, người chơi tự phân bổ ở tab Tiềm năng. Đường cong `exp_to_next` (hệ số nhân 1.25) giữ nguyên, chưa đổi sang đúng bảng EXP curve thật trong `progression.md` (ngoài phạm vi lần này).
- **`GrasslandScene.handlePlayerAttack()`**: Chí mạng đòn thường đổi từ số cố định `0.05` (Sprint 5, tạm vì `PlayerStats` chưa có field `crit`) sang đọc THẬT `combatManager.getTotalCrit() / 100`.
- **`CombatHud.ts`**: `playerMaxHp`/`playerMaxMp` ghi vào registry đổi sang `getTotalMaxHp()`/`getTotalMaxMp()` — HUD giờ hiện đúng số ĐÃ CỘNG bonus trang bị, không phải số gốc.
- **`docs/data/data-schema.md`**: cập nhật ví dụ `armor.json` (4 field mới) và `save_state.json` (`player_stats` thêm field mới).
- Đã verify bằng Puppeteer: mở bảng bằng C, đổi qua đủ 5 tab; `gainExp(100000)` lên 24 cấp → `free_points = 240` đúng (24×10); bấm nút phân bổ HP → `max_hp` 500→600, `free_points` trừ đúng 1; qua tab Trang bị bấm slot Mũ → equip `leather_helmet`, `getTotalMaxHp()` = 700 (600 gốc + 100 bonus mũ) đúng; Esc đóng bảng; **reload trang thật** → `free_points`/`max_hp`/`equipped_armor` giữ đúng nguyên (tự động qua hệ Save/Load Sprint 6 có sẵn, không cần sửa gì thêm vì mọi field mới đều nằm trong `PlayerStats`); mở bảng ở `GrasslandScene` → hiện đúng, không bị đè bởi HUD (xác nhận bug layering đã sửa đúng).
- **Chưa làm** (ngoài phạm vi lần này): shop/craft/drop thật cho trang bị (hiện coi như "unlock hết" giống crop Farm), UI reroll thuộc tính phụ/cường hóa (+1 → +10, đó là Sprint 14), đường cong EXP curve thật theo bảng trong progression.md, giao diện chọn item trong tab Trang bị (hiện chỉ xoay vòng qua click vì mỗi slot có ≤2 item thật).

## Nhân dạng "Vegeta" (thử nghiệm) ✅ — ngoài lịch trình, ngoài spec 2 giới tính gốc

User cung cấp 31 file sprite thô (`public/assets/sprites/player/vegeta/raw/`, rip từ 1 animation "xoay
người/tung đấm" kiểu Vegeta trong Dragon Ball — không phải asset tự vẽ cho dự án này) đã cắt sẵn thành đầu/
thân/chân rời, yêu cầu ghép lại thành nhân vật chơi được thứ 3. **Lệch khỏi thiết kế gốc** — `asset-manifest.md`
ghi rõ "Game có 2 giới tính chọn lúc tạo nhân vật... mọi asset player đều tách theo men/women" — coi đây là bổ
sung thử nghiệm ngoài spec, KHÔNG sửa lại tài liệu thiết kế gốc (men/women vẫn là 2 giới tính chính thức).

- **Không có metadata toạ độ/pivot** giữa 31 mảnh — đã hỏi lại user 2 lần trước khi làm: (1) chấp nhận ghép
  bằng mắt, lặp lại nhiều vòng cho tới khi khớp thay vì có nguồn tham khảo chính xác; (2) chấp nhận 1 số pose
  không đúng ngữ nghĩa dáng đi 100% (asset gốc là combo tung đấm, không phải chu kỳ bước đi thật) — miễn khớp
  hình, không hở/lệch.
- **[scripts/build-vegeta-sprites.py](../../scripts/build-vegeta-sprites.py)** (mới, cần `pip install pillow`)
  — đọc từng bộ 3 (đầu, thân+tay, hông+chân) từ `raw/`, chồng lên nhau theo toạ độ CĂN GIỮA trục X + độ lấn
  (overlap) dò bằng mắt qua nhiều vòng thử-xem-ảnh-chỉnh (test trực tiếp bằng Puppeteer, phóng to 3-4x để soi
  từng pixel chỗ nối). Nhiều mảnh "chân xoay chéo" (dáng chạy động) không khớp bằng phép chồng đơn giản (điểm
  neo lệch tâm khác hẳn so với mảnh đứng yên) — thay vì cố ép, dùng lại 2 mảnh chân đứng/bước đã test ổn định
  (`6044`/`6045`) cho mọi hướng đi thay vì ép dùng mảnh chéo bị hở hình.
  - **Cả 5 action dùng CHUNG đúng 1 kích thước cell (152×190)** — phát hiện `Player.ts` chỉ tính 1
    `FRAME_SIZES`/`shadowOffsetY` DUY NHẤT cho toàn bộ giới tính (không tách theo action như men/women vốn đã
    gần đều nhau) — nếu để mỗi action 1 size khác nhau (ảnh ghép thật ra khác size nhau theo pose), nhân vật sẽ
    "nhảy" vị trí/bóng đổ mỗi lần đổi animation. Lấy max width/height qua toàn bộ 11 frame rồi pad về chung 1
    cell, neo đáy (chân luôn chạm cạnh dưới cell) để giữ baseline đồng nhất.
  - Kết quả: `idle_front` (2 frame), `walk_front`/`walk_back`/`walk_side` (2 frame/hướng — giới hạn đúng số pose
    ghép được từ 31 mảnh gốc, không đủ để làm chu kỳ bước chân thật như men/women 8 frame), `attack` (3 frame:
    lên gối → tung 2 đấm → thu tay).
- **`Player.ts`**: `Gender` thêm `'vegeta'`, `FRAME_SIZES`/`SPRITE_SCALE` (0.35, canh gần đúng chiều cao hiển
  thị của `women` để đồng bộ tỉ lệ) /`WALK_CONFIG` (`frames: 'all', stillFrame: 0`, giống cách `women` dùng hết
  toàn bộ frame ghép được).
- **`PreloadScene.ts`**: thêm `vegeta` vào `frameSizes` (152×190 cho cả 5 action) + vòng lặp load spritesheet.
- **`GameScene.ts`/`GrasslandScene.ts`/`TrainingGroundScene.ts`**: đổi tạm `new Player(..., 'women')` →
  `'vegeta'` để thấy/test ngay trong game — **chưa có màn hình chọn nhân dạng thật** (cùng tình trạng với
  men/women trước đó, chưa từng có UI chọn giới tính), đổi code trực tiếp là cách duy nhất để xem nhân dạng nào
  đang active.
- Đã verify bằng Puppeteer (chạy game thật, không chỉ xem ảnh ghép rời): idle đứng yên, đi bộ cả 3 hướng
  (trước/sau/nghiêng), tấn công — không lỗi tải asset mới (chỉ còn lỗi 404 cũ đã biết, không liên quan), nhân
  vật hiện đúng tỉ lệ so với map/bóng đổ/con trỏ tương tác, không "nhảy" vị trí khi đổi animation.
- **Chưa làm / giới hạn đã biết**: chưa có UI chọn nhân dạng thật; poses walk không phải chu kỳ bước chân
  chuẩn (chấp nhận theo yêu cầu user); nếu sau này có thêm frame gốc mới, chạy lại
  `scripts/build-vegeta-sprites.py` sau khi thêm entry vào `ACTIONS` + đặt file mới vào `raw/`.

## Sprint 7+ — chưa bắt đầu (Phase 2 — Beta)

Xem chi tiết từng sprint ở [dev-schedule.md](dev-schedule.md) Phụ lục C.
